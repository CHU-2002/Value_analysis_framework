---
batch: 2026-09-20-REQ-007
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-007
base: 9735c84
full-suite: "1467 passed, 3 skipped, 覆盖率 76.8%"
---

# 独立验收报告：REQ-007 门禁与治理工具加固

## 独立验收声明

本报告由独立 agent 出具：未参与 REQ-007 的实现，未查看开发过程，只读取仓库内的
需求条目、脚本、测试与文档，并以「试图证伪」为前提自行构造输入复现判定。所有结论
均附文件:行或命令输出摘要，可独立重跑。

三轮验收过程（同一独立评审者）：

- **第一轮**（基线 `3b76914`/`e68e05d`）：AC-1、AC-2、AC-4、AC-5、AC-6、AC-7 成立；
  **AC-3 部分不成立**（「超出预算」单测不隔离）、**AC-8 不成立**（夹具仍用 `develop`）。
- **第二轮**（基线 `229593e`）：需求方修复后重验，8 条 AC 全部成立；并发现报告里字面引用
  未勾选示例行会被 `acceptance_gate` 正则误判，已修正措辞。
- **第三轮（本轮，基线 `9735c84`）**：PR #29 以 squash `c65b47e` 合入 `main` 后，需求方在
  `docs/req-007-verified` 分支做收尾：把 REQ-007 状态推进到 `verified` 并补上验收戳，
  同时给台账声明的 4 个测试文件补 `REQ-007` 覆盖注释（**仅注释，无行为变化**）。
  本轮在该 HEAD 上重验：8 条 AC 仍全部成立，无阻断项。

核验对象与基线说明：分支 `docs/req-007-verified`，`base` = `git rev-parse --short HEAD` =
`9735c84`。工作树在本轮开始与结束时都只有本报告一个文件被改动（`git status --porcelain`
无其它条目）。破坏性验证（AC-3 的检查禁用、AC-5 的临时目录）均已完全恢复，复核方式与结果
见对应条目。本报告将作为独立提交 `docs(verification): …` 落库，不再与代码/台账改动混在一起。

耗时说明：本报告不把墙钟耗时作为判据。核验中出现的墙钟时间会受机器休眠/挂起影响，
不以 `real` 推断测试或门禁有问题。

## 全量测试

```bash
make verify
```

- 结果：`1467 passed, 3 skipped in 54.11s`（`make verify` 退出码 0）
- 覆盖率：`Required test coverage of 74% reached. Total coverage: 76.76%`（TOTAL 11141 语句 / 2589 未覆盖，即报告口径 76.8%）
- 分项门禁：`lint` 通过；追溯门禁 `8 passed`；`test_scope --check` → `测试 scope 检查通过：33 支文件、1470 个用例，均在预算内`；回归门禁 → `main 批量回归门禁通过（自 e453255 起累积的特性合入未达阈值 3）`
- 基线（对比的 HEAD sha）：`9735c84`
- 跳过的 3 条均为 `TUSHARE_TOKEN not set` 的 live API 测试（`tests/test_integration.py:30/76/110`），符合「全 mock、离线、无 Token」约束
- 与第二轮同值（`1467 passed / 3 skipped`、76.76%），本轮只改注释与台账，未引入回归

## 逐条验收

### REQ-007 门禁与治理工具加固

- [x] **AC-1**：成立（第三轮重跑，无回归）。`scripts/pr_body_guard.py` 判据只看
  `## 需求编号` 小节是否 `meaningful`（`:61-67`），HTML 注释先被剥离（`:30-31`）。
  实跑：「小节空 + 正文写 REQ-003」→ 退出码 1；「小节只有 `<!-- REQ-003 -->`」→ 退出码 1；
  「小节填 `REQ-007` 且自测栏填实」→ 退出码 0。报错文本明确写出「正文其它地方提到编号不能替代」。
- [x] **AC-2**：成立（第三轮重跑，无回归）。`registered_requirement_ids()` 取
  `docs/requirements/REQ-*.md` 已登记编号（`:138-140`），`ownership_problems()` 对归属列
  每个 `REQ-NNN` 校验并报出文件与编号（`:143-154`），`--check` 调用它（`:202`）。
  实测：运行期拼接的未登记编号 `REQ-777` 被报为
  `tests/test_temp_probe.py 的归属编号 REQ-777 未在 docs/requirements/ 登记…`；
  未在仓库留下任何未登记编号字面量。
- [x] **AC-3**：成立（第三轮重跑 + 重新证伪）。`_run_check` 的 `registry=` 参数
  （`tests/test_test_scope.py:104-118`）把 `SCOPE_PATH` 指向与 `entries` 对齐的最小登记表
  （`_write_scope`，`:88-101`），三条失败路径各自隔离并断言具体报错文本（`:121-139`）。
  本轮该文件只多了一行 `REQ-007` 覆盖注释，逻辑未变；我仍按「关掉被守护的检查、对应测试
  必须失败」重新证伪（临时改 `scripts/test_scope.py`，`cp` 备份还原 + sha256 复核，原值
  `f02ea4a3562574ab4f2c074c90ae8e992719b6611de5628d20afa6c3af1e4f63`）：
  1. `if len(entries) > MAX_TEST_FILES:` → `if False:` ⇒ `test_check_fails_when_over_budget` **FAILED**；
  2. `if missing:` → `if False:` ⇒ `test_check_fails_when_a_test_file_is_unregistered` **FAILED**；
  3. `problems.extend(ownership_problems(entries))` → `problems.extend([])` ⇒
     `test_check_fails_on_an_unregistered_ownership_id` **FAILED**。
  三个用例单独失败、恢复后 `scripts/test_scope.py` sha256 回到原值且 `git status` 为空；
  基线 `pytest tests/test_test_scope.py` → `13 passed`。
- [x] **AC-4**：成立（第三轮重跑，无回归）。`validate_record` 深入 `## 逐条验收` 小节内校验
  （`scripts/regression_gate.py:127-138`）。实测：「（待补）」→ FAIL；只有 HTML 注释 → FAIL；
  含 `AC` 结论勾选行 → PASS；含 `REQ` 编号勾选行 → PASS；指向 `docs/verification/…` → PASS；
  写「本批无功能改动」→ PASS；小节整体缺失 → FAIL。
- [x] **AC-5**：成立（第三轮重跑，无回归）。守卫 `os.walk` 全仓 + 显式排除
  `{.git,.venv,output,docs,tests,node_modules,__pycache__}`，基线生产者以
  `BASELINE_PRODUCER_DOCS` 白名单豁免（`tests/test_update_docs_contract.py:186-202`；
  本轮该文件只多了一行覆盖注释）。独立扫描全仓：87 个 `.md`，命中者恰为三个白名单文件，
  **非豁免命中为空**。
- [x] **AC-6**：成立（第三轮重跑，无回归）。
  `tests/test_two_layout_e2e.py::test_incremental_run_runs_prepare_with_prior_analysis`
  （`:119-171`，本轮只多一行覆盖注释）真跑
  `prepare_run(update_dir, …, primary_period="2026H1", prior_analysis=baseline_dir/"synthesis"/"result.json")`，
  断言 `manifest["primary_period"]`、`manifest["inputs"]` 含 `prior_analysis`、证据索引
  `sources` 含 `prior_analysis`、`build_module_context("period_delta", …)` 选中它且
  `missing_prior_analysis == []`、baseline 仍 `structured`。`pytest tests/test_two_layout_e2e.py`
  → `6 passed`。
- [x] **AC-7**：成立（第三轮重跑，无回归）。`docs/DEVELOPMENT.md:160` 为「以 `make cov` 实测
  为准（墙钟时间会被机器休眠/挂起显著拉长，不作为判据）」，无写死数值；`docs/TESTING.md:82-84`
  与 `CHANGELOG.md` 不再写死测试数量、指向 `docs/TEST_SCOPE.md`。复扫三份文档无「N passed /
  N skipped」「N 支文件 / N 用例」写死数字；仅剩覆盖率一位小数（76.8%）与稳定 scope 上限 40/1600。
- [x] **AC-8**：成立（第三轮重跑，无回归）。`tests/test_release_gates.py:318` 为
  `feat/periodic-update`；本轮该文件只在「覆盖需求」注释里加了一行 `REQ-007`。
  `grep -rn develop tests/*.py` 无命中（退出码 1）；`pytest tests/test_release_gates.py`
  → `28 passed`。

## 治理一致性复核

本轮改动正是治理门禁抓出的「台账声明 vs 实际标注」不一致，我独立复核如下：

- **台账声明的关联测试 == 实际标注 REQ-007 的文件**：台账 `REQ-007` 主行列 4 个文件；
  独立扫描 `tests/` 全目录，实际含 `REQ-007` 字面量的文件恰为同样 4 个
  （`test_release_gates.py`、`test_test_scope.py`、`test_two_layout_e2e.py`、
  `test_update_docs_contract.py`），双向差集均为空。**一致成立。**
- 标注内容与实际覆盖的 AC 吻合：`test_release_gates.py`→AC-1/AC-4/AC-8、
  `test_test_scope.py`→AC-2/AC-3、`test_two_layout_e2e.py`→AC-6、
  `test_update_docs_contract.py`→AC-5；AC-7 是 prose 文档类，无测试覆盖，未强行标注。
- 台账行集合 == `REQ-*.md` 文件集合（`REQ-001`…`REQ-008`）；台账状态与条目 front matter
  状态一致；无悬空 `REQ-NNN` 引用；`REQ-007` 状态 `verified`、`pr: "#29"`、验收戳
  （复验 `229593e`、合并 `c65b47e`）齐备；`docs/requirements/README.md` 一览表同步为 `verified`。
- `make verify` 里的追溯门禁 `8 passed`，未因新注释触发悬空引用或「已交付无测试引用」。

## 未通过 / 存疑项

1. **无阻断项**：8 条 AC 全部成立。第一轮的 AC-3、AC-8 已在 `229593e` 修好，第三轮确认未回归。
2. **新增非阻断观察：`docs/TEST_SCOPE.md` 的「归属需求」列已过期**。该表由
   `scripts/test_scope.py --write` 自动生成、禁止手工编辑，但本轮给 4 个测试文件补
   `REQ-007` 标注后没有重新生成它，于是表里这 4 行仍是 `REQ-003, REQ-005, REQ-006` /
   `REQ-005` 等旧值。因为 `--check` 只比对**文件集合**、预算与归属编号是否已登记，
   **不比对「归属需求」列内容**，所以 `make verify` 仍全绿、掩盖了这处漂移。
   我用只读方式 `diff` 了当前文件与 `render(collect_scope(), 1470)`：差异仅这 4 行的归属列。
   建议合入报告时顺带跑 `make scope-write` 并提交新表（这不会改变任何门禁判定）。
   注意：这不影响 AC-2/AC-7 的字面要求（AC-2 只要求归属编号已登记，AC-7 只禁写死数量与耗时），
   但它是与本次修复同一类「声明 vs 事实」的残留。
3. **观察（非阻断）AC-4 口径**：小节内**裸写** `REQ-007`（非勾选行）不算有效结论、会被拒；
   `REQ-NNN` 需写在勾选行里。与 AC 文案括号写法有细微歧义，我按「勾选行内」理解，认为可接受。
4. **观察（非阻断）文档里的模块级覆盖率整数**：`docs/TESTING.md:89-91` 仍列各模块覆盖率
   （如 `valuation_engine.py` 34%）。属覆盖率信息、非测试数量/耗时，AC-7 允许保留。
5. **前一轮观察已消解**：squash 后的 `c65b47e` 提交信息不再写死测试计数（旧 `3b76914` 的
   「1466 passed」已不在 main 历史里）；报告提交归属问题已按建议改为单独提交。

## 结论

**通过**。8 条 AC 全部成立：AC-1、AC-2、AC-3、AC-4、AC-5、AC-6、AC-7、AC-8。
第三轮改动为测试文件覆盖注释、需求条目状态/验收戳、台账与一览表同步，**未触及任何门禁行为**；
`make verify` 全绿（`1467 passed, 3 skipped`，覆盖率 76.76%/76.8%），AC-1/2/4/5/6/7 无回归，
AC-3 的隔离性经重新证伪仍成立，AC-8 的夹具已统一为 `feat/…`。

对「补 REQ-007 标注」这个修法的评价：**认可，且是正确的最小修法**。理由：治理门禁
（`tests/test_requirement_traceability.py::test_delivered_requirements_are_covered_by_tests`，
`:183-193`）要求 `implemented`/`verified` 的需求被至少一个测试引用；REQ-007 置 `verified`
后必须在测试里有真实标注。而这 4 个文件确实各自覆盖了 REQ-007 的 AC（对应关系见上），
所以「如实补标注」比「把文件从台账关联列删掉」（那会让已验证需求无处落地、且门禁照样红）
更正确。唯一要补的是配套的 `make scope-write`（见存疑项 2），让自动生成的 scope 表与事实一致。

治理一致性：台账与 `REQ-*.md` 文件集合一致（`REQ-001`…`REQ-008`），台账声明 == 实际标注，
状态两处同步，无悬空引用。仓库工作树在本轮结束时除本报告外干净，破坏性验证（AC-3 的检查禁用、
AC-5 的临时目录）均已完全恢复并复核。本报告全部 AC 为 `- [x]`；以本报告调用
`scripts/acceptance_gate.evaluate`（批次需求 `REQ-007`）返回空问题列表，即 `acceptance_gate` 通过。
