---
batch: 2026-09-20-REQ-007
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-007
base: e68e05d
full-suite: "1467 passed, 3 skipped, 覆盖率 76.8%"
---

# 独立验收报告：REQ-007 门禁与治理工具加固

## 独立验收声明

本报告由独立 agent 出具：未参与 REQ-007 的实现，未查看开发过程，只读取仓库内的
需求条目、脚本、测试与文档，并以「试图证伪」为前提自行构造输入复现判定。所有结论
均附文件:行或命令输出摘要，可独立重跑。

核验对象与基线说明：验收期间分支 `feat/req-007-governance-hardening` 的工作树始终干净
（`git status --porcelain` 为空）。开始核验时 HEAD 为 `3b76914`；核验过程中需求方在
其上提交了 `e68e05d docs: correct the wrong attribution of the wall-clock stalls`
（仅改 `docs/DEVELOPMENT.md`、`docs/TESTING.md`、`docs/verification/2026-09-20-REQ-005-reverify.md`
三个文档，未触碰脚本/测试）。本报告以最终 HEAD `e68e05d` 为基线。破坏性验证（AC-5）在仓库内
临时新建了 `req007_break_probe/spec.md`，验证后已删除，`git status --porcelain` 与
`git rev-parse HEAD` 均复原（详见 AC-5）。除本报告外未新建、未修改、未提交任何文件。

耗时说明：本报告不把墙钟耗时作为判据。核验过程中出现的墙钟时间主要由机器休眠/挂起决定，
不以 `real` 推断测试或门禁有问题。

## 全量测试

```bash
make verify
```

- 结果：`1467 passed, 3 skipped in 58.71s`（`make verify` 退出码 0）
- 覆盖率：`Required test coverage of 74% reached. Total coverage: 76.76%`（TOTAL 11141 语句 / 2589 未覆盖，即报告口径 76.8%）
- 分项门禁：`lint` 通过；追溯门禁 `8 passed`；`test_scope --check` → `测试 scope 检查通过：33 支文件、1470 个用例，均在预算内`；回归门禁 → `main 批量回归门禁通过（自 e453255 起累积的特性合入未达阈值 3）`
- 基线（对比的 HEAD sha）：`e68e05d`
- 跳过的 3 条均为 `TUSHARE_TOKEN not set` 的 live API 测试（`tests/test_integration.py:30/76/110`），符合「全 mock、离线、无 Token」约束

观察（非阻断）：实现提交 `3b76914` 的 commit message 写「全量 1466 passed / 3 skipped」，
本次实测为 `1467 passed`。commit message 不是 AC-7 所指的 prose 文档，不影响 AC 判定，
但数字与实测不一致，记录备查。

## 逐条验收

### REQ-007 门禁与治理工具加固

- [x] **AC-1**：成立。`scripts/pr_body_guard.py` 删除了「正文任意位置有 `REQ-NNN`」兜底，
  判据只看 `## 需求编号` 小节是否 `meaningful`（`scripts/pr_body_guard.py:61-67`，占位集合见 `:23`，
  HTML 注释在 `:30-31` 先被剥离，故注释不算内容）。实跑四种输入：
  「小节空 + 正文写 REQ-003」→ 退出码 1；「小节只有 `<!-- REQ-003 -->`」→ 退出码 1；
  「完全无该小节、正文写 REQ-003」→ 退出码 1；「小节填 `REQ-007` 且自测栏填实」→
  退出码 0（`PR 描述校验通过（目标分支 feat/xxx）。`）。报错文本明确写出「正文其它地方提到编号不能替代」。
- [x] **AC-2**：成立。`registered_requirement_ids()` 从 `docs/requirements/REQ-*.md` 取已登记编号
  （`scripts/test_scope.py:138-140`），`ownership_problems()` 对归属列里每个 `REQ-NNN` 校验并报出
  文件与编号（`:143-154`），`--check` 主流程调用它（`:202`）。实测：登记编号集合为
  `['REQ-001'…'REQ-008']`；以运行期拼接的未登记编号 `REQ-777` 构造条目，
  `ownership_problems` 返回 `tests/test_temp_probe.py 的归属编号 REQ-777 未在 docs/requirements/ 登记…`；
  把 `--check` 的登记表与条目对齐到该编号后跑 `main(['--check'])` → 退出码 1 且同一条报错。
  未在仓库留下任何未登记编号字面量（用完即恢复，工作树干净）。
- [ ] **AC-3**：**部分不成立**。`tests/test_test_scope.py` 单独跑为 `13 passed`，三条失败路径
  也确实各有一个测试函数，但其中「超出预算」这条的单测**不能证伪其目标路径**：
  `_run_check` 只 monkeypatch 了 `collect_scope`/`collect_cases_via_pytest`/预算常量，未替换
  `SCOPE_PATH`；于是 `collect_scope` 返回的单文件条目对登记表而言同时触发「登记表里有已不存在的
  测试文件」（stale）失败，`main` 无论预算检查是否存在都会返回 1。实测：把 `ownership_problems`
  置空并令 `MAX_COLLECTED_CASES` 极大（即关掉预算检查本身），用与 `test_check_fails_when_over_budget`
  完全相同的夹具调用 `main(['--check'])`，仍得 `rc = 1`。也就是说删掉预算检查，该测试照过。
  「文件集合漂移」一条是隔离的（若删掉漂移检查，同夹具返回 0，测试会失败）；
  「归属未登记」在 `main` 层同样不隔离（关掉 `ownership_problems` 仍得 `rc = 1`），
  但另有函数级直测 `test_ownership_problems_names_the_file_and_id`（`tests/test_test_scope.py:109-112`）兜底。
  结论：三条里「超出预算」缺少能守住该失败路径的有效直接单测。
- [x] **AC-4**：成立。`validate_record` 会深入 `## 逐条验收` 小节内部，仅当小节内匹配到
  `- [x]/[ ] **AC-n**`、`- [x]/[ ] **REQ-nnn**`、或含 `docs/verification`、或含「无功能改动」
  时才通过（`scripts/regression_gate.py:127-138`）。实测八种记录：「（待补）」→ FAIL；
  只有 HTML 注释 → FAIL；`- [x] **AC-1**：成立` → PASS；`- [ ] **AC-2**：不成立` → PASS；
  `- [x] **REQ-007**` → PASS；指向 `docs/verification/…` → PASS；写「本批无功能改动」→ PASS；
  小节整体缺失 → FAIL（另报「缺少小节」）。
- [x] **AC-5**：成立。守卫改为 `os.walk` 全仓 + 显式排除
  `{.git,.venv,output,docs,tests,node_modules,__pycache__}`（`tests/test_update_docs_contract.py:186-202`），
  基线生产者以显式白名单 `BASELINE_PRODUCER_DOCS`（两份 `business-analysis.md` 与
  `shared/qualitative/coordinator_v2.md`）豁免（`:198-202`）。我按同一正则/排除/白名单独立扫全仓：
  共 87 个 `.md`；命中者恰为三个白名单文件（`.claude/commands/business-analysis.md`、
  `.opencode/commands/business-analysis.md`、`shared/qualitative/coordinator_v2.md`），
  **非豁免命中为空**。破坏性验证：在仓库内新建 `req007_break_probe/spec.md` 写入
  `{output_dir}/qualitative_input.json`（未改测试），
  `pytest tests/test_update_docs_contract.py -k never_use_a_flat` 立即失败，报错点名
  `req007_break_probe/spec.md: {output_dir}/qualitative_input.json`，证明**新目录无需改测试即被覆盖**；
  删除后 `git status --porcelain` 为空、`git rev-parse HEAD` 仍为 `e68e05d`，工作树完全恢复。
- [x] **AC-6**：成立。`tests/test_two_layout_e2e.py::test_incremental_run_runs_prepare_with_prior_analysis`
  （`:119-171`）真的走完整增量链路：`adopt_legacy` 取 baseline → `create_run(kind="report-update",
  primary_period="2026H1", supersedes=…)` → 在 run 私有 `inputs/` 放入 `data_pack_market.md` 与
  `pdf_sections_2026H1.json` → `prepare_run(update_dir, …, primary_period="2026H1",
  prior_analysis=baseline_dir/"synthesis"/"result.json")`。断言不是「文件存在」：
  `manifest["primary_period"] == "2026H1"`、`"prior_analysis"` 出现在 `manifest["inputs"]` 的
  `source_id` 集合、证据索引 `index["sources"]` 含 `source_id == "prior_analysis"`、
  `build_module_context("period_delta", …)` 选出的 evidence 含 `prior_analysis` 且
  `selection["missing_prior_analysis"] == []`、以及 `resolve_qualitative_input(baseline_dir)["source"]
  == "structured"`。代码侧对应关系已核对：`scripts/results/prepare.py:158-159` 注册
  `prior_analysis` 证据源、`:243`/`:255-256` 写 manifest，`scripts/results/context.py:63`/`:159`/
  `:293-296` 供 `period_delta` 选中并统计缺失。复跑 `pytest tests/test_two_layout_e2e.py` → `6 passed`，
  其中该用例单独复跑 `1 passed`。断言非平凡：若 `prior_analysis` 未接线，
  `tests/test_results_pipeline._write_complete_structured_run` 造出的 baseline 不会带该证据源，
  上述三个集合断言会立即失败。
- [x] **AC-7**：成立（残留一处近似耗时表述，见存疑项）。三份 prose 文档已不再写死
  「N passed / N skipped」「N 支文件 / N 用例」：`CHANGELOG.md` 把「33 支文件 / 1461 用例」改为
  「预算与当前值以该表为准」、覆盖率基线改 76.8%；`TESTING.md:82-84` 明确「本文件不再写死数量」并
  指向自动生成的 `docs/TEST_SCOPE.md`；`DEVELOPMENT.md:154` 把「当前 33 / 1461」改为指向
  `docs/TEST_SCOPE.md`。保留的数字只有覆盖率基线（`DEVELOPMENT.md:153` 76.8%、
  `CHANGELOG.md:13` 76.8%、`TESTING.md:82` 76.8%，均一位小数，AC 允许）与 scope 上限
  40/1600（稳定策略值，非当前计数）。据此「新增一个测试无需回填这三份文档」成立。
- [ ] **AC-8**：**不成立**。`tests/test_release_gates.py` 仍以废弃分支名 `develop` 作夹具：
  `tests/test_release_gates.py:318` 为 `problems = pr_body_guard.evaluate(body, "develop")`，
  位于本需求**新增**的 `test_pr_body_guard_rejects_prose_only_requirement_ids` 内。
  实现对既有的三处夹具做了替换（`:57`、`:63`、`:68` 已改为 `feat/periodic-update`），
  却在同一次提交新增的测试里把 `develop` 又写了回来。`grep -n develop tests/test_release_gates.py`
  仅此一行命中；改用 `feat/xxx` 对该断言无任何行为影响，属纯遗漏。

## 未通过 / 存疑项

1. **AC-8 不成立（建议阻断合并）**：`tests/test_release_gates.py:318` 仍用 `"develop"` 作夹具，
   与 AC-8「不再用已废弃分支名 `develop` 作夹具，改用 `feat/xxx`」直接冲突。
   复现：`grep -n develop tests/test_release_gates.py` → 仅 `318:`。
   处理建议：改为 `pr_body_guard.evaluate(body, "feat/periodic-update")`（与同文件其余三处一致），
   断言不受影响。
2. **AC-3 部分不成立**：三条失败路径中「超出预算」的单测不隔离，删掉预算检查该测试仍通过，
   不能作为该路径的回归保护。复现见 AC-3 条目与下方命令。
   处理建议：让 `_run_check` 同时把 `SCOPE_PATH` 指向与 `collect_scope` 一致的最小登记表
   （或在断言中检查返回的问题文本含「超过预算」），使每条失败路径各自独立。
3. **AC-7 残留（非阻断）**：`docs/DEVELOPMENT.md:160` 的「全量测试耗时」仍写「约 1 分钟」。
   AC-7 要求 prose 文档「不再硬编码测试数量与耗时」。该值近似且已被标注「不作为判据」，
   加测试也不需要回填，故不据此判 AC-7 不成立，但与「不再硬编码耗时」的字面要求不完全一致，
   建议改为「约 1 分钟」之外的定性描述或直接去掉数值。本行同时是需求方在 `e68e05d` 中更正过的行，
   更正后仍保留了该近似数值。
4. **实现提交的测试计数与实测不符（非阻断）**：`3b76914` 的 commit message 写
   `1466 passed / 3 skipped`，`make verify` 实测 `1467 passed / 3 skipped`。
   不影响任何 AC，但记录该差异。
5. **AC-4 口径提示（非阻断）**：小节内**裸写** `REQ-007`（非 `- [x] **REQ-007**` 形式）
   不算有效结论，会被拒。这与 AC-4 文案「（`- [x] **AC-n**` / `- [ ] **AC-n**` / `REQ-NNN`）」
   的括号内写法有细微歧义；按现实现，`REQ-NNN` 需写在勾选行里。若需求意图是任意提及即可，
   则实现偏严；我按「勾选行内」理解，认为可接受，仅记录。

AC-3 的复现命令（不改仓库文件，运行期拼接编号）：

```bash
.venv/bin/python -c "
import sys; sys.path.insert(0,'scripts'); sys.path.insert(0,'.')
import test_scope
def fake(file='tests/test_test_scope.py', reqs='基线'):
    return {'file':file,'reqs':reqs,'layer':'unit','target':'—','cases':1}
test_scope.collect_scope = lambda: [fake()]
test_scope.collect_cases_via_pytest = lambda: 10
test_scope.ownership_problems = lambda e: []
test_scope.MAX_TEST_FILES = 40
test_scope.MAX_COLLECTED_CASES = 10**9   # 关掉预算检查本身
print('rc =', test_scope.main(['--check']))
"
# 实测输出 rc = 1（由 stale 登记项导致），证明 test_check_fails_when_over_budget 不隔离
```

## 结论

**不通过（有条件）**。8 条 AC 中 6 条成立：AC-1、AC-2、AC-4、AC-5、AC-6、AC-7
（AC-7 有一处非阻断的近似耗时残留）。两条未通过：

- **AC-8 不成立**（`tests/test_release_gates.py:318` 仍有 `develop`），属明确、易修的直接冲突，
  建议作为合并阻断项；
- **AC-3 部分不成立**（「超出预算」失败路径的单测不隔离，无法守住该路径），
  建议在合入前补齐隔离断言。

其余治理一致性核验通过：需求台账与 `docs/requirements/REQ-*.md` 文件集合完全一致
（文件与台账行均为 `REQ-001`…`REQ-008`，双向差集为空）；`make verify` 全绿
（`1467 passed, 3 skipped`，覆盖率 76.76%/76.8%）。仓库工作树在验收结束时干净，
破坏性验证已完全恢复。

说明：本报告对 AC-3 与 AC-8 使用 `- [ ]`，因此 `scripts/acceptance_gate.py` 会（正确地）
把本报告判为未通过；这正是上述两项修好之前不应合并的预期信号。
