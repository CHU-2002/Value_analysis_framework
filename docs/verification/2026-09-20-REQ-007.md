---
batch: 2026-09-20-REQ-007
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-007
base: 229593e
full-suite: "1467 passed, 3 skipped, 覆盖率 76.8%"
---

# 独立验收报告：REQ-007 门禁与治理工具加固

## 独立验收声明

本报告由独立 agent 出具：未参与 REQ-007 的实现，未查看开发过程，只读取仓库内的
需求条目、脚本、测试与文档，并以「试图证伪」为前提自行构造输入复现判定。所有结论
均附文件:行或命令输出摘要，可独立重跑。

两轮验收过程（同一独立评审者）：

- **第一轮**（基线 `3b76914`/`e68e05d`）：判 AC-1、AC-2、AC-4、AC-5、AC-6、AC-7 成立；
  **AC-3 部分不成立**（「超出预算」单测不隔离，关掉预算检查仍通过）、**AC-8 不成立**
  （`tests/test_release_gates.py:318` 仍用 `develop`）。结论为不通过。
- **第二轮**（基线 `229593e`）：需求方提交 `229593e fix(governance): isolate the budget test
  and finish the AC-7/AC-8 cleanup` 修 AC-3/AC-7/AC-8。本轮在 `229593e` 上重跑全部核验，
  8 条 AC 全部成立，无阻断项。

核验对象与基线说明：分支 `feat/req-007-governance-hardening` 的工作树在第二轮验收期间
除本报告外保持干净。本轮 `base` 取 `git rev-parse --short HEAD` = `229593e`。破坏性验证均已
完全恢复，复核方式与结果见 AC-3、AC-5 条目。本报告本身被包含在 `229593e` 中（需求方提交时
一并纳入），本轮只在工作区就地更新它，未新建、未修改、未提交其它任何文件。

耗时说明：本报告不把墙钟耗时作为判据。核验中出现的墙钟时间会受机器休眠/挂起影响，
不以 `real` 推断测试或门禁有问题。

## 全量测试

```bash
make verify
```

- 结果：`1467 passed, 3 skipped in 54.82s`（`make verify` 退出码 0）
- 覆盖率：`Required test coverage of 74% reached. Total coverage: 76.76%`（TOTAL 11141 语句 / 2589 未覆盖，即报告口径 76.8%）
- 分项门禁：`lint` 通过；追溯门禁 `8 passed`；`test_scope --check` → `测试 scope 检查通过：33 支文件、1470 个用例，均在预算内`；回归门禁 → `main 批量回归门禁通过（自 e453255 起累积的特性合入未达阈值 3）`
- 基线（对比的 HEAD sha）：`229593e`
- 跳过的 3 条均为 `TUSHARE_TOKEN not set` 的 live API 测试（`tests/test_integration.py:30/76/110`），符合「全 mock、离线、无 Token」约束

与第一轮一致（同为 `1467 passed / 3 skipped`、76.76%），说明 AC-3/AC-7/AC-8 的修复未引入回归。

观察（非阻断）：实现提交 `3b76914` 的 commit message 写「全量 1466 passed / 3 skipped」，
实测两轮均为 `1467 passed`；需求方说明该数字是修台账行之前测得、且该提交信息已合入 main
不改写历史。commit message 不是 AC-7 所指的 prose 文档，不影响任何 AC 判定。

## 逐条验收

### REQ-007 门禁与治理工具加固

- [x] **AC-1**：成立（第二轮重跑，无回归）。`scripts/pr_body_guard.py` 判据只看
  `## 需求编号` 小节是否 `meaningful`（`:61-67`），HTML 注释先被剥离（`:30-31`），
  占位集合见 `:23`。实跑：「小节空 + 正文写 REQ-003」→ 退出码 1；「小节只有
  `<!-- REQ-003 -->`」→ 退出码 1；「小节填 `REQ-007` 且自测栏填实」→ 退出码 0。
  报错文本明确写出「正文其它地方提到编号不能替代」。
- [x] **AC-2**：成立（第二轮重跑，无回归）。`registered_requirement_ids()` 取
  `docs/requirements/REQ-*.md` 已登记编号（`:138-140`），`ownership_problems()` 对归属列
  每个 `REQ-NNN` 校验并报出文件与编号（`:143-154`），`--check` 调用它（`:202`）。
  实测：登记集合 `['REQ-001'…'REQ-008']`；运行期拼接的未登记编号 `REQ-777` 被报为
  `tests/test_temp_probe.py 的归属编号 REQ-777 未在 docs/requirements/ 登记…`。
  未在仓库留下任何未登记编号字面量（工作树干净）。
- [x] **AC-3**：成立（第一轮不成立，`229593e` 修复后本轮证伪验证通过）。
  `_run_check` 新增 `registry=` 参数（`tests/test_test_scope.py:104-118`）：给定 registry 时
  用 `_write_scope`（`:88-101`）把 `SCOPE_PATH` 指向一份与 `entries` 对齐的最小登记表，
  从而消除单文件夹具附带的「登记表漂移」噪声；三条失败路径的测试还各自断言了具体报错文本
  （漂移→`未登记`、预算→`超过预算`、归属→未登记编号，`:121-139`）。
  **我用「关掉被守护的检查，测试必须失败」逐条证伪**（临时改 `scripts/test_scope.py`，
  每次用 `cp` 备份还原并以 sha256 复核；原值
  `f02ea4a3562574ab4f2c074c90ae8e992719b6611de5628d20afa6c3af1e4f63`）：
  1. `if len(entries) > MAX_TEST_FILES:` → `if False:` ⇒ `test_check_fails_when_over_budget`
     **FAILED**（第一轮此改法该测试照过）；
  2. `if collected > MAX_COLLECTED_CASES:` → `if False:` ⇒ 同一测试 **FAILED**（覆盖第二个断言）；
  3. `if missing:` → `if False:` ⇒ `test_check_fails_when_a_test_file_is_unregistered`
     **FAILED**（其 `未登记` 文本断言使漂移路径仍被守住）；
  4. `problems.extend(ownership_problems(entries))` → `problems.extend([])` ⇒
     `test_check_fails_on_an_unregistered_ownership_id` **FAILED**。
  每次还原后 `scripts/test_scope.py` 的 sha256 均回到原值、`git status --porcelain` 为空。
  基线 `pytest tests/test_test_scope.py` → `13 passed`。
- [x] **AC-4**：成立（第二轮重跑，无回归）。`validate_record` 深入 `## 逐条验收` 小节内校验
  （`scripts/regression_gate.py:127-138`）。实测：「（待补）」→ FAIL；只有 HTML 注释 → FAIL；
  `- [x] **AC-1**：成立` → PASS；未打勾的 AC-2 行 → PASS；`- [x] **REQ-007**` → PASS；
  指向 `docs/verification/…` → PASS；写「本批无功能改动」→ PASS；小节整体缺失 → FAIL。
- [x] **AC-5**：成立（第二轮重跑，无回归）。守卫 `os.walk` 全仓 + 显式排除
  `{.git,.venv,output,docs,tests,node_modules,__pycache__}`，基线生产者以
  `BASELINE_PRODUCER_DOCS` 白名单豁免（`tests/test_update_docs_contract.py:186-202`）。
  独立扫描全仓：87 个 `.md`，命中者恰为三个白名单文件，**非豁免命中为空**。
  第二轮再次「先破坏再恢复」：在仓库内新建 `req007_break_probe/spec.md` 写入
  `{output_dir}/qualitative_input.json`（未改测试），
  `pytest tests/test_update_docs_contract.py -k never_use_a_flat` 立即 FAILED 并点名该文件；
  删除后 `git status --porcelain` 为空。
- [x] **AC-6**：成立（第二轮重跑，无回归）。
  `tests/test_two_layout_e2e.py::test_incremental_run_runs_prepare_with_prior_analysis`
  （`:119-171`）走完整增量链路：`adopt_legacy` → `create_run(kind="report-update",
  primary_period="2026H1", supersedes=…)` → `prepare_run(update_dir, …,
  primary_period="2026H1", prior_analysis=baseline_dir/"synthesis"/"result.json")`。
  断言非「文件存在」：`manifest["primary_period"] == "2026H1"`、`manifest["inputs"]` 的
  `source_id` 含 `prior_analysis`、证据索引 `index["sources"]` 含 `source_id == "prior_analysis"`、
  `build_module_context("period_delta", …)` 选中 `prior_analysis` 且
  `selection["missing_prior_analysis"] == []`、`resolve_qualitative_input(baseline_dir)["source"]
  == "structured"`。代码侧核对 `scripts/results/prepare.py:158-159`/`:243`/`:255-256`、
  `scripts/results/context.py:63`/`:159`/`:293-296`。复跑 `pytest tests/test_two_layout_e2e.py`
  → `6 passed`。
- [x] **AC-7**：成立（第一轮的「约 1 分钟」残留已在 `229593e` 清除）。
  `docs/DEVELOPMENT.md:160` 现为「以 `make cov` 实测为准（墙钟时间会被机器休眠/挂起显著拉长，
  不作为判据）」，不再写死数值；`docs/TESTING.md:82-84` 与 `CHANGELOG.md` 不再写死测试数量、
  指向自动生成的 `docs/TEST_SCOPE.md`。第二轮按
  `passed|skipped|支文件|用例|约 1 分钟|耗时` 复扫三份文档：无任何写死的用例数/文件数/耗时；
  仅剩覆盖率基线（`DEVELOPMENT.md:153`、`TESTING.md:82`、`CHANGELOG.md:13`，均 76.8% 一位小数，
  AC 允许）与稳定的 scope 上限 40/1600。据此「新增一个测试无需回填这三份文档」成立。
- [x] **AC-8**：成立（第一轮不成立，`229593e` 修复后本轮复核通过）。
  `tests/test_release_gates.py:318` 现为 `pr_body_guard.evaluate(body, "feat/periodic-update")`
  （`git show 229593e -- tests/test_release_gates.py` 可见 `-…"develop"` → `+…"feat/periodic-update"`）。
  全仓复核：`grep -rn develop tests/*.py` 无任何命中（退出码 1），四处夹具
  （`:57`/`:63`/`:68`/`:318`）统一为 `feat/periodic-update`。
  `pytest tests/test_release_gates.py` → `28 passed`。

## 未通过 / 存疑项

1. **无阻断项**：第一轮的 AC-3、AC-8 两项均已在 `229593e` 修好并经证伪法复验。
2. **观察（非阻断）AC-4 口径**：小节内**裸写** `REQ-007`（非 `- [x] **REQ-007**` 形式）
   不算有效结论、会被拒。这与 AC-4 文案括号内「`- [x] **AC-n**` / `- [ ] **AC-n**` / `REQ-NNN`」
   的写法有细微歧义；按现实现 `REQ-NNN` 需写在勾选行内。我按「勾选行内」理解，认为可接受。
3. **观察（非阻断）文档里的模块级覆盖率整数**：`docs/TESTING.md:89-91` 仍列各模块覆盖率
   （如 `valuation_engine.py` 34%）。属覆盖率信息、非测试数量/耗时，AC-7 允许保留，记录备查。
4. **观察（非阻断）commit message 计数**：`3b76914` 写「1466 passed」，实测两轮均为 1467；
   需求方已说明原因且不改写历史。不影响 AC。
5. **观察（流程）**：本报告文件被需求方连同修复一起提交进 `229593e`；本轮就地更新，
   未提交。若流程要求「报告由评审者独立提交」，请注意这一提交归属。

AC-3 的证伪复现（临时改 `scripts/test_scope.py`，改后必须还原并核对 sha256）：

```bash
cp scripts/test_scope.py /tmp/scope.bak
shasum -a 256 scripts/test_scope.py   # 期望 f02ea4a3562574ab4f2c074c90ae8e992719b6611de5628d20afa6c3af1e4f63
.venv/bin/python -c "
p='scripts/test_scope.py'; s=open(p).read()
open(p,'w').write(s.replace('    if len(entries) > MAX_TEST_FILES:\n','    if False:\n',1))
"
.venv/bin/python -m pytest -q tests/test_test_scope.py::test_check_fails_when_over_budget  # 期望 FAILED
cp /tmp/scope.bak scripts/test_scope.py
shasum -a 256 scripts/test_scope.py   # 必须与改前一致
git status --porcelain                # 必须为空
# 同理分别关掉 if missing: → if False:（漂移测试应 FAILED）、
# problems.extend(ownership_problems(entries)) → problems.extend([])（归属测试应 FAILED）
```

## 结论

**通过**。8 条 AC 全部成立：AC-1、AC-2、AC-3、AC-4、AC-5、AC-6、AC-7、AC-8。
第一轮判定的 AC-3（「超出预算」单测不隔离）与 AC-8（`develop` 夹具）已在 `229593e` 修复，
本轮用「关掉被守护的检查、对应测试必须失败」的证伪法逐条确认 AC-3 的三条失败路径现在都被守住，
并复核 AC-1/2/4/5/6/7 无回归。无应阻断合并的问题。

治理一致性：需求台账与 `docs/requirements/REQ-*.md` 文件集合完全一致
（文件与台账行均为 `REQ-001`…`REQ-008`，双向差集为空）；Inbox 仅余 3 条产品想法。
`make verify` 全绿（`1467 passed, 3 skipped`，覆盖率 76.76%/76.8%）。仓库工作树在验收结束时
除本报告外干净，破坏性验证（AC-3 的检查禁用、AC-5 的临时目录）均已完全恢复并复核。
本报告全部 AC 为 `- [x]`；直接以本报告调用 `scripts/acceptance_gate.evaluate`（批次需求 `REQ-007`）
返回空问题列表，即 `acceptance_gate` 通过。
