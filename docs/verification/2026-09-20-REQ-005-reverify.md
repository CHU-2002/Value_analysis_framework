---
batch: 2026-09-20-REQ-005-reverify
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-005
base: b8b1545
full-suite: "1458 passed, 3 skipped, 覆盖率 76.43%"
---

# 独立复验报告：REQ-005 增量更新文档与下游接线（缺口修复复验）

## 独立验收声明

本报告由**同一个无上下文的独立 agent** 出具：未参与本修复的开发，未查看其 PR 讨论，
只以当前工作树（`feat/req-005-run-dir-wiring`，HEAD `b8b1545`）的代码、测试、文档与可执行行为为证据。
默认姿态仍是**试图证伪**：用户给出的修复清单**不被采信**，每一处都自己 `grep` / 读文件 / 跑脚本确认；
守卫不是「跑通就算数」，我按用户建议**把扁平路径重新写回去实测守卫会失败**，再恢复工作树并用 sha256 复核
（见存疑项 2）。验收期间工作树仅本报告被修改（`git status --porcelain` 只显示该文件）。

本报告是 REQ-005 的第四轮结论，历经：
- 上一轮报告 `docs/verification/2026-09-20-REQ-003-004-005.md`（历史记录，未改动）判 AC-1/AC-2/AC-5 不成立；
- `c04dbfa`：消除点名的 6 处残留 + 契约测试扩展 7 份文档 + 双布局 e2e；
- `64e6c31`：消除我复验发现的 4 处同类残留 + 新增全局守卫；
- `b8b1545`（当前 HEAD）：补守卫正则两处漏洞（死分支、末位边界）+ 单测 + 把隐式豁免改成显式 `BASELINE_PRODUCER_DOCS`、扫描根扩到 `shared/qualitative/` 与两份命令目录。

被复验 head 为 `b8b1545`；`main` 是其祖先（`git merge-base --is-ancestor main HEAD` 成立）。
所有复现均用 `.venv/bin/python`（3.12）离线执行，未使用任何 Token。开始前用 `pgrep -fl "pytest|make verify"`
确认无并发测试进程。

## 全量测试

```bash
make verify   # lint + cov + trace + scope-check，等价于 CI 本地检查项
```

- 结果：**1458 passed, 3 skipped in 108.32s**（`make verify` 退出码 0）
- 覆盖率：**76.43%**（门禁 `--cov-fail-under=74` 通过；TOTAL 11119 stmts / 2621 miss）
- 基线（被复验的 head sha）：`b8b1545`；`main` 为其祖先
- 3 个 skip 均为 `tests/test_integration.py` 的实网用例（`TUSHARE_TOKEN not set`），其余用例全 mock、离线
- 附带门禁：`tests/test_requirement_traceability.py` 8 passed；`scripts/test_scope.py --check` 通过（33 支文件 / 1461 用例）；批量回归门禁通过
- 逐轮对比：1436 → 1456 → 1457 → **1458** passed；覆盖率 76.29% → 76.44% → 76.44% → **76.43%**（本轮 TOTAL miss 由 2620 变 2621，故 0.01pp 回落；实现者给出的 76.44% 与我的 76.43% 差 0.01pp，属计数/四舍五入差异，不影响门禁）
- 定向复跑：`tests/test_update_docs_contract.py` + `tests/test_two_layout_e2e.py` = **35 passed**
- **耗时异常提示（环境，不是测试问题）**：pytest 自报 108.32s，但本次 `make verify` 整条管线的 wall time 高达
  `real 33m55s`，而 `user 0m25.8s / sys 0m6.7s`——进程绝大部分时间在等待（主机负载/调度/IO），
  与用户说明的「进程抢占、600s 超时」一致；pytest 本身的结果与耗时不受影响。我已在开始前确认无并发 pytest。

## 逐条验收

### REQ-005 增量更新文档与下游接线

- [x] **AC-1**：成立，扁平路径在守卫范围内已清零，且护栏从「文件清单」升级为「全局扫描 + 显式豁免」。
  - **解析链路**：上一轮点名的 6 处（`.claude/commands/valuation.md:16/22`、`.opencode/commands/valuation.md:16/22`、`.claude/commands/portfolio-strategy.md:36`、`.opencode/commands/portfolio-strategy.md:14/17`、`strategies/value/valuation/coordinator.md:38`、`strategies/portfolio/coordinator.md:83/86`）与再上一轮的 4 处（`strategies/value/coordinator.md:262`、`strategies/value/phase2_value_analysis.md:10`、`strategies/value/valuation/phase2_valuation.md:10`、`strategies/portfolio/coordinator.md:81`）均确认使用 `{run_dir}`。
  - **`strategies/` 全量扫描**：`grep -rnE "(\{output_dir\}|\{company_output_dir\}|output/\{code\}_\{company\})/qualitative_input\.json" strategies/` → NONE。
  - **行为复现（run-store-only，`adopt --prune`）**：`runs.py resolve --company-dir … --latest`（rc=0）→ run 目录；`resolve_qualitative(company_dir)=unavailable`、`(run_dir)=structured`；legacy（无 `latest.json`）→ `structured`。
  - **stale 标记与用户可见性未回归**：`record.json:downstream = {'stale': True, 'value_computed': True, 'buy_sell_basis': True}`；`evaluate_company` → `state=stale`、`reasons=['downstream_stale']`、exit=1。
  - **回归护栏（本轮重点）**：`tests/test_update_docs_contract.py` 的全局守卫 `test_prompt_docs_never_use_a_flat_qualitative_input_path`（`:219-232`）扫描 `PROMPT_TREES = (strategies, shared/qualitative, .claude/commands, .opencode/commands)`（`:184`，本轮实扫 18+30+7+7 = 62 份 `.md`），对 `BASELINE_PRODUCER_DOCS`（`:186-190`，3 份基线生产者）显式豁免；配套单测 `test_flat_qualitative_regex_matches_only_flat_paths`（`:200-216`）。详见存疑项 2/3。
- [x] **AC-2**：成立。`legacy` 与 `run-store` 两种布局均以命令复现可用（同上）；`prepare` 实跑后 manifest 含 `framework`（`version=0.2.0`、`prompt_fingerprint` 非空）且 `schema_version='1.0'`，不传 framework 时省略该键（加性未回归）；`/buy-sell-plan` 读扁平 `value_computed.json`（设计如此，本分支未改动），两种布局下行为不变。
- [x] **AC-3**：成立（本分支未改动该文档）。`docs/ARCHITECTURE.md:126`（run-store 数据流）、`:133-139`（`latest.json`/`record.json`/`history.jsonl` 布局）、`:205-214`（生命周期与指纹）、`:214`（跨 run 硬约束「绝不混用两个 run 的参数」）。
- [x] **AC-4**：成立。`README.md:83/92/97-112`（`/update-analysis` 用法、期次与下游 stale 说明）在；`CHANGELOG.md` 的 `[Unreleased] → Added` 保留本期能力条目并追加三轮修复记录（run_dir 解析 + 扁平路径清零 + 双布局 e2e/守卫）。
- [x] **AC-5**：成立。`tests/test_two_layout_e2e.py` 覆盖需求点名的四步；我独立复跑底层行为（不依赖该测试自身断言）：mock baseline（`_write_complete_structured_run` + `adopt --prune`）→ 注入新期次 PDF 并 `runs.py new --kind report-update` → `finish` → `新 run 纳入注入期次=True`、`resolve --latest == 新 run=True`、`history.supersedes == baseline_id=True`、**baseline 目录树逐项不变=True**、**baseline `run_manifest.json` sha256 不变=True**、`baseline 仍可解析=structured`。（e2e 粒度说明见存疑项 4。）

## 未通过 / 存疑项

**1. 两轮点名的 6 + 4 处残留：全部确认消除。** 逐处核验通过；`strategies/` 下无任何扁平 `qualitative_input.json` 写法。

**2. 全局守卫：两处正则漏洞已修，我以「先破坏再恢复」实测有效。**
- **正则覆盖面（我独立用合成输入验证，非仅采信其单测）**：4 种扁平写法**全部命中**——`{output_dir}/…`、`{company_output_dir}/…`、`output/{code}_{company}/…`、`output/{directory_code}_*/…`（上一轮的死分支已修好，现在能命中）；6 种写法**全部不命中**——`{run_dir}/…`、裸 `qualitative_input.json`、`{output_dir}/qualitative_input.json.md`、`…jsonx`、`…json.bak`、`{run_dir}/qualitative_input.json.md`。末位 `(?![A-Za-z0-9_.-])` 收口有效。
- **守卫有效性（先破坏再恢复，在**本轮新扩的根**上做）**：向消费者文档 `shared/qualitative/coordinator_update.md` 追加一行 `<!-- TEMP PROBE: {output_dir}/qualitative_input.json -->` → 运行守卫测试 → **FAILED**，报错精确点名 `shared/qualitative/coordinator_update.md: {output_dir}/qualitative_input.json`，并提示「若该文件是基线生产者，请加入 BASELINE_PRODUCER_DOCS 并说明理由」；随后 `git checkout -- <file>` 恢复，恢复后 sha256 与改前一致（`a11435206f0fe01068eac9ac5b17db8342818426d9c220458f8d1eee1e8a563d`），`git status` 只剩本报告被修改。**结论：守卫有效，且对扩展后的根同样生效。**
- **全树扫描**：4 个根共 62 份 `.md`，非豁免命中 = **NONE**；豁免命中恰为 3 份生产者（`shared/qualitative/coordinator_v2.md`、`.claude/commands/business-analysis.md`、`.opencode/commands/business-analysis.md`）。全仓库 `.md` 扫描中，守卫范围之外的扁平写法**只有本复验报告自身**（我引用这些字符串作为证据/测试样例）——即不存在真实的、无护栏的消费者文档。

**3.「生产者豁免」改显式后：判定合理，此前的盲区已关闭。**
- **合理性**：`.claude`/`.opencode` 的 `business-analysis.md:124` 与 `shared/qualitative/coordinator_v2.md:227` 是「从零开始」的完整分析**生产者**，按设计产出并就地读取扁平布局；REQ-005 的 AC-1/AC-2 管的是**下游消费** run-store 产物，故把生产者排除在不变量之外合理，且与 `value-analysis.md:41` / coordinator 已有的「run-store 存在时不得自动跑 `/business-analysis`」闸门自洽。豁免现由 `BASELINE_PRODUCER_DOCS` 显式列出并在测试注释里说明理由，比上一轮的「靠扫描范围隐式漏掉」更可维护。
- **`shared/qualitative/` 扩入后是否还有无护栏的消费者**：就**现有**消费者而言**没有了**——该树 30 份 `.md` 已纳入扫描，非豁免命中 NONE，且我在该树内的消费者文档上做了破坏性验证（上条），守卫确实会拦住回归。
- **`coordinator_update.md:274` 的裸名是否会被误报**：**不会**。该行是 `    ├── qualitative_input.json`（run 目录树示意图），无路径前缀；正则要求前缀分支，故该文件内 `FLAT_QUALITATIVE_RE` 命中数为 0（实测），不是误报。
- **仍存的理论维护点（非阻断）**：豁免与扫描根仍是「根列表 + 显式名单」，若将来在 4 个根之外新增消费者文档（如 `prompts/`、`.claude/skills/`），守卫不会覆盖。当前这些树内无扁平写法（全仓库扫描已确认）。

**4. AC-5 的 e2e 属「台账级增量 run」，未跑 prepare/模块（边界说明，非阻断）。**
`tests/test_two_layout_e2e.py:73-106` 的「增量 run」是 `runs.py new --kind report-update` + `finish`（写新期次快照并切指针），未对新 run 执行 `prepare`/模块/`resolve_qualitative`；它证明的是**布局不变量**（旧 run 不可变、指针切换、旧 run 仍可解析），与 AC-5 括号里点名的四步一致，故判成立。

**5. 环境噪声与数字差异（不构成 AC 失败）。**
`make verify` 会写 `.coverage`（gitignored）。本轮 `make verify` 的 wall time 异常偏高（`real 33m55s` vs `user 25.8s`），pytest 自报仅 108.32s——属主机进程抢占/调度等待，与用户在上一轮的说明一致；开始前我已确认无并发 pytest。覆盖率我实测 **76.43%**，与实现者给出的 76.44% 差 0.01pp（miss 2620→2621）。

**6. 本轮是否仍有应修的阻断项：没有。**
AC-1…AC-5 全部成立，未发现新的功能性缺口；存疑项 3/4 的两条均为**非阻断**的维护性建议（守卫根列表的覆盖边界、e2e 粒度），可由台账 Inbox 处理，不必阻塞本次合并。

## 结论

**通过（AC-1…AC-5 全部成立，无本轮阻断项）。**

- **AC-1**：`strategies/` 及守卫范围内扁平路径清零；run-store-only 下「`runs.py resolve --latest` → run 目录 → resolver `structured`」成立；stale 标记与用户可见性未回归（exit 1 / `downstream_stale`）；护栏为「全局扫描 + 显式生产者豁免」，正则两处漏洞已修，我以「先破坏再恢复」在扩展后的根上实测守卫会失败并精确报点。
- **AC-2**：legacy 与 run-store 双布局均可用；`prepare` 透传 `framework` 且保持加性。
- **AC-3 / AC-4**：ARCHITECTURE、README、CHANGELOG 证据齐备。
- **AC-5**：双布局 e2e 覆盖 mock baseline → 注入新期次 → 增量 run → 旧 run 目录树与 manifest 摘要不变、仍可解析、latest 指向新 run。
- 全量 `make verify` 退出码 0：**1458 passed, 3 skipped in 108.32s，覆盖率 76.43%**。
- **生产者豁免判定：合理**，且已从隐式改为显式；`shared/qualitative/` 扩入后现有消费者无护栏盲区，`coordinator_update.md:274` 的裸名不被误报。剩余仅两条非阻断维护建议与一项 e2e 粒度说明，可入 Inbox。**可以推送合并。**
