---
batch: 2026-09-20-REQ-005-reverify
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-005
base: c04dbfa
full-suite: "1456 passed, 3 skipped, 覆盖率 76.44%"
---

# 独立复验报告：REQ-005 增量更新文档与下游接线（缺口修复复验）

## 独立验收声明

本报告仍是同一个**无上下文的独立 agent** 出具：未参与本修复的开发，未查看其 PR 讨论，
只以当前工作树（`feat/req-005-run-dir-wiring`，HEAD `c04dbfa`，`git status --porcelain` 验收前后均为空）
的代码、测试、文档与可执行行为为证据。默认姿态仍是**试图证伪**：用户给出的修复清单**不被采信**，
每一处都自己 `grep` / 读文件 / 跑脚本确认；找不到证据就判不成立。

复验对象是 REQ-005 的 AC-1…AC-5，重点是我上一轮判不成立的 AC-1 / AC-2 / AC-5
（见 `docs/verification/2026-09-20-REQ-003-004-005.md`，本报告不改动该文件）。
被复验的修复提交为 `c04dbfa`（`fix(update): resolve the run directory before the qualitative resolver`），
其父提交 `77da58d` 已含 #25/#26；`main` 是 HEAD 的祖先（`git merge-base --is-ancestor main HEAD` 成立）。
所有复现均用 `.venv/bin/python`（3.12）离线执行，未使用任何 Token。

## 全量测试

```bash
make verify   # lint + cov + trace + scope-check，等价于 CI 本地检查项
```

- 结果：**1456 passed, 3 skipped in 57.88s**（`make verify` 退出码 0；`real 0m59.7s`）
- 覆盖率：**76.44%**（门禁 `--cov-fail-under=74` 通过；TOTAL 11119 stmts / 2620 miss）
- 基线（被复验的 head sha）：`c04dbfa`；`main` 为其祖先
- 3 个 skip 均为 `tests/test_integration.py` 的实网用例（`TUSHARE_TOKEN not set`），其余用例全 mock、离线
- 附带门禁：`tests/test_requirement_traceability.py` 8 passed；`scripts/test_scope.py --check` 通过（33 支文件 / 1459 用例）；批量回归门禁通过
- 与上一轮对比：1436 → 1456 passed（+20，来自 `tests/test_two_layout_e2e.py` 与契约测试参数化），覆盖率 76.29% → 76.44%

## 逐条验收

### REQ-005 增量更新文档与下游接线

- [x] **AC-1**：成立（附带一项非阻断的同类文档残留，见「未通过 / 存疑项」2）。两半都自行复现过：
  - **「命令经 `latest.json`/`record.json` 解析产物、不硬编码扁平路径」**：上一轮点名的 6 处**全部消除**，逐文件断言通过（脚本对每个文件检查同时含 `runs.py resolve --company-dir`、`{run_dir}`、`resolve_qualitative --output-dir "{run_dir}"`，且不含把 `{output_dir}`/`{company_output_dir}` 交给 resolver）：
    `.claude/commands/valuation.md`（`:16` resolve、`:22` resolver 用 `{run_dir}`）、`.opencode/commands/valuation.md`（`:16`/`:22`）、`.claude/commands/portfolio-strategy.md`（`:36`）、`.opencode/commands/portfolio-strategy.md`（`:14` resolve、`:17` resolver）、`strategies/value/valuation/coordinator.md`（`:38`）、`strategies/portfolio/coordinator.md`（`:83` resolve、`:86` resolver），输出 6× `OK`。
  - **行为复现（run-store-only，`adopt --prune` 后公司目录无 `run_manifest.json`）**：按文档实跑 `.venv/bin/python scripts/runs.py resolve --company-dir <公司目录> --latest` → 返回 run 目录（rc=0）；`resolve_qualitative(company_dir).source = unavailable`，`resolve_qualitative(run_dir).source = structured`。上一轮的功能性缺口（命令把公司目录交给 resolver）已不再复现。
  - **「新期次后 `value_computed`/`buy_sell_basis` 被标记 stale 且对用户可见」**：`finish_run` 写 `record.json:downstream = {'stale': True, 'value_computed': True, 'buy_sell_basis': True}`；`evaluate_company` → `state=stale`、reasons=`['downstream_stale']`、exit=1（`runs.py:491-522`、`analysis_status.py:320-324`）。
  - 契约测试从只钉 `/value-analysis` 扩展为 7 份下游文档参数化（`tests/test_update_docs_contract.py:155-175` 的 `RUN_DIR_DOCS` + `test_downstream_docs_resolve_the_run_directory`），我实跑该文件 + e2e：**33 passed**。
- [x] **AC-2**：成立。两种布局均以命令复现：
  - **legacy 扁平布局**：公司目录无 `latest.json`，`resolve_qualitative(company_dir).source = structured`（`tests/test_two_layout_e2e.py:44-50`）。
  - **run-store 布局**：`adopt --prune` 后 `runs.py resolve --latest` 指向 run 目录，resolver 用 run 目录得 `structured`（`tests/test_two_layout_e2e.py:53-70`）。
  - **`prepare` 透传 `framework`**：实跑 `prepare_run` → manifest 含 `framework`（`version=0.2.0`，`prompt_fingerprint` 非空）且 `schema_version='1.0'`；不传 framework 时省略该键（`prepare.py:230`、`manifest.py:142-143`，REQ-003 AC-8 的加性不变）。
  - `/buy-sell-plan` 读扁平 `value_computed.json`（设计如此，与 run 布局无关），本分支未改动它，两种布局下行为不变。
- [x] **AC-3**：成立（本分支未改动该文档，仍满足）。`docs/ARCHITECTURE.md` 记录 run-store 数据流（`:126`）、布局（`:133-139` `latest.json`/`record.json`/`history.jsonl`）、run 生命周期与框架指纹（`:205-214`），并写明跨 run 硬约束「绝不混用两个 run 的参数」（`:214`，另见 `:12`）。
- [x] **AC-4**：成立。`README.md:83`（`/update-analysis` 用法）、`:92`（示例）、`:97-112`（增量更新与期次/下游 stale 说明）均在；`CHANGELOG.md` 的 `[Unreleased] → Added` 既保留本期能力条目（run-store、`period_delta`、`/update-analysis`），又新增了本次修复条目（run_dir 解析 + 双布局 e2e）。
- [x] **AC-5**：成立。新增 `tests/test_two_layout_e2e.py`（113 行）覆盖需求点名的四步，我独立复跑其行为（不依赖该测试本身）：mock baseline（`_write_complete_structured_run` + `adopt --prune`）→ 注入新期次 PDF 并 `runs.py new --kind report-update` → `finish` → 结果：`新 run 纳入注入期次=True`、`resolve --latest == 新 run=True`、`history.supersedes == baseline_id=True`、**baseline 目录树逐项不变=True**、**baseline `run_manifest.json` sha256 不变=True**、`baseline 仍可解析=structured`。该文件实跑 33 项（与契约测试合计）全绿，并通过 `scripts/test_scope.py --check` 登记（33 支文件 / 1459 用例）。

## 未通过 / 存疑项

**1. 上一轮点名的 6 处残留：全部确认消除。**
逐文件断言（含/不含 4 项检查）6× `OK`，且行为复现已从 `unavailable` 变为「经 `runs.py resolve` 取 run_dir → `structured`」。契约测试已把这一约束固定为 7 份文档的参数化断言（`tests/test_update_docs_contract.py:155-175`），后续回归不会再漏。

**2. 同类「扁平路径」残留 4 处（非阻断，建议跟进）。**
修复只覆盖了调用 resolver 的 7 份文档；`phase 2` 规范与个别路径表仍把定性输入写成扁平路径，与已修好的命令/协调器自相矛盾：

| 位置 | 现状 | 冲突对象 | 影响 |
|------|------|----------|------|
| `strategies/value/phase2_value_analysis.md:10` | 输入 2 = `{output_dir}/qualitative_input.json` | `value-analysis.md:37/74` 写 `{run_dir}/qualitative_input.json` | phase-2 若照此读取，run-store 布局下会找不到文件 |
| `strategies/value/valuation/phase2_valuation.md:10` | 输入 2 = `{output_dir}/qualitative_input.json` | `valuation.md:22/42` 写 `{run_dir}` | 同上 |
| `strategies/portfolio/coordinator.md:81` | 扫描表写 `output/{code}_{company}/qualitative_input.json` | 同文件 `:86`/`:144` 写 `{run_dir}/qualitative_input.json` | 同文件内两张表互相矛盾 |
| `strategies/value/coordinator.md:262` | `{qualitative} = {output_dir}/qualitative_input.json` | 同文件 `:195` 写 `{run_dir}/qualitative_input.json` | 该 `{qualitative}` 占位符全文件**未被引用**，属僵尸定义 |

判定说明：我仍判 AC-1 成立，因为 AC 的可执行核心（命令/协调器把 `run_dir` 交给 resolver）已全部正确、有契约测试 + e2e 兜底，且我用命令复现的是**正确行为**而非失败；上表 4 处是**规范正文的输入清单失同步**，不改变 resolver 实参。若按「不硬编码扁平路径」的**字面**严格衡量，其中 `phase2_*.md:10` 两处可被判为 AC-1 的部分成立——这是本轮唯一的分歧点，建议顺手把 4 处一并改到 `{run_dir}` 后闭环。复现：`grep -rn "qualitative_input.json" strategies/ | grep -v run_dir`。

**3. AC-5 的 e2e 属「台账级增量 run」，未跑 prepare/模块（边界说明）。**
`tests/test_two_layout_e2e.py:73-106` 的「增量 run」是 `runs.py new --kind report-update` + `finish`（写入新期次快照并切指针），并未对新 run 执行 `prepare`/模块/`resolve_qualitative`，因此它证明的是**布局不变量**（旧 run 不可变、指针切换、旧 run 仍可解析），而不是一条完整的增量分析流水线。这与 AC-5 括号里点名要验的四步一致，故判成立；若后续想要「增量 run 自身结论也可解析」的证据，需要另补一条跑通 prepare + 模块的 e2e。

**4. 环境噪声（不构成 AC 失败）。**
`make verify` 会写 `.coverage`（gitignored）；验收前后 `git status --porcelain` 均为空，本分支未产生任何由我造成的改动，唯一新增文件为本报告。

## 结论

**通过（AC-1…AC-5 全部成立）。**

- 上一轮判不成立的 **AC-1 / AC-2 / AC-5 均已修复并经独立复现确认**：
  - AC-1：6 处残留全部消除；run-store-only 布局下「`runs.py resolve --latest` → run 目录 → resolver `structured`」链路成立；契约测试扩展到 7 份文档；stale 标记与用户可见性未回归（exit 1 / `downstream_stale`）。
  - AC-2：legacy（无 `latest.json`）与 run-store（`adopt --prune`）两种布局均可用；`prepare` 透传 `framework` 且保持加性。
  - AC-5：`tests/test_two_layout_e2e.py` 覆盖 mock baseline → 注入新期次 → 增量 run → 旧 run 目录树与 manifest 摘要不变、仍可解析、latest 指向新 run。
- AC-3（ARCHITECTURE）、AC-4（README / CHANGELOG）保持成立。
- 全量 `make verify` 退出码 0：**1456 passed, 3 skipped in 57.88s，覆盖率 76.44%**。
- 遗留 **1 项非阻断建议**（存疑项 2：`phase2_value_analysis.md:10`、`phase2_valuation.md:10`、`portfolio/coordinator.md:81`、`value/coordinator.md:262` 仍写扁平 `qualitative_input.json`）——不影响 AC-1 的判定与本次放行，但建议在合入前或紧随其后一并改成 `{run_dir}`，以免 phase-2 规范与命令继续各说各话。
