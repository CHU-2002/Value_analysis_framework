---
batch: 2026-09-25-REQ-006.1
date: 2026-09-25
reviewer: independent-agent
independence: independent
requirements: REQ-006.1
base: 56a7d487ba19ce76f95ad17180e02064202e1d17
full-suite: "1564 passed, 3 skipped，覆盖率 78.31%"
---

# 独立验收报告：REQ-006.1 财报分析端到端实跑加固与实跑验收规则

## 独立验收声明

**本报告的独立评审者没有参与实现**：我没有参与 REQ-006.1 的任何一行代码、测试或文档，
没有看过实现过程、PR 讨论或实现者的口头结论，也没有读过会话内上下文。我开始工作时
对这份需求一无所知，只按 `docs/requirements/REQ-006-requirement-test-dev-flow.md` 的
`### REQ-006.1` 原文逐条核对。

独立性措施（可复现）：

- 只读实现（`scripts/`、`tests/`），不改任何实现文件；本报告只写自己的命令与观察，
  不把 `docs/run-records/2026-09-25-REQ-006.1-AC-1.9-实跑记录.md` 的结论抄成自己的结论
  ——该文件只当**素材**，其中每条我另行复算/复现（见「## 实跑记录」）。
- 自己跑 `make verify`（不是引用别人的数字）；每条 AC 自己跑对应测试、并尽量用 CLI 端到端复核。
- 接受验基线：`main = 56a7d487ba19ce76f95ad17180e02064202e1d17`（`git rev-parse main` 与
  `git rev-parse origin/main` 都指向它；工作区干净，仅有一个与本任务无关的未跟踪文件 `dsh-lan.patch.yml`）。

## 全量测试

```bash
make verify
```

- 结果：**`1564 passed, 3 skipped in 40.60s`**
  （3 条 skip 是 `tests/test_integration.py` 的 live API 用例，原因行写明
  `TUSHARE_TOKEN not set — skipping live API tests`：`make` 不加载仓库 `.env`，属预期）。
- 覆盖率：**78.31%**（`Required test coverage of 74% reached`，门槛 `COV_MIN=74`）。
- 追溯门禁：`tests/test_requirement_traceability.py` **17 passed**。
- scope 门禁：`测试 scope 检查通过：34 支文件、1567 个用例，均在预算内`（预算 48 / 1800）。
- 回归门禁：`main 批量回归门禁通过（自 e453255 起累积的特性合入未达阈值 3）`。
- lint：`compileall` + `git diff --check` 无输出，通过。
- 基线（对比的 main sha）：`56a7d487ba19ce76f95ad17180e02064202e1d17`
  （`git rev-parse HEAD` 同值）。
- 环境：macOS，`.venv/bin/python` 3.12.13。

## 逐条验收

### REQ-006.1 财报分析端到端实跑加固与实跑验收规则

- [x] **AC-1.1**：CNINFO 查询协议可配置、https 403 时回退 http 且请求头正确；两条路径都有 mock 单测。
  - 实现：`scripts/discover_report.py:34-42`（`DEFAULT_CNINFO_PROTOCOL`/`CNINFO_PROTOCOLS`/`CNINFO_PROTOCOL_ENV_VAR`）、
    `:72-76`（`build_cninfo_query_url`）、`:91-118`（`resolve_cninfo_protocol`：显式参数 > 环境变量 > https 默认，
    非法显式值 `ValueError`，非法环境值告警后回落）、`:121-138`（无显式协议时把另一个协议作为一次性回退）、
    `:141-163`（`post_cninfo_query`：403 才换协议、`headers=CNINFO_HEADERS` 与 `data=` 完全相同、
    换协议打印 stderr 告警、全部 403 时返回最后一个响应交给 `raise_for_status`）、
    `:823-828`（`--cninfo-protocol`）。两个调用点 `:359`、`:463` 都走 `post_cninfo_query`，
    全文件再无第二处 `requests.post`（`grep -n "requests.post" scripts/discover_report.py` 只有 `:154`）。
  - 测试：`tests/test_discover_report.py:874-1025`（`TestCninfoProtocolFallback`，10 例）。
    我用 `pytest -q tests/test_discover_report.py::TestCninfoProtocolFallback` 实跑：通过。
    关键用例 `test_https_403_falls_back_to_http_and_keeps_headers`（`:916-937`）断言两次请求的 URL 是
    `https://…` → `http://…`、两次 `headers` 对象**同一个** `CNINFO_HEADERS`、且 stderr 出现 `403`；
    `test_explicit_https_pins_and_does_not_fall_back`（`:962-970`）断言显式协议只发一次请求。
  - 判定：成立。回退是「403 才回退」，不是无条件降级；显式指定协议被钉住，符合判据原文。

- [x] **AC-1.2**：Tushare token 以不写 HOME 的方式注入，只读 HOME 下不再 `PermissionError`。
  - 实现：`scripts/tushare_collector.py:71-88` `_new_pro_api()` 直接 `ts.pro_api(self.token, timeout=30)`，
    不再调用 `ts.set_token()`；`scripts/screener_core.py:172-182` 同法。
    `grep -rn "set_token" scripts/` 除 docstring/注释外无调用点。
  - 测试：`tests/test_tushare_client.py:55-104`（3 例）与 `tests/test_screener.py:241-269`（2 例）；
    其中 `test_collector_entry_point_with_read_only_home`/`test_get_pro_with_read_only_home` 用**真实 tushare**
    + `HOME` 指向 `chmod 0o500` 的目录，断言 `pro._DataApi__token` 已注入且 `home/tk.csv` 不存在。
    单独实跑 `TestScreenerTokenInjection`：3 passed。
  - 我自己的端到端复核（离线，构造客户端不发网络请求；`tushare/pro/data_pro.py:pro_api` 只在 `token` 为空时才读
    `~/tk.csv`，非空 token 直接 `client.DataApi(...)`）：
    ```bash
    rm -rf /tmp/ro-verify && mkdir -p /tmp/ro-verify && chmod 555 /tmp/ro-verify
    cd /Users/everett.li/Project/Turtle_investment_framework
    HOME=/tmp/ro-verify .venv/bin/python -c "
    import sys; sys.path.insert(0,'scripts')
    from config import get_token
    from tushare_collector import TushareClient
    from screener_core import TushareScreener
    t=get_token(); c=TushareClient(t); s=TushareScreener(t); p=s._get_pro()
    print(type(c.pro).__name__, type(p).__name__, p._DataApi__token==t)"
    ls -A /tmp/ro-verify
    ```
    观察：`DataApi DataApi True`，`exit=0`；`touch /tmp/ro-verify/probe` → `Permission denied`（确认 HOME 真只读）；
    `ls -A` **空**，未生成 `~/tk.csv`。
  - 局限（声明）：我没有在本轮重新发起真实联网采集（真实 token 的实跑按 `AGENTS.md` 由人执行），
    上面是「代码路径 + 只读 HOME 下真实客户端构造」的替代手段；真实性网络路径的证据是 AC-1.9 的实跑记录与产物
    （见「## 实跑记录」第 3 条）。
  - 判定：成立。

- [x] **AC-1.3**：文档 Step 4 与 `prepare` 参数表一致（带 `--run-id`）；run 目录 id 与传入 id 分叉时 `prepare` 报错；有契约测试。
  - 实现：`scripts/results/prepare.py:170-187`——先算默认 id，但若运行目录有 `run.json`，
    则**以 `run.json` 里 `runs.py new` 签发的 id 为准**；显式传入且不一致 → `raise ValueError(...)`
    （消息含 `ledger id and the run's internal id cannot diverge`）。CLI 参数 `:284`。
  - 文档：`docs/PERIODIC_UPDATE_PLAN.md:209-211` 的 Step 4 已写 `prepare --run-id {run_id} --primary-period`
    并说明「否则台账 id 与 run.json 分叉——`prepare` 现在会直接报错」。
  - 测试：`tests/test_results_pipeline.py:1155-1160`（分叉报错）、`:1163-1169`（沿用 run.json 的 id）；
    契约测试 `tests/test_update_docs_contract.py:266-274` 断言文档里真的含
    `prepare --run-id {run_id} --primary-period` 与「台账 id 与 run.json 分叉」字样。三例实跑通过。
  - 判定：成立（三条子判据：文档一致 ✓、分叉报错 ✓、契约测试钉住 ✓）。

- [x] **AC-1.4**：`period_delta` 作为可选模块参与 digest；提供时不再 `result_digest does not match`；
  不提供时 digest 与基线逐字节一致。
  - 实现：`scripts/results/resolve_qualitative.py:34-41`（`OPTIONAL_MODULES` 增 `period_delta`）、
    `:257-269`（**仅当 `modules/period_delta/result.json` 存在**才进入 `optional_results`，并逐条走
    `validate_result_evidence`）、`:280` `upstream_digest = result_set_digest(artifact_results)`；
    `reconciliation` 的 `result_digest` 必须等于它（`:286` 调用 `_load_reconciliation`，`:110-111` 在
    不等时报 `reconciliation result_digest does not match module results`）。
  - 测试：`tests/test_results_pipeline.py:1109-1133`（提供 D7：`source == "structured"`、
    `period_delta in payload["modules"]`、重算 digest == reconciliation `result_digest`）、
    `:1136-1143`（不提供 D7：`period_delta not in modules`、进入 `missing_modules`）。两例实跑通过。
  - 真实产物（我自己的复核）：两轮 run 的 `qualitative_input.json` 的 `source` 均为 `structured`，
    `missing_modules == []`；我重算 5 个模块（4 核心 + D7）的 `result_set_digest`：
    `20260925T042255414048Z` → `c9761a965512ad54…`、`20260925T053458757536Z` → `96b0774dcd20d1f1…`，
    与各自 `synthesis/reconciliation.json` 的 `result_digest` 及 `synthesis/result.json` 的
    `upstream_digest` **三者完全一致**——即 AC-1.4 第一半在真实产物上成立。
  - 「不提供时逐字节一致」的独立依据：`git diff --stat 4450863^ 4450863 -- scripts/results/schema.py`
    **为空**（`result_set_digest` 未被动过）；旧代码（`git show 4450863^:scripts/results/resolve_qualitative.py`
    的 `:273`）与新代码（`:280`）同样是
    `result_set_digest(artifact_results) if len(results) == len(CORE_MODULES) else None`，
    而 D7 缺席时 `artifact_results` 就只有 4 个核心模块。我另用旧树（`git archive 4450863^` 解到 `/tmp/old-repo`）
    对同一批真实模块 JSON 计算 `result_set_digest`：核心四模块
    `f726bb8bf7f6eff7e94d6fa88f70e84052b36e8a76b7ec21fcaacfad44546699`，与当前代码**逐字节相同**。
  - 判定：成立。

- [x] **AC-1.5**：`synthesis` 与 `change_report` 默认预算按真实载荷设定，「4 核心 + D7 全卡片」不丢卡；
  丢卡时 payload 与 CLI 都列出被丢模块与原因。
  - 实现：`scripts/results/synthesis.py:20` `DEFAULT_MAX_CHARS = 120000`（CLI 默认 `:309`）；
    丢卡记账 `:60-96`（`_candidates` 覆盖 metrics/risks/claims/quality/watchlist，
    `_list_item_label` 给出可读标签）、`:233-245`（`budget.dropped` 列模块 + 最多 3 个字段样本、
    `dropped_count` 计数）、`:254-259`（**受保护内容**装不下时抛错而不是静默）、
    `:289-299`（记账本身超预算时继续让出并记 `budget:disclosure`）、`:323-328`（CLI 打印
    `预算不足，已丢弃 N 项卡片内容（module: field…）`）。
    `scripts/results/change_report.py:85`（默认 160000）、`:212-223`（`degraded` 块写明
    `prior_synthesis_dropped`/`synthesis_dropped`/条数）、`:303-314`（CLI 打印降级项）。
  - 真实产物（判据的决定性依据）：`output/600887_伊利/runs/20260925T053458757536Z/synthesis/context.json`
    的 `budget` 我逐字段读出并复算：
    `max_chars=120000`（= `DEFAULT_MAX_CHARS`，即该轮**未传** `--max-chars`）、
    `actual_chars=97359`、`dropped_count=0`、`dropped={}`、`module_count=6`、`evidence_count=31`；
    逐卡片 `omitted` 我把 6 张卡全部相加：`metrics/claims/risks/watchlist/evidence` 与
    `quality.{missing_inputs,warnings,unresolved_questions}` **合计 0**（与实跑记录一致，且是我自己算的）。
    第一轮 `20260925T042255414048Z`：`120000 / 96496 / 0`。变化报告侧：
    `change_report_context.json` 的 `budget = 160000 / 67534`，
    `degraded = {prior_synthesis_dropped: false, synthesis_dropped: false, prior_synthesis_claims: 6, synthesis_claims: 6}`。
  - 测试：`tests/test_results_pipeline.py:1188-1198`（默认值 ≥ 96,000）、`:1201-1227`（真实规模夹具下
    默认预算零丢弃）、`:1230-1261`（列表条目逐条计数、标签不得为 `None`）、`:1172-1185`（预算不足必须列出被丢模块）；
    `tests/test_change_report.py:236-265`、`:459-486`（默认值 + CLI 必须说出来）、`:373-395`。
    我实跑：`pytest -q tests/test_results_pipeline.py -k "budget or dropped or disclose"` 与
    `pytest -q tests/test_change_report.py -k "budget or degrade or default"` → 全通过。
  - 我自己做的对照实验（复现 T8 声称的 148 → 220）：
    ```bash
    R1=output/600887_伊利/runs/20260925T042255414048Z
    # 旧代码（b23270b^，即 T8 修复前）
    rm -rf /tmp/old-syn && mkdir -p /tmp/old-syn && git archive b23270b^ | tar -x -C /tmp/old-syn
    (cd /tmp/old-syn && .venv/bin/python -m scripts.results.synthesis <4 core --input> \
       --optional-input $R1/modules/period_delta/result.json \
       --optional-input $R1/modules/holding_structure/result.json \
       --reconciliation $R1/synthesis/reconciliation.json --evidence-index $R1/evidence/index.json \
       --output /tmp/syn-40k-old.json --max-chars 40000)
    # 当前代码，同一批真实模块、同一预算
    .venv/bin/python -m scripts.results.synthesis <同样的参数> --output /tmp/syn-40k.json --max-chars 40000
    ```
    观察：旧代码 `dropped_count=148`，标签里真的出现 `claims:None`（不可读，正是 F15）；
    当前代码 **220**，标签是可读的 `claims:business_moat-003` 等。另对 R2 载荷同法得到 257。
    这也直接验证了「丢卡时 payload 与 CLI 都列出被丢模块与原因」：两轮 CLI 都打印了 6 个模块名
    与字段标签（模块键齐全、原因即预算不足），`--max-chars 30000` 跑 `change_report` 时
    CLI 打印 `预算不足，已降级：prior_synthesis 整卡丢弃；synthesis 整卡丢弃`，
    payload `degraded` 两个 flag 同为 `true`。
  - 判定：成立（两半都成立）。附带一处**非阻断**的记账精度观察，见「未通过 / 存疑项」第 1 条。

- [x] **AC-1.6**：`finish --artifact name=path` 三条路径（不存在报错且不写台账 / 相对路径按 cwd 解析并存绝对路径 /
  重复 name 报错）都有单测。
  - 实现：`scripts/runs.py:379-419` `_parse_artifacts()`：`name=path` 格式校验、重复 name
    `raise LedgerError("--artifact name given more than once")`、相对路径 `candidate.resolve()`
    （基于 `Path.cwd()`，不是 `--company-dir`）、`not candidate.is_file()` 时 `does not exist` 报错；
    在写台账前调用（`:466`），所以失败不会留下台账条目。
  - 测试：`tests/test_runs_ledger.py:364-373`（缺失 → 报错、`history.jsonl` 不产生）、
    `:384-403`（相对路径按调用者 cwd 解析并在 `history.jsonl`/`latest.json` 里存绝对路径）、
    `:406-418`（重复 name）。三例实跑通过。
  - 我自己的 CLI 端到端复核：
    ```bash
    rm -rf /tmp/ac16 && mkdir -p /tmp/ac16/co
    .venv/bin/python scripts/runs.py new --company-dir /tmp/ac16/co --ticker 600887.SH \
      --company Test --market CN --kind report-update --primary-period 2026H1
    .venv/bin/python scripts/runs.py finish --company-dir /tmp/ac16/co \
      --run-dir /tmp/ac16/co/runs/<id> --status complete --artifact report=does-not-exist.md
    ```
    观察：`finish` **exit=2**，stderr 写明
    `--artifact 'report' does not exist: does-not-exist.md (resolved to <repo>/does-not-exist.md)`，
    `/tmp/ac16/co/` 下**只有** `runs/`，没有 `history.jsonl` / `latest.json` / `record.json`；
    随后在 cwd=`/tmp/ac16` 下用 `report=out/report.md` 成功，台账里存的是
    `/private/tmp/ac16/out/report.md`（绝对路径，按调用者 cwd）。真实两轮的台账 `artifacts` 也全是绝对路径。
  - 判定：成立。

- [x] **AC-1.7**：模块结果引用的每条 evidence id 必须属于该模块 bundle；越界可由检查手段检出。
  - 实现：校验器 `scripts/results/evidence.py:167-215`
    （`bundle_evidence_ids` / `validate_bundle_evidence`：同时检查 `result["evidence"]`
    与每条 claim 的 `evidence_ids`），可执行入口 `scripts/results/validate_result.py:38-47`
    的 `--context <bundle.json>`（越界时 `exit 1`，消息 `… is outside this module's context bundle`）。
  - 测试：`tests/test_results_pipeline.py:1264-1277`（越界被拒）、`:1280-1282`（bundle 无证据 id 被拒）、
    `:1285-1299`（CLI `--context` 退出码 1）。实跑通过。
  - 我自己的端到端复核：从 R2 的 `evidence/index.json` 里挑一条**在索引里、但不在
    `contexts/environment.json` 的 bundle 里**的 id（实测挑到 `market_data:data_pack_market:001`），
    追加进 `environment/result.json` 的 `evidence` 后跑
    `validate_result.py /tmp/tampered2.json --evidence-index … --context …` →
    **exit=1**，stderr：`Evidence outside the module's context bundle: evidence[7] ID
    'market_data:data_pack_market:001' is outside this module's context bundle`。
    另外我用官方 `validate_bundle_evidence` 扫描两轮 run 的 12 个模块结果：**违例 0 个**
    （claims 的引用也全在 bundle 内）。
  - 判定：成立（检出手腕存在且真的会红；真实产物也确实是合规的）。

- [x] **AC-1.8**：写了「实跑」的编号，收口报告必须带「## 实跑记录」（可复制命令），由 `acceptance_gate.py` 强制；
  实跑发现当次登记，不允许只用运行时补丁绕过。
  - 实现：`scripts/acceptance_gate.py:46-52`（`LIVE_RUN_HEADING` / `LIVE_RUN_HINT` / 命令块正则）、
    `:199-201` `requires_live_run()`（编号的 scoped 文本含「实跑」即要求）、
    `:305-325`——对推进到 `verified` 的每个编号逐一检查：报告必须有该编号的逐条验收、
    每条 AC 必须打勾、且要求实跑时 `## 实跑记录` 小节里必须有**代码块形式的可复制命令**，
    否则报错拒绝。`:114-116` 的 `scannable()` 会先剔除围栏代码块/引用块/行内代码，
    避免把举例当成结论。
  - 测试：`tests/test_release_gates.py:550-573`（无实跑记录 → 门禁报「实跑记录」问题；补齐命令后通过）、
    `:575-578`（`requires_live_run("REQ-006.1") is True`、`REQ-005 is False`）。实跑通过。
  - 登记面：实跑记录 §6 把 27 条发现（F1~F27）逐条给了去处：F6/F15 归任务 T8（PR #46 已合入 `b23270b`）、
    F2 归 `REQ-009.4` 的 AC-4.5、其余 24 条归新受理的子需求 `REQ-006.2`
    （`docs/requirements/REQ-006-requirement-test-dev-flow.md:96-161` 有 `AC-2.1`~`AC-2.8`），
    5 条经核实为「设计如此」。我抽查了其中几条（见「## 实跑记录」第 5 条），现象都真实存在。
    第一轮那处 `--max-chars 120000` 覆盖在实跑记录 §0/§3 明示披露，并当次登记为 T8 修掉
    ——符合「补丁必须当次登记为待修项」。
  - 判定：成立。

- [x] **AC-1.9**：只读 HOME + 真实 token 下复跑一次迭代，不用运行时补丁，产出完整 run，命令与观察写进实跑记录。
  - 素材：`docs/run-records/2026-09-25-REQ-006.1-AC-1.9-实跑记录.md`（两轮：`20260925T042255414048Z` @ `fff05d74`、
    `20260925T053458757536Z` @ `b23270b`）。
  - **我自己对产物的复核**（命令与数字见「## 实跑记录」第 3 条）：
    - 台账/指纹：`history.jsonl` 4 条；两轮的 `framework.git_commit`（`fff05d74…` / `b23270b8…`）
      与各自 `runs/<id>/run.json` 的 `framework.git_commit` **一致**，`dirty=false`；
      `latest.json` → `20260925T053458757536Z`；`record.json` 的 `latest_run`/`framework` 同值。
    - manifest：两轮 `run_manifest.json` 的 `validate_manifest_inputs` 与 `validate_manifest_artifacts`
      都返回 `[]`（无错）；`run_id` 与目录名一致。
    - 结构化结果：6 个 `modules/*/result.json` 全部 `validate_result` + `validate_result_evidence` 通过；
      `result_set_digest` == `reconciliation.result_digest` == `synthesis.upstream_digest`；
      `evidence/index.json` 的 `run_id`/`input_digest` 与 manifest 一致；`qualitative_input.json` `source=structured`。
    - 报告：`qualitative_report.md`（50,895 字节）与 `change_report_2026H1.md`（32,542 字节）存在且非空，
      sidecar `change_report_2026H1.json` 存在（`schema=investment.change_report`、`schema_version=1.0`）。
    - 只读 HOME：`/tmp/ro-home` 现在是 `dr-xr-xr-x`（555）的**空**目录、无 `tk.csv`；
      R2 的输入快照时间线（`data_pack_market.md` 13:34:46 → `run.json` 13:34:58）与记录 §Step 1B/3 相符。
  - **局限（声明）**：真实 token 的联网采集我没有重跑（按 `AGENTS.md`，「实跑」类判据由人执行，
    且会真实消耗配额）；我复核的是**产物与台账的自洽性、环境与时间线**，以及 AC-1.2 的离线替代验证。
    因此 AC-1.9 的「真实联网」这一环，我的依据是实跑记录 + 可核对的产物，不是我自己重新发起的请求。
  - 判定：成立（在上述依据范围内）。附带观察见「未通过 / 存疑项」第 4、5 条。

- [x] **AC-1.10**：`TushareScreener._safe_call` 的重试作用在重建后的客户端上；有「第一次失败、第二次成功」回归测试。
  - 实现：`scripts/screener_core.py:199-221`——循环体里每次都 `pro = self._get_pro()` 再 `getattr(pro, api_name)`
    （**不是**在循环外取一次）；失败分支 `self._pro = self._new_pro_api()`；
    `_get_pro()`（`:184-192`）在 `self._pro` 被重建后会返回新对象。
  - 测试：`tests/test_screener.py:271-296` `test_retry_actually_uses_the_rebuilt_client`：
    旧 `pro.income` 抛 `OSError("RemoteDisconnected")`，新 `pro.income` 返回数据；
    断言 `old_pro.income.call_count == 1`、`new_pro.income.call_count == 1`、
    `tushare.pro_api` 只被调用一次（重建），且 `tushare.set_token` **未被调用**。实跑通过。
  - 判定：成立（测试确实证明「第一次打在旧客户端、第二次打在重建后的客户端」）。

### 附：任务 T7（门禁阈值调整）的独立复核

T7 行写着「本项是门禁阈值调整，按 AC-3 的评审粒度在 REQ-006.1 收口时由独立评审者复核」。我复核的内容：

- 预算现值：`scripts/test_scope.py:44-45` `MAX_TEST_FILES = 48`、`MAX_COLLECTED_CASES = 1800`；
  `make scope` 实测 **34/48 文件、1567/1800 用例**——离新上限仍有实质余量，不是为了「刚好不红」而上调。
- 留痕：`scripts/test_scope.py:35` 注释写明 2026-09-21 上调（40→48、1600→1800）、经需求 owner 书面批准；
  `docs/TESTING.md:83,102` 与 `docs/DEVELOPMENT.md:183` 同步到同一数字，并保留「先清理再谈上调」的纪律。
- AC-7 例外通道：需求条目 `:185-189` 已把「门禁阈值/预算经 owner 书面批准 + 变更记录后**唯一例外**」
  写进判据，并在 `:199-207` 留了变更记录（原条款、原因、新条款、批准人 CHU-2002、
  以及「独立评审在该子需求收口时一次完成」的指明）。AC-7 原文的收窄有 owner 批准与留痕，无静默放宽。
- 结论：T7 的交付物（代码常量、两份文档、AC-7 例外通道与留痕）齐备；`## 任务清单` 里 T7 仍写「进行中 / PR #TBD」
  与「完成 = 有独立验收链接」的约定自洽——本次报告即可作为它的独立复核链接，T7 的 PR 号由收口 PR 回填。

## 实跑记录

> 本节的每条命令都是**我自己**在 2026-09-25 于仓库根目录
> `/Users/everett.li/Project/Turtle_investment_framework` 执行的；我只读 `output/`，未改任何 run 产物。

**1. 环境（我执行时的实际环境）**

| 项 | 值 |
|----|-----|
| 日期 | 2026-09-25 14:00 CST |
| Python | `.venv/bin/python` 3.12.13 |
| 基线 | `main = origin/main = 56a7d487ba19ce76f95ad17180e02064202e1d17`；`git status` 只有未跟踪的 `dsh-lan.patch.yml` |
| 我的只读 HOME | `/tmp/ro-verify`（`chmod 555`，写探针 `touch` → `Permission denied`） |
| 实跑记录的 HOME | `/tmp/ro-home`（现存、`dr-xr-xr-x`、空、无 `tk.csv`） |
| token | 仅从仓库 `.env` 经 `scripts/config.py:get_token()` 读出（我会打印 `True/len`，**不打印 token 本身**） |

**2. 全量门禁（我跑的）**

```bash
make verify
```
观察：`1564 passed, 3 skipped in 40.60s`；覆盖率 `78.31%`（≥74）；`17 passed`（追溯）；
`测试 scope 检查通过：34 支文件、1567 个用例，均在预算内`；回归门禁「未达阈值 3」通过。退出码 0。

**3. AC-1.9 产物的自洽性复核（我写的脚本，只读 `output/`）**

```bash
.venv/bin/python -c "
import json,pathlib,sys; sys.path.insert(0,'scripts')
from results.manifest import validate_manifest_inputs, validate_manifest_artifacts
from results.schema import validate_result, result_set_digest
from results.resolve_qualitative import validate_result_evidence
from results.evidence import validate_bundle_evidence
C=pathlib.Path('output/600887_伊利')
for rid in ['20260925T042255414048Z','20260925T053458757536Z']:
    R=C/'runs'/rid
    m=json.loads((R/'run_manifest.json').read_text())
    print(rid,'manifest errors',validate_manifest_inputs(m),validate_manifest_artifacts(m))
    idx=json.loads((R/'evidence'/'index.json').read_text())
    res=[json.loads((R/'modules'/d.name/'result.json').read_text()) for d in sorted((R/'modules').iterdir())]
    print('  digest',result_set_digest(res),
          'recon',json.loads((R/'synthesis'/'reconciliation.json').read_text())['result_digest'],
          'synthesis',json.loads((R/'synthesis'/'result.json').read_text())['upstream_digest'])
    print('  bundle violations',[d.name for d in sorted((R/'modules').iterdir())
          if validate_bundle_evidence(json.loads((R/'modules'/d.name/'result.json').read_text()),
             json.loads((R/'contexts'/(d.name+'.json')).read_text()))])
    print('  source',json.loads((R/'qualitative_input.json').read_text())['source'])
"
python3 -c "
import json
for l in open('output/600887_伊利/history.jsonl'):
    e=json.loads(l)
    rj=json.load(open('output/600887_伊利/runs/%s/run.json'%e['run_id']))
    print(e['run_id'], e['framework']['git_commit']==rj['framework']['git_commit'], rj['framework']['dirty'], e['status'])
"
wc -c output/600887_伊利/runs/20260925T053458757536Z/qualitative_report.md \
      output/600887_伊利/runs/20260925T053458757536Z/change_report_2026H1.md
```
观察：
- 两轮 `manifest errors [] []`；`run_id` 与目录名一致；`input_digest` 分别
  `d6c3b3e0…` / `ca9781d2…`（第二轮 Step 1B 刷新了行情数据，两轮不可比的是数值而非框架）。
- 两轮 digest 三者一致（`c9761a96…` / `96b0774d…`）；`bundle violations []`；
  `source structured`（两轮 `missing_modules == []`）。
- 台账指纹与 `run.json` 一致（`True True …`），`dirty=false`，`status=complete`；`latest.json` 指向第二轮。
- 报告非空：50,895 / 32,542 字节。

**4. AC-1.2 的离线实跑替代（只读 HOME 下真实构造客户端）**

命令见「AC-1.2」条目（`HOME=/tmp/ro-verify .venv/bin/python -c "…TushareClient/TushareScreener…"`）。
观察：`DataApi DataApi True`、退出码 0、`/tmp/ro-verify` 执行前后都是空目录、写探针被拒。
局限：未发真实网络请求（`pro_api` 构造不联网）；真实联网路径的证据来自 AC-1.9 的实跑记录与产物。

**5. 实跑记录里几条「只有真跑才能看见」的发现，我用自己的观察复算**

```bash
# F11：总股数量级（用数据包自己的两个数字反推）
grep -n "当前价\|总市值 (万元)\|总股本 (万股)" output/600887_伊利/data_pack_market.md
# F8：§9 重复行与字面 nan
awk '/^## 9\./,/^## 10\./' output/600887_伊利/data_pack_market.md | grep -n "nan\|冷饮\|其他"
# F9：§12「营收同比增长率 (%)」整行为 —
awk '/^## 12\./,/^## 13\./' output/600887_伊利/data_pack_market.md | grep -n "营收同比增长率"
# F17：§13.1 的异常期次
awk '/^## 13\./,/^## 14\./' output/600887_伊利/data_pack_market.md | head -12
# 现状复核：HEAD 移动后 analysis_status 是否仍如记录 §Step 10
.venv/bin/python scripts/analysis_status.py --company-dir output/600887_伊利 --ticker 600887 --json
```
观察：
- **F11 成立**：`§1 当前价 26.9`、`总市值 (万元) 17,015,220.01`、`§16 总股本 (万股) 63.25`；
  17,015,220.01 万元 ÷ 26.9 元 = **632,536.06 万股**，与 `63.25` 差 **10,001 倍**（F11 的数字我复算吻合）。
- **F8 成立**：`§9` 同时有 `冷饮产品系列` 与 `冷饮产品`、`其他主营业务` 与 `其他` 两组重复行，
  且毛利率列有字面 `nan`（`| 合计特别调整 | 158.78 | — | nan |`）。
- **F9 成立**：`§12` 的 `营收同比增长率 (%)` 整行是 `—`，而 `净利润同比增长率 (%)` 有值。
- **F17 成立**：`§13.1` 的高风险只报 `2007→2008`、`2008→2009`，本期减值异常未出现在列出的两条里。
- **现状复核**：`analysis_status --json` 现在 **exit=3**、`state=stale`、`reasons=[downstream_stale, framework_changed]`，
  其中 `framework_changed` 的 detail 是 `code_fingerprint: b23270b8… -> 56a7d487…`。
  即：实跑记录 §Step 10 当时观察到的「只有 downstream_stale、exit 1」在记录提交后**已不再成立**
  ——不是记录写错，而是 `code_fingerprint` = HEAD sha，**纯文档提交** 也会把 run 判成 `framework_changed`
  （见「未通过 / 存疑项」第 5 条；该现象不在实跑记录的发现清单里，是我这一轮新观察到的）。

**6. 我没有做的事（边界）**

- 没有重新发起真实联网采集/下载（不做、也不应做真实 token 消耗，且 `AGENTS.md` 要求实跑由人执行）。
- 没有改 `output/` 下任何既有文件；没有改任何实现、测试或其它文档（除本报告与状态推进所需的两个需求文件）。
- 没有改动未跟踪的 `dsh-lan.patch.yml`。

## 未通过 / 存疑项

没有 AC 不成立。以下是**不影响本次判定**的观察与记录性问题，供收口方决定去处：

1. **`dropped_count` 的口径仍是「准入队列标签去重数」，不是「被丢条目数」（低）**。
   我实测：R1 载荷 + `--max-chars 40000`，当前代码 `dropped_count=220`，而逐卡片 `omitted` 合计 299
   （其中 `evidence` 31 条按设计不进 `_candidates`），即非 evidence 的 268 条里有 48 条未被计数
   —— 差额来自 `:297` 的 `budget:disclosure` 标签被 `set()` 折叠（每个模块折叠成 1 条）。
   发现清单 F15 已把「`set()` 去重 + evidence 不计数」写成口径说明，**AC-1.5 的判据原文只要求
   「列出被丢模块与原因」，这一点是成立的**；但如果下游把 `dropped_count` 当精确丢弃条数用，会低估。
   建议：在 `docs/` 或 `budget` 字段旁写明口径（或给 `budget:disclosure` 带上序号），不需要改判据。

2. **实跑记录 §Step 8 的字节数与磁盘不符（低）**。记录写 `change_report_2026H1.md`（32,295 字节 /
   16,572 字符），我 `wc -c` 实测 **32,542 字节**、字符数 16,572（一致）。
   文件 mtime 13:46:56 晚于 `qualitative_report.md`（13:44:20），看起来记录抄的是中间态。
   建议：收口时把该数字改成实测值或注明取样时点。同时该处写 `schema=investment.change_report/1.0`，
   实际 sidecar 是 `schema=investment.change_report` + `schema_version=1.0`（同一处笔误）。

3. **实跑记录 §6 标题写「26 条」，实际是 27 条（低）**。发现清单有 F1~F27 共 27 条
   （`grep -c "^## F"` = 28，含一条 `## F6 补充`），提交信息也写 27。标题的计数与正文自相矛盾。

4. **实跑记录 §8 的 owner 签署框未勾选（中，流程性）**。文件 front matter 写了
   `recorded-by: AI agent（会话内受需求 owner 授权代跑）` 与授权原话，但 §8 两个
   `- [ ]` 仍是空框、签署人/日期为空。`AGENTS.md` 把「真实 token / 真实环境的验收（实跑类判据）」
   列为**必须停下来问人**的事项，所以这份记录的人力授权目前只有自述、没有签署留痕。
   我判定这不构成 AC-1.9 的缺口（AC-1.9 的判据是产物 + 记录，两者都在），
   但建议收口 PR 里让 owner 补签，别让「人执行的实跑」停留在自述状态。

5. **`code_fingerprint` = HEAD sha，纯文档提交也会让 run 变 `framework_changed`（中，设计观察）**。
   我实测：在 `56a7d48`（只改 `docs/`）上跑 `analysis_status`，两轮 AC-1.9 run 都被判 `framework_changed`
   （`b23270b8… -> 56a7d487…`）→ `exit 3`，`recommended_action` 从 `report-update` 变成 `full-rerun`。
   `scripts/version.py:149-161` 的 `code_fingerprint` 优先用 `git rev-parse HEAD`，
   而 `DIRTY_TRACKED_PATHS`（`:46-56`）只用于 `dirty` 标记、不参与指纹。影响：记录实跑的那次文档提交
   本身就使该 run 失效，任何文档/需求改动都会让全部 run 需要「全量重跑」。
   这条**不在**实跑记录的发现清单里，是我本轮新观察到的；它不影响 AC-1.9 的产物完整性，
   但按仓库纪律应当登记（建议作为维护记录或并入 `REQ-006.2` 的 `as_of`/指纹一致性族）。

6. **实跑产物不在版本控制内（低，验证条件限制）**。`output/*` 被 `.gitignore` 忽略
   （`.gitignore:36`），所以两轮 run 的产物只存在于本机，异地评审者/CI 无法核对。
   本次判定因此依赖「本机产物 + 实跑记录」；建议在实跑记录里保留关键文件的 sha256（现只有字节数）。

7. **`REQ-006.1` 与实跑记录的 AC-1.9 证据分布在两个未提交即可能失真的地方（低）**：
   产物在 `output/`（被忽略）、记录在 `docs/run-records/`（已提交）。若将来清理 `output/`，
   本报告第 3、5 条的可复现性会下降。建议把关键摘要（digest、budget、台账指纹）落在记录正文里
   ——现在记录里已有大部分（我逐条对上了），只是缺 sha256。

## 结论

**通过。**

REQ-006.1 的 `AC-1.1`~`AC-1.10` 十条全部成立，且每条都有我**自己执行**的测试或 CLI 端到端复核，
以及可复现的「## 实跑记录」：全量门禁在 `56a7d48` 上是 `1564 passed, 3 skipped`、覆盖率 78.31%；
两轮真实 run 的台账指纹、manifest、digest、bundle 边界与报告产物我都独立核对过；
AC-1.5 的判据在真实载荷（97,359 / 120,000、零丢弃）与「旧 148 → 新 220」的对照实验上都成立。

「未通过 / 存疑项」里的 7 条都**不是** AC 缺口：第 1 条是已披露口径的精度问题，
第 2、3、4 条是实跑记录的记录质量/签署问题（建议在收口 PR 里一并修正），
第 5、6、7 条是建议登记的设计观察。按 `docs/requirements/README.md` §4 的状态机，
本报告支持把 `REQ-006.1` 推进到 `verified`（父需求 `REQ-006` 因 `REQ-006.2` 仍在 `in-progress` 而保持 `in-progress`）。
