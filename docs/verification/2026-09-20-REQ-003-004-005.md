---
batch: 2026-09-20-REQ-003-004-005
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-003, REQ-004, REQ-005
base: 397687f
full-suite: "1436 passed, 3 skipped, 覆盖率 76.29%"
---

# 独立验收报告：REQ-003 / REQ-004 / REQ-005（定期报告增量更新 v1）

## 独立验收声明

本报告的评审者是一个**无上下文的独立 agent**：未参与 REQ-003 / REQ-004 / REQ-005 的实现，
未查看开发过程与 PR 讨论，仅以合入 `main` 后的代码、测试、文档与 git 历史为证据。
核验姿态是**试图证伪**：每条 AC 都到代码/测试/文档里找可复现的证据，找不到就判不成立，
不为实现者圆场。所有复现均在当前工作树（`397687f`，分支 `chore/req-test-dev-flow`，验收前后 `git status --porcelain` 均为空）
用 `.venv/bin/python`（3.12）离线执行，未使用任何 Token。

被验收的三条需求均由代码/文档中**实际存在**的提交引入，已用 `git log` 确认：
`ab635c8`（REQ-003，#16）、`f5317f3`（REQ-004，#17）、`5fa4eb9`/`6c974ab`/`e453255`（REQ-005，#22/#23/#24）。
`main`（`e453255`）是当前 HEAD 的祖先（`git merge-base --is-ancestor main HEAD` 成立）。

## 全量测试

```bash
make verify   # lint + cov + trace + scope-check，等价于 CI 检查项
```

- 结果：**1436 passed, 3 skipped in 58.80s**（`make verify` 退出码 0；`real 1m1.4s`）
- 覆盖率：**76.29%**（门禁 `--cov-fail-under=74`，通过；TOTAL 11065 stmts / 2623 miss）
- 基线（对比的 main sha）：`397687f`（`main` 为其祖先，全量用例在含 `main` 的 HEAD 上全绿）
- 3 个 skip 均为 `tests/test_integration.py` 的实网用例（`TUSHARE_TOKEN not set`），
  不构成对「测试全 mock、不依赖网络与 Token」的反证；其余用例无需网络。
- `tests/test_requirement_traceability.py` 8 passed；`scripts/test_scope.py --check` 通过（32 支文件 / 1439 用例）。

## 逐条验收

### REQ-003 分析迭代台账（run-store）

- [x] **AC-1**：成立。`scripts/version.py` 提供 `FRAMEWORK_VERSION`（`version.py:24`）、`prompt_fingerprint`（`PROMPT_ROOTS` 覆盖 `strategies`/`shared/qualitative`/`.claude/commands`/`.opencode/commands`，`version.py:27-32`；按根相对 POSIX 路径排序后对 `path\0content\0` 取 sha256，`version.py:104-119`）、`code_fingerprint`（git `rev-parse HEAD` + 脏标记，非 git 退化为 `scripts/**/*.py` 哈希，`version.py:149-170`）、`schema_versions()`（`version.py:69-72`）、`framework_block()`（`version.py:173-197`）。重复调用稳定：`tests/test_version.py:65-73`（同一输入两次相等、不同绝对路径相同）、`:99-107`（非 git 退化为 sha256 前缀）。
- [x] **AC-2**：成立（字段命名有偏差，见「存疑项」）。`runs.py` 子命令 `new`/`resolve`/`finish`/`adopt`/`export` 齐全（`runs.py:1073-1138`，另有 `downstream`）；run 落在 `runs/{run_id}/`（`runs.py:264, 277-320`）；`run.json` 记录 `kind`/`primary_period`/`framework`/`supersedes`（`runs.py:309-320`，实测 keys = `['created_at','framework','kind','primary_period','run_id','schema','schema_version','subject','supersedes']`）；退出码语义固定且实测一致：成功 0、`ResolutionError`→3（`runs.py:1192-1194`）、`LedgerError`/`ValueError`/`OSError`→2（`runs.py:1195-1200`），对应 `tests/test_runs_ledger.py:215-227, 336-342`。
- [x] **AC-3**：成立。`_snapshot_file` 默认 `shutil.copy2`，仅 `hardlink=True` 才尝试 `os.link`（`runs.py:155-173`）；`sources_manifest.json` 记录 `method`/`hardlinked`（`runs.py:284-305`）。复现：`create_run(inputs=[market])` → `method='copy'`、inode 不同；随后**原地覆盖源文件** `market.write_text(...)` → 快照内容仍为原值、`sha256` 与 manifest 记录均不变；`hardlink=True` 时 `method='hardlink'` 且 inode 相同。与 `tests/test_runs_ledger.py:45-107` 一致。
- [x] **AC-4**：成立。`adopt_legacy` 默认复制、保留原文件，`prune=True` 才在 `_verify_copy` 通过后删除原件（`runs.py:897-903, 942-947`）。复现：对完整结构化目录 `resolve_qualitative_input` 在 `prune=False` 与 `prune=True` 下**均返回 `source=structured`（warnings 为空）**；对应 `tests/test_runs_ledger.py:605-621`（参数化 prune）。
- [x] **AC-5**：成立。`finish_run` 同一次调用写出 `history.jsonl` 追加行、`latest.json`、`record.json`（`runs.py:472-522`）。复现一次 report-update run 后三者一致：`latest.run_id == record.latest_run == history[-1].run_id`；`record.primary_period == latest.primary_period`；`record.report_periods == history[-1].report_periods`；`record.kind/status` 同源。对应 `tests/test_runs_ledger.py:235-287`。
- [x] **AC-6**：成立。状态集合 `no_record / legacy_layout / up_to_date / stale / broken / unsupported_market`（`analysis_status.py:52-57`）；reasons 覆盖 `new_report`（`:337-343`）、`framework_changed`/`schema_changed`（`:345-360`）、`inputs_changed`（`:362-367`）、`run_failed`/`downstream_stale`（`:318-324`）；港股/美股 `unsupported_market`（`:253-265`）。复现：`finish(status="failed")` → `state=stale`、codes=`['run_failed','downstream_stale']`、exit=3（**不再判 `up_to_date`**）；`00700.HK` / `AAPL.US` / `AAPL` → `unsupported_market`。对应 `tests/test_analysis_status.py:86-99, 257-264`。
- [x] **AC-7**：成立。`discover_periods` 经 `_lazy_discover_periods` 惰性导入，仅在 `check_upstream=True` 时调用（`analysis_status.py:37-49, 340-343`）。复现：`import scripts.analysis_status` 后 `'requests' in sys.modules == False`；默认路径不触网。对应 `tests/test_analysis_status.py:277-290`（未开 `--check-upstream` 时把 `discover_periods` 换成抛异常的桩仍返回 `up_to_date`）。
- [x] **AC-8**：成立。`build_manifest` 在 `framework is None` 时**完全省略该键**，`schema_version` 仍由默认参数保持 `"1.0"`（`manifest.py:118-144`）；`prepare.py:226-245` 才注入 `framework_block()`。复现：不传 framework 时 keys 无 `framework`、`schema_version='1.0'`；传入后新增键恰为 `{'framework'}`，除 `generated_at` 外逐键等价。对应 `tests/test_runs_ledger.py:789-808`。

### REQ-004 定期报告增量更新分析与变化报告

- [x] **AC-1**：成立。`RESULT_TYPE_CONTRACTS["qualitative.period_delta"]` scope=`["D7"]`、恰好 11 个参数（`schema.py:94-109`）；`MODULE_CONFIG["period_delta"]` scope=`["D7"]` 且声明 `prior_analysis` 六段（`context.py:56-64`）；`shared/qualitative/references/output_schema.md:112-128` 的 D7 表同样列出这 11 个参数。三方一致由合同测试锁定：`tests/test_period_delta_module.py:78-93`（契约注册与 11 参数集合）、`tests/test_qualitative_consumers.py:121-133`（`output_schema.md` 解析出的 `documented == implemented`，遍历**全部**契约）。
- [x] **AC-2**：成立。`_module_evidence` 把 `prior_analysis` 六段放在候选组**最前**，且对 prior 直接取 `candidates[:1]` 不做关键词排序截断（`context.py:136-149`）；`selection` 暴露 `prior_analysis_sections` / `missing_prior_analysis` / `omitted_prior_analysis`（`context.py:287-306`）。宽数据包回归由 `tests/test_period_delta_module.py:270-331` 覆盖（宽 index 下 6 个 prior 段全部入选、`max_evidence=12` 恰好饱和且仍保留本期 `market_data`/`pdf_sections`、`max_evidence=3` 时被挤出项进入 `omitted_prior_analysis`）。
- [x] **AC-3**：成立。`prepare_run` 在 `--prior-analysis` 存在时注册 `source_id="prior_analysis"` 证据源（`prepare.py:148-159`），CLI 透传（`prepare.py:278-285`）；文件缺失时记 `prior analysis not found: <path>` 到 `warnings` 并写入 manifest（`prepare.py:148-151, 244-245`），不失败。对应 `tests/test_prepare_prior_analysis.py:56-88`（注册 + manifest 记录）与 `:106-127`（缺失仅 warning、无 prior 时行为不变）。实测 `prepare --prior-analysis` 子进程退出码 0。
- [x] **AC-4**：成立。变化报告对上一 run 的 evidence **逐条**用同一套 locator/excerpt 规则校验，不可校验者丢弃并计数 `prior_evidence_unavailable`，同时把 claims 中失效引用剔除（`change_report.py:138-165`）；结果写入 `degraded.prior_evidence_unavailable` 并进入预算（`change_report.py:207-215`），提示词给出 `cite_prior_via`/`missing_section_wording` 指引（`change_report.py:216-224`）。对应 `tests/test_change_report.py:321-361`（旧快照摘录降级为 1 条且报告仍生成）与 `:415-451`（prior 卡片被挤出时计数归零、错误类型降级为 `unusable_inputs`）。
- [x] **AC-5**：成立（由提示词契约 + 上下文构建器保证，非确定性代码生成）。`change_report.py:22-23` 定义 `investment.change_report_context`；`shared/qualitative/agents/change_report.md:31` 要求写 `change_report_{period}.md`，`:56-77` 要求写 `change_report_{period}.json` 且 `schema="investment.change_report"`；`:23, :51` 与 `change_report.py:221` 明确「本期未披露」措辞与「未披露维度沿用上次/年报依据」。`coordinator_update.md:203` 标注该 sidecar。对应 `tests/test_period_delta_module.py:122-126`（提示词含 `change_report_{period}.md`、`investment.change_report`、`本期未披露`）。
- [x] **AC-6**：成立。`cmp` 实测 `.claude/commands/update-analysis.md` 与 `.opencode/commands/update-analysis.md` **逐字节一致**，sha256 同为 `5ec7de7ddeb8cb4102309329c084c002d9e49ecc6c14d94e7822f00600f05522`；对应 `tests/test_update_docs_contract.py:52-56`。
- [x] **AC-7**：成立。增量流程不自动改写买卖计划：`.claude/commands/update-analysis.md:106` 明确 “**Do not** regenerate or alter a buy/sell plan automatically.”，`value-analysis.md:67-68` Step 2B 同样禁止；`finish_run` 只写 `record.json:downstream` 的 `stale`/`value_computed`/`buy_sell_basis` 布尔标记，不触碰 `value_computed.json`/`buy_sell_*.json`（`runs.py:491-522`）。`period_delta` 不进全量流程：`business-analysis.md:81-85` 的模块清单只有四个核心模块 + 条件触发的 `holding_structure`，全仓 `grep period_delta .claude/commands/business-analysis.md` 无命中，且 `resolve_qualitative` 的必需模块集不含 D7（`resolve_qualitative.py:28-34`）。对应 `tests/test_runs_ledger.py:290-308`、`tests/test_analysis_status.py:102-121`。
- [x] **AC-8**：成立。全量 `make verify` 离线通过（1436 passed / 3 skipped / 76.29%，退出码 0，无 Token）；`main` 是 HEAD 祖先，故含 `main` 的当前树无回归。测试全 mock：3 个 skip 是会读取 `TUSHARE_TOKEN` 的实网 integration 用例。

### REQ-005 增量更新文档与下游接线

- [ ] **AC-1**：**部分成立，整体判不成立**。「新期次后 `value_computed`/`buy_sell_basis` 被标记 stale 且对用户可见」这一半有代码 + 测试支撑（`runs.py:491-496, 509-522` 写 `record.json:downstream`；`analysis_status.py:320-324` 输出 `downstream_stale` 并以退出码 1 暴露；`tests/test_runs_ledger.py:290-308`、`tests/test_analysis_status.py:102-121`），文档也在 `value-analysis.md:32`、`update-analysis.md:105-112`、`README.md:112`、`ARCHITECTURE.md:131` 落地。但「命令通过 `latest.json`/`record.json` 解析产物、不硬编码扁平路径」这一半只修了 `/value-analysis`：`/valuation` 与 `/portfolio-strategy`（及其 coordinator）仍把**公司目录**直接交给 `resolve_qualitative`，而该 resolver 明确不跟随 `latest.json`——`strategies/value/coordinator.md:45` 自己就写了这一点，却未同步这几处调用。复现（run-store-only 布局）：
  ```bash
  .venv/bin/python - <<'PY'
  import sys, tempfile; from pathlib import Path
  sys.path.insert(0, "scripts"); sys.path.insert(0, ".")
  import runs
  from results.resolve_qualitative import resolve_qualitative_input
  from tests.test_results_pipeline import _write_complete_structured_run
  with tempfile.TemporaryDirectory() as td:
      c = Path(td)/"600000_Example"; _write_complete_structured_run(c)
      r = runs.adopt_legacy(c, prune=True)          # 仅保留 runs/<id>/ + latest.json/record.json
      print(resolve_qualitative_input(c, ticker="600000.SH")["source"])        # -> unavailable
      print(resolve_qualitative_input(Path(r["run_dir"]), ticker="600000.SH")["source"])  # -> structured
  PY
  ```
  输出 `unavailable` / `structured`。残留硬编码扁平路径：`.claude/commands/valuation.md:16`、`.opencode/commands/valuation.md:16`、`.claude/commands/portfolio-strategy.md:36`、`.opencode/commands/portfolio-strategy.md:17`、`strategies/value/valuation/coordinator.md:38`、`strategies/portfolio/coordinator.md:86`。契约测试只钉住了 `/value-analysis` 与 value coordinator（`tests/test_update_docs_contract.py:17-26, 59-67`），未覆盖以上文件，所以 CI 不会拦住这个缺口。
- [ ] **AC-2**：**判不成立**（`prepare` 透传 framework 成立，双布局命令不成立）。`prepare.py:230` 确实把 `framework_block()` 透传进 manifest，且加性不影响 `schema_version`（并见 REQ-003 AC-8）。但「run-store 布局下既有命令均可运行」对**组合策略 / 估值**不成立：AC-1 的复现即为其反证——run-store-only 布局下公司目录没有 `run_manifest.json`，`resolve_qualitative(company_dir)` 返回 `source=unavailable`（warnings 列出 4 个 missing module result），因此 `/portfolio-strategy` 会把最有价值的持仓标的标成「待分析」，`/valuation` 会以 status 3 要求先跑 `/business-analysis`（而该命令会写回扁平布局、按 `value-analysis.md:41` 的口径会丢弃增量 run）。受影响的正是 AC-2 点名的「组合策略、结果解析」路径。
- [x] **AC-3**：成立。`docs/ARCHITECTURE.md` 记录 run-store 数据流（`:126` “runs.py finish ─▶ history.jsonl + latest.json + record.json；标记下游 stale”）、布局（`:133-139` latest.json/record.json/history.jsonl 三件套）、run 生命周期与指纹（`:205-214`），并写明跨 run 硬约束「绝不混用两个 run 的参数」（`:214`，另见 `:12`）。契约测试 `tests/test_update_docs_contract.py:96-101, 104-110` 兜住子命令与刷新约束。
- [x] **AC-4**：成立。`README.md:83` 有 `/update-analysis` 用法表项，`:92` 有示例，`:97-112` 有「定期报告增量更新」小节（`analysis_status` 判定、run 布局、期次与下游 stale 说明）。`CHANGELOG.md` 的 `[Unreleased] → Added` 记录本期能力：`:42` run-store/框架指纹/状态判定，`:43` `period_delta` + 变化报告 + `/update-analysis`。
- [ ] **AC-5**：**判不成立**。找不到「mock baseline run → 注入新期次 → 增量 run → 校验旧命令仍可解析」的双布局端到端测试。全仓仅 `tests/test_runs_ledger.py:606-621` 与 `tests/test_analysis_status.py` 会 `import runs`；前者是最接近的 `adopt → resolve_qualitative`（单一布局、无新期次、无增量 run、未验证「旧命令仍可解析」），后者的 report-update 用例只断言 `record.json:downstream`（`tests/test_analysis_status.py:105-121, 137-159`）。`grep -rn "latest.json" tests/` 仅命中 `test_runs_ledger.py`、`test_analysis_status.py`、`test_update_docs_contract.py`（后者是纯文本契约，不跑双布局流程）。复现：`grep -rn "def test_" tests/test_runs_ledger.py | grep -i "adopt\|layout"` → 只有 `test_adopt_copies_flat_layout_without_deleting_originals` 与 `test_adopt_produces_consumable_structured_run`，均无「注入新期次 + 增量 run」步骤。

## 未通过 / 存疑项

**1. REQ-005 AC-1 / AC-2 —— run-store 布局下 `/valuation`、`/portfolio-strategy` 仍按扁平路径取定性产物（阻断项）。**
现象：这两条命令及其 coordinator 把公司目录传给 `resolve_qualitative`，而 `latest.json` 未被解析；run-store-only 布局下返回 `source=unavailable`（复现见 AC-1）。影响：用户按 REQ-005 目标使用 `/update-analysis` 建立 run-store 后，估值/组合命令读不到最新 run 的结论，要么降级为「待分析」，要么诱导回退到 `/business-analysis`（会丢弃增量 run）——正是需求背景中「下游读不到产物」的那一类。根因是 `strategies/value/coordinator.md:45` 已知 resolver 不跟随 `latest.json`，但只把修复施加于 `/value-analysis`。建议：按 `/value-analysis` 的同一模式（先 `runs.py resolve --latest` 取 `{run_dir}` 再传给 resolver）同步修改上列 6 处，并把契约测试从 value-analysis 扩展到 valuation / portfolio-strategy 及其 coordinator。

**2. REQ-005 AC-5 —— 缺少双布局端到端测试（阻断项）。**
需求把「mock baseline run → 注入新期次 → 增量 run → 校验旧命令仍可解析」明确列为**本需求的验收证据**，但仓库中不存在该测试。当前证据只能证明 run-store 的单点可用（adopt → resolve），无法证明「旧命令在两种布局下仍可解析」。建议补一条 `e2e` 用例：构造 baseline run → 注入 `2026H1` PDF/期次 → `runs.py new --kind report-update` → `finish` → 分别以 legacy 与 run-store 布局调用 resolver/命令入口并断言 `source=structured`。

**3. REQ-003 AC-2 —— `run.json` 的期次字段与需求/设计的字面表述不一致（存疑，不单独判失败）。**
需求 AC-2 与 `docs/PERIODIC_UPDATE_PLAN.md:87` 都写作 `run.json` 记录 `kind / periods / framework / supersedes`，实际 `run.json` 记录的是 `primary_period`（**单数**），覆盖期次列表只出现在 `history.jsonl`/`record.json`（`runs.py:309-320, 441-443, 451-470, 509-522`）。就 AC 意图（「上次结论、期次、框架版本可查」）而言成立，且当前无消费者读取 `run.json["periods"]`，故仅记为字面偏差。若后续有工具按设计文档消费 `run.json:periods`，需先补齐该字段或改文档。

**4. REQ-004 AC-5 —— 变化报告由 Agent 生成，无确定性产物测试（存疑，不单独判失败）。**
`change_report_{period}.md/.json` 与 `investment.change_report` sidecar 只由提示词契约（`change_report.md:31, 56-77`）+ 上下文构建器固定，测试断言的是提示词字符串（`tests/test_period_delta_module.py:122-126`），不校验 Agent 实际落盘。这是 Agent 交付物的固有边界，记为存疑而非失败。

**5. 门禁脚本按 AC 编号全局匹配，会把同号 AC 串台（工具局限，提示读者勿误读）。**
`scripts/acceptance_gate.py:52-58` 的 `checked`/`unchecked` 正则在**整份报告**上按 `AC-<n>` 匹配、不区分所属需求。因此本报告里 REQ-005 的 `- [ ] **AC-1** / **AC-2** / **AC-5**` 会让门禁把 REQ-003、REQ-004 的**同号** AC 也一并报成「未打勾」。逐条结论以本报告 `## 逐条验收` 的各需求小节为准：REQ-003 AC-1/AC-2/AC-5 与 REQ-004 AC-1/AC-2/AC-5 均为**成立**（`- [x]`）。自检命令：`.venv/bin/python -c "import sys;sys.path.insert(0,'scripts');import acceptance_gate as g;from pathlib import Path;print(g.evaluate('REQ-003\nREQ-004\nREQ-005\n',[Path('docs/verification/2026-09-20-REQ-003-004-005.md')]))"`。

**6. 环境噪声（不构成 AC 失败）。**
`make verify` 会写 `.coverage`（gitignored），验收前后 `git status --porcelain` 均为空，未修改任何代码/文档；唯一新增文件为本报告。

## 结论

**不通过（有 2 项阻断，3 项存疑）。**

- **REQ-003**：AC-1…AC-8 **全部成立**。台账、指纹、快照不可变、adopt 接管、失败 run 不判 up_to_date、默认离线、manifest 加性字段均有用例与独立复现支撑。唯一保留意见是 `run.json` 记 `primary_period`（单数）而非设计文档写的 `periods` 列表（存疑项 3，字面偏差、不影响 AC 意图）。
- **REQ-004**：AC-1…AC-8 **全部成立**。period_delta 三方契约、`prior_analysis` 优先选入与可见缺失、`--prior-analysis` 注册与缺失降级、变化报告的降级计数、独立报告与 sidecar 契约、双目录命令逐字节一致、增量不自动改写买卖计划、全量离线无回归均有证据。保留意见是变化报告的落盘依赖 Agent（存疑项 4）。
- **REQ-005**：**3/5 成立，2 项阻断**。AC-3（架构文档）、AC-4（README/CHANGELOG）成立；**AC-1、AC-2 不成立**（run-store 布局下 `/valuation`、`/portfolio-strategy` 仍把公司目录交给不解析 `latest.json` 的 resolver，实测 `source=unavailable`）；**AC-5 不成立**（需求点名的双布局端到端测试不存在）。
- **放行条件**：修复存疑项 1（同步 6 处调用 + 扩展契约测试）与存疑项 2（补双布局 e2e）后，重跑 `make verify` 并由独立评审者对新报告复验 REQ-005 的 AC-1/AC-2/AC-5。REQ-003 / REQ-004 已可推进到 `verified`。
- 备注：本报告按指令以 `- [ ]` 标记不成立的 AC，`scripts/acceptance_gate.py` 会因此把该批次判为未通过——这是预期行为，不是报告格式错误。另注意门禁按 AC 编号全局匹配（存疑项 5），它还会把 REQ-003/REQ-004 的同号 AC 一起报成未打勾，属工具串台，逐条结论以正文为准。
