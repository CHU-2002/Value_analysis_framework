---
batch: 2026-09-27-REQ-006.2-final-qa
date: 2026-09-27
reviewer: independent-agent
independence: independent
requirements: REQ-006.2
---

# REQ-006.2 最终产品验收报告

## 验收方式

本次只按产品使用结果验收，检查真实 run 目录、模块结果、对账结果、报告和台账文件；未运行 pytest、`make verify` 或研发内部测试脚本，也未用源码断言代替产品观察。

## 复核更正

报告初稿误把 `output/600887_伊利/runs/20260925T053458757536Z/` 视为修复后的 run。
该目录的 `run.json` 明确记录框架版本 `0.2.0`、代码指纹和提交 `b23270b`，早于本次
REQ-006.2 修复提交，因此只能作为历史完整 run，不能作为修复后 AC-2.7/AC-2.8 的验收证据。

## 逐条结果

- [x] **AC-2.1**：真实市场数据包的 §9 已去重，未见字面 `nan`，并有分部合计与特别调整的口径说明；§12 营收同比增长率有实际数值。产物：`output/600887_伊利/runs/20260925T053458757536Z/inputs/data_pack_market.md`。
- [x] **AC-2.2**：质押数据量级与总股本、总市值/现价一致，质押比例可由质押股数与总股本复核。产物：同一市场数据包及最终报告中的质押指标。
- [x] **AC-2.3**：异常检测按 2026H1 与 2025H1 的可见期间输出；不可用的增量段落在 bundle/报告中标为缺失或未覆盖，没有把占位文字当成证据。产物：`contexts/*.json`、`qualitative_report.md`。
- [x] **AC-2.4**：PDF 产物中 MDA 正文在前置上下文之前；重大担保表的列和值可对应，包含逾期金额、逾期状态和汇总比例；最终报告能引用这些结果。
- [x] **AC-2.5**：6 个模块均有可读 bundle 和 `context_text`；证据引用能在各自 bundle 中找到。`period_delta` 保留了利润表关键行、上一版结论和 PDF 证据，最终报告明确披露证据缺口与低置信度。
- [x] **AC-2.6**：附注输入缺失和期次不一致均在 manifest 中有 warning/unavailable 记录；完整 run 的输入清单、期次和来源可追溯。
- [ ] **AC-2.7**：未通过本次最终复核。历史完整 run 虽有 `DATE-001` 和 `requires_llm_review`，但其框架指纹早于修复，不能证明修复后的跨 run 一致性、压缩引用和元数据行为。
- [ ] **AC-2.8**：未通过本次最终复核。修复后的隔离 run 只有 `prepare` 产物；尚未有同一修复版本下的 6 个模块、synthesis、change report、最终报告和 finish 台账闭环。

## 实跑记录

复核日期：2026-09-27。复核对象：`output/600887_伊利/runs/20260925T053458757536Z/`。

产品可见观察：

1. `run.json` 的框架提交为 `b23270b880e2517a811bdf3c86845bd228b77bef`，`primary_period=2026H1`，输入、6 个模块结果和报告均在同一 run 目录。
2. 6 个模块均有 `result.json` 与 `report.md`；`synthesis/reconciliation.json` 给出 `result_digest`、`as_of`、`DATE-001` 和 `overall_confidence=low`。
3. `change_report_2026H1.json` 给出 `report_period=2026H1`、`comparable_period=2025H1`、`business_trend=稳定`、`requires_full_rerun=false`，并逐条列出判断是否变化及证据 id。
4. `qualitative_report.md` 是可读的最终交付物，披露了模块状态、日期口径、缺失证据、引用和最终投资判断。

## 结论

REQ-006.2 本次不能收口为 `verified`。代码实现和确定性门禁已完成，但仍需在修复版本上完成一次真实产品闭环，再重新进行最终 QA。
