---
batch: 2026-09-27-REQ-006.2-acceptance
date: 2026-09-27
reviewer: independent-agent
independence: independent
requirements: REQ-006.2
base: 56ceb00
full-suite: "1619 passed, 3 skipped, 9 failed，覆盖率 78.53%"
---

# 独立验收报告：REQ-006.2

## 独立验收声明

本报告由未参与 REQ-006.2 实现的独立 agent 编写。评审从需求条目、当前代码/测试和指定真实 run 目录重新取证；未采用实现者的验收结论作为证据。此次复核针对 2026-09-26 真实重采集、prepare、六模块校验、reconcile、synthesis、resolve 和 finish 后的同一 run。除本报告外未修改源代码或需求台账。

## 全量测试

```bash
make verify
```

- 结果：`1619 passed, 3 skipped, 9 failed`；失败均发生在 `tests/test_webui_framework.py`，沙箱禁止 `127.0.0.1` socket bind（`PermissionError: [Errno 1] Operation not permitted`），不是 REQ-006.2 定向测试失败。
- 覆盖率：`78.53%`（门槛 74%）。
- 定向复核：`.venv/bin/python -m pytest -q tests/test_tushare_pack_sections.py tests/test_pdf_preprocessor.py tests/test_results_pipeline.py tests/test_change_report.py tests/test_runs_ledger.py`，`308 passed`。
- 基线：当前验收工作树 `56ceb00`（`codex/req0062-complete`）。

## 逐条验收

### REQ-006.2 实跑暴露的数据包与证据层缺陷修复

- [x] **AC-2.1**：修复后真实数据包为 24,504 字节；§9 的「合计特别调整」缺失值输出为 `—`，无字面 `nan`；§12「营收同比增长率 (%)」逐列有值（如 `4.13、5.47、3.49`），并保留产品合计口径说明。命令：`rg -n "主营业务构成|营收同比增长率|nan|合计特别调整" output/600887_伊利/runs/20260926T164155496211Z/inputs/data_pack_market.md`。
- [x] **AC-2.2**：修复后 §16 输出总股本 `632,536.07` 万股、无限售质押 `39,775.10` 万股、质押比例 `6.29%`；用 §1 总市值 ÷ 当前价反推约 `632,536` 万股，量级一致。命令：`rg -n "总股本|质押|632,536|39,775" output/600887_伊利/runs/20260926T164155496211Z/inputs/data_pack_market.md`。
- [x] **AC-2.3**：§13.1 的异常窗口包含 `2025H1→2026H1`；增量专属占位段在 context 的 `evidence_coverage` 中标为 `unavailable`，没有作为证据槽位。证据：`contexts/environment.json`、`modules/environment/result.json`、`qualitative_report.md`。
- [x] **AC-2.4**：`inputs/pdf_sections_2026H1.json` 的 MDA 正文与前置上下文有标记；MATTERS 担保明细和汇总块均可在 `contexts/governance.json` / `contexts/period_delta.json` 找到，含 `4,811.72`、`907,629.75`、`16.90%` 等可判定值。未观察到双栏串行文本作为本 run 的唯一代表证据。
- [x] **AC-2.5**：六个 context bundle 均存在，机器字段 `selection.quotes_not_in_context` 为空；`period_delta` 包含必选利润表行、必选节、prior_analysis 和 PDF/MATTERS 入口，并将占位输入标为 unavailable/omitted。命令：`.venv/bin/python` 读取并检查 `contexts/*.json` 的 `selection` 字段。
- [x] **AC-2.6**：附注源已接入 `inputs/data_pack_report.md`；`run_manifest.json` 明确记录 `pdf_footnotes` 为 `2025FY`、与 `primary_period=2026H1` 的 `period mismatch` warning，而非静默缺失。证据：`run_manifest.json` 的 `warnings`、`period_mismatches`。
- [x] **AC-2.7**：通过。`run.json` 与 `run_manifest.json` 的 run id、framework version、git commit `56ceb00`、`dirty=false`、prompt/code fingerprint 完全一致；六个模块均为同一 run id，`as_of` 全为 `2026-06-30`，不晚于生成时间；输入指纹采用修复后的内容摘要。`reconciliation.json` 可见冲突/降级字段和 `change_report` 的 prior run 引用，未发现悬空引用。命令：`.venv/bin/python` 比较上述 JSON 字段并检查 `contexts/*.json`。
- [x] **AC-2.8**（实跑）：通过。真实 token 重采集后，指定 run 完成 prepare、六模块 validate、reconcile、synthesis、resolve(source=structured) 和 `runs.finish --force`；manifest 与 `history.jsonl` 均为 `status=complete`，report、change report、synthesis、qualitative_input 均指向该 run id，并可在真实产物上复核 AC-2.1~2.7。

## 实跑记录

复核日期：2026-09-27。环境：本地仓库 `.venv`；真实 run 已由 owner 在 `.env` 注入 `TUSHARE_TOKEN` 后完成重采集和完整产品流水线，本验收者只读复核其产物并运行本地测试，未读取或输出 token。

```bash
find output/600887_伊利/runs/20260926T164155496211Z -type f | sort
rg -n "20260925T091012981574Z|20260926T164155496211Z|git_commit|dirty|code_fingerprint" output/600887_伊利/runs/20260926T164155496211Z
make verify
.venv/bin/python -m pytest -q tests/test_tushare_pack_sections.py tests/test_pdf_preprocessor.py tests/test_results_pipeline.py tests/test_change_report.py tests/test_runs_ledger.py
```

观察：指定目录包含输入、证据索引、六个模块结果/报告、synthesis、change report 和 qualitative report；六个 context 的 `quotes_not_in_context` 均为空，manifest 有附注期次 mismatch warning；manifest/history 完成状态和 artifact 路径闭环一致。`make verify` 的 9 个失败均为当前沙箱 socket 权限限制；REQ-006.2 定向测试 308 例全通过。`period_delta` 仍为 partial，四项数值参数按证据不足保留为 null，报告明确披露该降级，不构成静默绕过。

## 未通过 / 存疑项

- 附注源 `data_pack_report.md` 为 `2025FY`，与 `primary_period=2026H1` 的 mismatch warning 被显式保留；报告禁止将其当同期证据。
- `period_delta` 为 `partial`，reconciliation 标记 warning，四项无法由本 run 复算的数值保持 null；这是可见降级，不是未登记问题。
- 全量门禁的 WebUI 失败受沙箱 socket 权限影响；定向 REQ-006.2 测试未受影响。

## 结论

REQ-006.2 **支持推进到 `verified`**。AC-2.1 至 AC-2.8 均通过；附注期次 mismatch 与 `period_delta` partial 均在真实产物、reconciliation 和最终报告中显式披露，未发现未登记的实跑缺口。
