---
batch: 2026-09-20-REQ-006
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-006
base: fd9c7e5
full-suite: "1442 passed, 3 skipped, 覆盖率 76.36%"
---

# 独立验收报告：REQ-006 需求-测试-开发流程与三道门

## 独立验收声明

评审者为无上下文的独立 agent：未参与 REQ-006 的实现，未查看开发过程，也未复述实现者的说法。
本报告的所有结论只来自我自己读到的文件、我自己运行的命令与其输出。

- 被验收对象（第四轮 / 收尾复验）：分支 `chore/req-test-dev-flow`，`git rev-parse --short HEAD` = `fd9c7e5`（`git status --porcelain` 为空，工作树干净）。
- 历史基线：`397687f`（首版，AC-6 不成立）、`e080655`（第二轮，AC-6 仍不成立）、`bbbf330`（第三轮，全 AC 通过）；本轮在 `bbbf330..fd9c7e5` 的 13 个改动文件上复核，重点是新批次语义。`main` = `e453255`。
- 复核范围：`scripts/acceptance_gate.py`、`scripts/pr_body_guard.py`、`scripts/regression_gate.py`、`scripts/test_scope.py`、`tests/test_release_gates.py`、`tests/test_test_scope.py`、`tests/test_requirement_traceability.py`、`Makefile`、`docs/requirements/REQ-006-…md`、`docs/requirements/README.md`、`docs/DEVELOPMENT.md`、`docs/TESTING.md`、`CONTRIBUTING.md`、`README.md`、`CHANGELOG.md`、`docs/TEST_SCOPE.md`、`docs/regression/`、`docs/verification/`、`.github/workflows/*.yml`，以及 CI 失败时使用的真实 PR 正文 `/tmp/pr_body_req006.md`。
- 本地无 `python` 命令，脚本一律用 `.venv/bin/python`（Python 3.12）运行；CI 侧用 `python`。

## 全量测试

命令（仓库根目录，全 mock，未设置 `TUSHARE_TOKEN`）：

```bash
make verify
```

结果摘要（`fd9c7e5` 实测）：

```
1442 passed, 3 skipped in 54.39s
Required test coverage of 74% reached. Total coverage: 76.36%
6 files skipped due to complete coverage.   （TOTAL 11112 语句 / 2627 未覆盖）
.venv/bin/python -m pytest -q tests/test_requirement_traceability.py   ->  8 passed in 0.03s
.venv/bin/python scripts/test_scope.py --check  ->  测试 scope 检查通过：32 支文件、1445 个用例，均在预算内。
main 批量回归门禁通过（自 e453255 起累积的特性合入未达阈值 3）。
本地可复现的门禁全部通过；PR 上下文门禁（pr-title / pr-body / acceptance-gate）由 CI 执行。
real 0m56.704s   （exit code 0）
```

- passed / skipped / 覆盖率：**1442 passed / 3 skipped / 76.36%**（门禁 ≥ 74%）。
- 3 个 skipped 全部是 `tests/test_integration.py:30/76/110` 的 live API 用例（`TUSHARE_TOKEN not set`），符合「CI 不依赖网络与 Token」的约束。
- `make verify` 展开：compileall → `git diff --check`（工作区）→ `git diff --check $(git merge-base origin/main HEAD)..HEAD` → `cov` → `trace` → `scope-check` → `regression-check` → 汇总；回归门禁结果行确实出现在输出中。
- 数字一致性（本轮已重新回填）：`docs/DEVELOPMENT.md:153/154/160`、`docs/TESTING.md:77`、`CONTRIBUTING.md:191`、`README.md:248`、`CHANGELOG.md:13/26`、`docs/TEST_SCOPE.md:18`、`docs/regression/2026-09-20-baseline.md`、`docs/regression/TEMPLATE.md`、`docs/requirements/ledger.md:52` 均为 1442 passed / 3 skipped / 76.36% / 32 支文件 / 1445 用例，与 `make verify`、`pytest --collect-only` 实测吻合；旧数字（1440/1443/76.35%）已无残留（`grep` 为空）。各验收报告保留其自身 base sha 时间点的数字（本报告 base 更新为 `fd9c7e5`、数字刷新为本轮实测）。

## 逐条验收

- [x] **AC-1**：成立。分支模型为「一个特性一条特性分支、子 PR 合入特性分支、特性分支直接合入 main、不维护长期集成分支」（`CONTRIBUTING.md:33-37`、`docs/DEVELOPMENT.md:23-37`、`:31` 明文「没有 `develop` 之类」）；`main` 保护由 `setup-branch-protection.yml:80-96` 应用（必需状态检查 `ci-success`、至少 1 个 review、CODEOWNERS 审阅、线性历史、禁 force push/删除），`branch` 输入说明为「如 main；特性分支要保护也可填分支名」（`:7`）；`git branch -a` 实测只有 `main` / `feat/buy-sell-plan` / `fix/qualitative-context-evidence-budget` / 本 `chore/…`，无 `develop`；全仓 grep「develop」只剩否定式表述（`docs/DEVELOPMENT.md:31`、`docs/requirements/REQ-006-…md:37`）、新 AC-6 的禁止性条款（`:52`）与 `tests/test_release_gates.py:57/63/68` 的测试夹具；CI 只在整支合 `main` 时加跑验收与回归门禁（`ci.yml:139`、`:163`）。限制（不变）：GitHub 分支保护无法按来源分支约束 PR，只能由评审与门禁流程保证。
- [x] **AC-2**：成立。`ci.yml:8` 的 `pull_request:` 无 `branches` 过滤，`test` 作业（`ci.yml:18-48`）无 `if`，任何目标分支的 PR 都跑全量 pytest + 覆盖率门禁 `--cov-fail-under=74`（矩阵 3.10/3.12）；`pr-body` 作业（`ci.yml:116-135`）同样无 `if`。实测：自测栏填实 + `--base feat/x` → `exit=0`；自测栏为空/只有 `<!-- 待填 -->` → `exit=1`；`--base main` 且验收报告栏「不适用」→ `exit=1`；链接 `docs/verification/…md` → `exit=0`。判定确定性，PR 模板三栏齐备（`.github/PULL_REQUEST_TEMPLATE.md:12-39`）。
- [x] **AC-3**：成立（含本轮的新批次语义）。`acceptance-gate` 仅 `base_ref == 'main'` 时运行（`ci.yml:139`）。**批次定义**：`batch_requirements(body)`（`acceptance_gate.py:127-138`）只取「## 需求编号」小节（复用 `pr_body_guard.section`，会剥离 HTML 注释），该小节为空/缺失时才回退全篇扫描；我用真实 PR 正文 `/tmp/pr_body_req006.md` 复核——正文共提到 5 个需求（REQ-001/003/004/005/006），`batch_requirements` = `['REQ-003','REQ-004','REQ-006']`，`evaluate(正文, [REQ-003-004-005 报告, REQ-006 报告])` = `[]`，即「REQ-005 不在本批」的说明不再把 REQ-005 卷进来，CI 那次误判已消除。**其余判据逐项复核**：报告必须新增/修改且非模板（`:81-87`）、front matter 四字段（`:25`、`:119-122`）、`independence` ∈ {independent,yes,true}（`:123-128`）、覆盖本批全部 REQ（`:131-134`）、记录形如「1442 passed」（`:24`、`:136-137`）、每条 AC 打勾（`:52-58`、`:139-144`）、多份报告取并集（实测两份各覆盖一半 AC → `[]`）、docs-only 放行（`docs/…`+`CHANGELOG.md` → `[]`；doc+`scripts/runs.py` → 拦下）、按需求分节隔离（实测批次仅 REQ-003 时，报告里 REQ-005 的 `- [ ]` 不连累 REQ-003，反向亦然）、缺小节回退全篇（实测无小节但正文有 REQ-005 → 批次 `['REQ-005']`）。新增 2 项单测（`tests/test_release_gates.py:257-282`）单独跑 `2 passed`。残留风险见存疑项 2–5（均为非阻断）。
- [x] **AC-4**：成立。阈值 `THRESHOLD = 3`（`regression_gate.py:30`），只计 `^feat(\(|:|!)` 的 squash 提交或 `^Merge pull request`（`:28-29`、`:66-72`），自上次记录的 `covered-until` 起算（`:75-84`、`:190-200`），达阈值且无更新记录即阻断（`:87-104`）；`validate_record`（`:107-122`）校验 front matter `date`/`covered-until`/`reviewer`/`full-suite` 有效（`TBD` 无效）、正文含「## 全量测试」+ passed 数字、含「## 逐条验收」小节。实测：3 条 feat → 阻断，2 条 → `[]`；`--check --main-ref main`/`origin/main` → `exit=0`；`--new` 出含 `covered-until` 与「## 逐条验收」的草稿；`validate_record` 对空壳记录/缺 passed/`reviewer: TBD` 分别报错，对完整记录 → `[]`；`make regression-check` 已挂进 `verify` 并 `exit=0`。残留软点见存疑项 6。
- [x] **AC-5**：成立。`docs/TEST_SCOPE.md` 有登记表与预算（上限 40 文件 / 1600 用例，当前 32 / 1445）；预算常量在 `scripts/test_scope.py:31-32`；`--check`（`:169-195`）校验登记表与真实 `tests/test_*.py` 集合一致（未登记 → missing、已删除仍登记 → stale）并检查预算。实测：`--check` → `exit=0`「32 支文件、1445 个用例，均在预算内」；`--report` → 「测试文件 32/40，用例 1445/1600」＋分层＋逐文件归属；用 `registered_files()` 复现：当前 `actual ^ registered == []`，并入虚构新文件 → missing 命中，并入虚构已删文件 → stale 命中。`tests/test_test_scope.py` 覆盖采集/归属/解析/渲染/预算常量。残留软点见存疑项 7。
- [x] **AC-6**：成立。新条款（`docs/requirements/REQ-006-…md:49-52`）要求本地 `make verify` 覆盖 CI 中对仓库内容的全部检查、PR 元数据三项由 CI 执行并由 `make gates` 给本地预演、文档描述与数字一致且不得出现废弃分支说法。逐项核验：`make -n verify` = `lint cov trace scope-check regression-check`，覆盖 CI 的 `test`→`cov`（同一命令）、`lint`→`lint`（compileall + 工作区 diff + `git diff --check $(git merge-base origin/main HEAD)..HEAD`，与 `ci.yml:64-66` 同工具同语义，实测 BASE=`e453255…` 非空即执行）、`test_scope`→`scope-check`、`regression-gate`→`regression-check`（`Makefile:36-40`，且 `make verify` 输出含回归门禁结果行）；`pr-title`/`pr-body`/`acceptance-gate` 属 PR 元数据，`make gates`（`Makefile:30-34`，验收示例含 `--base origin/main --head HEAD`）给出预演；`grep -rn 等价 README.md docs/TESTING.md docs/DEVELOPMENT.md CONTRIBUTING.md Makefile` 为空；数字已在 9 处文档对齐实测（见上）；`develop` 只剩否定式/禁止性表述与测试夹具。第三轮已批准的 AC-6 需求变更记录仍在（`:54-62`，含原条款原文/原因/新条款/owner 批准/独立评审指向），本轮数字回填只改度量值、未动条款。

## 未通过 / 存疑项

1. **无阻断项。** AC-1…AC-6 全部成立，`make verify` 全绿（1442 passed / 3 skipped / 76.36%，exit 0），真实 PR 正文在门②下通过（`evaluate` = `[]`）。
2. **批次语义改动的合规性判断：是门禁缺陷修复，不是改小标准，也无需走需求变更流程。** 理由：(a) 没有任何 AC 文本被改动，AC-3 仍是「报告覆盖本批全部 `REQ-NNN`、逐条 `AC-n` 打勾、缺失或不合格时 CI 失败」；(b) 「本批」的权威来源本来就是 PR 模板的「## 需求编号」必填栏（`.github/PULL_REQUEST_TEMPLATE.md:12-16`，`pr_body_guard` 会校验该栏），旧实现按全篇匹配是把说明性提及误当批次，属实现与模板约定不符的缺陷；(c) 改动方向是消除误判，且在小节缺失时仍回退全篇扫描，覆盖没有变松；(d) 我复核了「真实 CI 失败」的复现路径：正文提到 5 个 REQ、批次声明 3 个，新语义下 `evaluate` = `[]`，与门禁应得的结论一致。结论：合规。
3. **安全性核查（被故意绕过）——结论：没有引入新的绕过口子，但有两个既有/边角软点。** 先说被问到的正向性质，均已复现：(i) 刻意省略「## 需求编号」小节**不能**逃验收——`batch_requirements` 会回退全篇扫描（实测：无小节 + 正文有 REQ-005 → 批次 `['REQ-005']`，仍要求其 AC）；(ii) 省略小节**且**正文任何位置（注释外）都不出现 `REQ-NNN` 时，`pr_body_guard` 与 `acceptance_gate` 双双拦下（实测 `pr_body_guard` 报「「## 需求编号」为空」，`acceptance_gate` 报「必须写明本批包含的 REQ-NNN」）。**但你的表述需要修正**：「`pr_body_guard` 在 base=main 时会强制该栏非空」并不准确——`pr_body_guard.py:60-65` 的判据是「小节非空 **或** 正文任意位置出现 `REQ-NNN`」（与 base 无关）：我实测一份**没有**「## 需求编号」小节、但正文提到 REQ-005 且自测栏填实的正文，在 `--base main` 与 `--base feat/x` 下 `evaluate` 都返回 `[]` 通过。反绕过之所以成立，靠的是「缺小节 → 回退全篇」这条更严的路径，而不是「强制该栏非空」；建议把注释/文档里的因果写准，或在 `pr_body_guard` 里把该栏改成无条件必填（后者会改变门①行为，需另行评估）。
4. **存疑（非阻断，新代码的边角不一致）：回退路径把 HTML 注释里的 `REQ-NNN` 也算进批次。** `batch_requirements` 用 `scope = declared if declared else body`（`acceptance_gate.py:136-138`），小节路径经 `section()` 剥离注释，回退路径却直接扫**原始正文**。复现：正文为 `## 需求编号\n\n<!-- 模板残留，提到 REQ-005 -->\n\n## 变更概述\n\n本批交付 REQ-006。\n`，则 `batch_requirements` = `['REQ-005','REQ-006']`，而对 `strip_comments(body)` 扫描只得 `['REQ-006']`——即 1 个残留注释会**多**要求一个需求的 AC，可能复现同类 CI 误判（方向更严，不是绕过）。一行修复：`scope = declared if declared else strip_comments(body)`（`strip_comments` 已从 `pr_body_guard` 可取）。建议连同单测一起补。
5. **存疑（非阻断，既有设计信任假设）：不存在的需求编号可被写进批次并通过门禁。** `batch_requirements` 只做 `REQ-\d{3}` 形式匹配，不校验该编号是否已登记；`requirement_ac_ids("REQ-999")` 返回 `[]`（无文件即无 AC）。实测：正文仅写「## 需求编号\n\nREQ-999」+ 一份声明 REQ-999 的报告（含 passed 数字），`evaluate` = `[]`——没有任何 AC 需要打勾。此弱点在旧语义下同样存在（旧语义按全篇匹配，只要不写别的编号即可），**非本次改动引入**，但批次改为「声明驱动」后，声明的可信度更关键。建议：`batch_requirements`/`evaluate` 增加「批次编号必须存在于 `docs/requirements/REQ-NNN-*.md`」的校验（`tests/test_requirement_traceability.py` 已有台账↔条目一致性可复用）。
6. **存疑（非阻断）：回归记录内容校验只到「小节存在」这一层**。`validate_record`（`regression_gate.py:107-122`）要求出现「## 逐条验收」，但不要求其中真有 `- [x] **AC-` 留痕；实测仅写「## 逐条验收」+「见 docs/verification/…」即可通过。AC-4 的可判定核心成立，故判成立。
7. **存疑（非阻断）：`test_scope.py --check` 的失败路径仍无直接单测**（`tests/test_test_scope.py:4-7` 明确不测 CLI `main`，避免 pytest 递归）；本轮仍以 `registered_files()` 内存模拟验证 missing/stale。受本次「只改一个报告文件」的约束，未在仓内实造未登记文件。
8. **观察（非阻断）：`docs/DEVELOPMENT.md:104-105` 把 `make gates` 写了两遍**（一行说明 PR 上下文预演、一行仍为「查看三道门在本地怎么自检」），两行描述都正确，属编辑残留。
9. **观察（非阻断）：`docs/verification/TEMPLATE.md:8` 的示例数字仍是 `1389 passed … 76.77%`**，而 `docs/regression/TEMPLATE.md` 已刷新为 1442/76.36%。模板示例值本就可用占位，但两者风格不一致，可一并统一。
10. **观察（非阻断）：`tests/test_release_gates.py:57/63/68` 仍以 `"develop"` 作为「非 main 目标分支」的测试夹具字符串**；功能正确，属可选清理项。
11. **观察（非阻断）：REQ-006 自己的提交不会被门③计数**。`git log --pretty=%s e453255..HEAD` 的标题均非 `feat` 前缀（`fix(process)`/`refactor(process)`/`chore(process)`/`chore(governance)`/`fix(process)`）；若以 squash 合入 `main`，按 `regression_gate.py:28-29` 的明文口径不计入「特性合入」。属 AC-4 计数口径的副作用，非缺陷。
12. **观察（非阻断）：PR 正文 `/tmp/pr_body_req006.md` 的「验证方式」段仍写 1440 passed / 76.35% / 1443 用例**（本轮前面几轮的实测值）。PR 正文不是仓库文件、也不受 AC-6 约束，但推送前建议同步为 1442 / 76.36% / 1445，以免评审者对着旧数字核对。

## 结论

**通过。**

- 全量测试：**1442 passed, 3 skipped, 覆盖率 76.36%**（`make verify`，exit 0，pytest 54.39s / 总 56.7s，全 mock；追溯门禁 8 passed；scope 32 支文件 / 1445 用例；回归门禁通过）。
- AC-1…AC-6 全部成立：分支模型与 main 保护；每个 PR 的 CI 全量 + 自测栏守卫；独立验收报告门禁（**批次取自「## 需求编号」+ 缺节回退 + 被排除需求不卷入 + 按需求分节隔离 + 并集 + docs-only 放行**）；每 3 个特性合入的批量回归门禁（含记录内容校验，已挂进 verify）；测试 scope 登记与预算；本地 `make verify` 覆盖 CI 对内容的全部检查、PR 元数据边界写明、文档措辞与数字一致、无废弃分支说法。
- 本轮新修项的独立核验：真实 PR 正文下 `batch_requirements` = `['REQ-003','REQ-004','REQ-006']`、`evaluate` = `[]`（CI 误判已消除）；新增 2 项单测单独跑 `2 passed`；`make verify` 输出含回归门禁结果行；9 处文档数字对齐 1442/76.36%/1445，旧数字无残留。
- 对批次语义改动的判断：**属门禁缺陷修复，不是改小标准**——AC 文本未变、「本批」的权威来源本就是 PR 模板的必填字段、改动消除误判且缺节时更严。两点建议在推送前顺手处理（都可选）：存疑项 4 的一行修复（回退路径改用 `strip_comments(body)`，避免注释里残留的 REQ 引发同类误判）与存疑项 5 的登记校验（拒绝不存在的 `REQ-NNN`）；其余存疑/观察项均不影响本次验收结论。
