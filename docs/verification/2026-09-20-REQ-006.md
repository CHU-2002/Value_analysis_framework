---
batch: 2026-09-20-REQ-006
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-006
base: a0d5ef6
full-suite: "1444 passed, 3 skipped, 覆盖率 76.37%"
---

# 独立验收报告：REQ-006 需求-测试-开发流程与三道门

## 独立验收声明

评审者为无上下文的独立 agent：未参与 REQ-006 的实现，未查看开发过程，也未复述实现者的说法。
本报告的所有结论只来自我自己读到的文件、我自己运行的命令与其输出。

- 被验收对象（第五轮 / 收尾复验）：分支 `chore/req-test-dev-flow`，`git rev-parse --short HEAD` = `a0d5ef6`（`git status --porcelain` 为空，工作树干净）。
- 历史基线：`397687f`（AC-6 不成立）→ `e080655`（AC-6 仍不成立）→ `bbbf330`（全 AC 通过）→ `fd9c7e5`（批次语义）→ 本轮 `a0d5ef6`；本轮在 `fd9c7e5..a0d5ef6` 的 14 个改动文件上复核。`main` = `e453255`。
- 复核范围：`scripts/acceptance_gate.py`、`scripts/pr_body_guard.py`、`scripts/regression_gate.py`、`scripts/test_scope.py`、`tests/test_release_gates.py`、`tests/test_test_scope.py`、`tests/test_requirement_traceability.py`、`Makefile`、`docs/requirements/REQ-006-…md`、`docs/requirements/README.md`、`docs/DEVELOPMENT.md`、`docs/TESTING.md`、`CONTRIBUTING.md`、`README.md`、`CHANGELOG.md`、`docs/TEST_SCOPE.md`、`docs/regression/`、`docs/verification/`、`.github/workflows/*.yml`，以及 CI 失败时使用的真实 PR 正文 `/tmp/pr_body_req006.md`。
- 本地无 `python` 命令，脚本一律用 `.venv/bin/python`（Python 3.12）运行；CI 侧用 `python`。

## 全量测试

命令（仓库根目录，全 mock，未设置 `TUSHARE_TOKEN`）：

```bash
make verify
```

结果摘要（`a0d5ef6` 实测）：

```
1444 passed, 3 skipped in 59.26s
Required test coverage of 74% reached. Total coverage: 76.37%
6 files skipped due to complete coverage.   （TOTAL 11119 语句 / 2627 未覆盖）
.venv/bin/python -m pytest -q tests/test_requirement_traceability.py   ->  8 passed in 0.03s
.venv/bin/python scripts/test_scope.py --check  ->  测试 scope 检查通过：32 支文件、1447 个用例，均在预算内。
main 批量回归门禁通过（自 e453255 起累积的特性合入未达阈值 3）。
本地可复现的门禁全部通过；PR 上下文门禁（pr-title / pr-body / acceptance-gate）由 CI 执行。
real 1m1.284s   （exit code 0）
```

- passed / skipped / 覆盖率：**1444 passed / 3 skipped / 76.37%**（门禁 ≥ 74%）。
- 3 个 skipped 全部是 `tests/test_integration.py:30/76/110` 的 live API 用例（`TUSHARE_TOKEN not set`），符合「CI 不依赖网络与 Token」的约束。
- `make verify` 展开：compileall → `git diff --check`（工作区）→ `git diff --check $(git merge-base origin/main HEAD)..HEAD` → `cov` → `trace` → `scope-check` → `regression-check`（输出含回归门禁结果行）。
- 数字一致性：`docs/DEVELOPMENT.md:153/154/160`、`docs/TESTING.md:77`、`CONTRIBUTING.md:191`、`README.md:248`、`CHANGELOG.md:13/26`、`docs/TEST_SCOPE.md:18`、`docs/regression/2026-09-20-baseline.md`、`docs/requirements/ledger.md:52` 均为 1444 passed / 3 skipped / 76.37% / 32 支文件 / 1447 用例，与 `make verify`、`pytest --collect-only` 实测吻合；上一轮数字（1442/1445/76.36）在主文档中已无残留（`grep` 为空）。`docs/verification/TEMPLATE.md:8`、`docs/regression/TEMPLATE.md:5-6` 以及 `regression_gate --new` 的草稿示例已改为 `<passed>` / `<x>%` 占位符，不再写死会过期的真实数字。本报告按惯例更新为自身 base sha（`a0d5ef6`）时间点的实测数字。

## 逐条验收

- [x] **AC-1**：成立。分支模型为「一个特性一条特性分支、子 PR 合入特性分支、特性分支直接合入 main、不维护长期集成分支」（`CONTRIBUTING.md:33-37`、`docs/DEVELOPMENT.md:23-37`、`:31`）；`main` 保护由 `setup-branch-protection.yml:80-96` 应用（必需状态检查 `ci-success`、≥1 review、CODEOWNERS、线性历史、禁 force push/删除），`branch` 输入说明为「如 main；特性分支要保护也可填分支名」（`:7`）；`git branch -a` 无 `develop`，全仓 grep「develop」只剩否定式/禁止性表述（`docs/DEVELOPMENT.md:31`、`docs/requirements/REQ-006-…md:37/52`）与 `tests/test_release_gates.py:57/63/68` 的测试夹具；CI 只在整支合 `main` 时加跑验收与回归门禁（`ci.yml:139`、`:163`）。限制（不变）：GitHub 分支保护无法按来源分支约束 PR。
- [x] **AC-2**：成立。`ci.yml:8` 的 `pull_request:` 无 `branches` 过滤，`test` 作业（`ci.yml:18-48`）无 `if`，任何目标分支的 PR 都跑全量 pytest + `--cov-fail-under=74`（3.10/3.12 矩阵）；`pr-body` 作业（`ci.yml:116-135`）同样无 `if`。实测：自测栏填实 + `--base feat/x` → `exit=0`；自测栏为空/只有 `<!-- 待填 -->` → `exit=1`；`--base main` 且验收报告栏「不适用」→ `exit=1`；链接 `docs/verification/…md` → `exit=0`。判定确定性，PR 模板三栏齐备（`.github/PULL_REQUEST_TEMPLATE.md:12-39`）。
- [x] **AC-3**：成立（含本轮两处加固）。`acceptance-gate` 仅 `base_ref == 'main'` 时运行（`ci.yml:139`）。**批次语义**：`batch_requirements`（`acceptance_gate.py:131-142`）只取「## 需求编号」小节（复用 `pr_body_guard.section`），为空/缺失时回退全篇但**先 `strip_comments`**（本轮修复 1）；回退复现：正文 `## 需求编号\n\n<!-- 模板残留，提到 REQ-005 -->\n\n## 变更概述\n\n本批交付 REQ-006。` → 批次 `['REQ-006']`（修复前为 `['REQ-005','REQ-006']`）。**未知编号拒绝**（本轮修复 2）：`evaluate` 在算出批次后先用 `requirement_entry` 校验编号已登记（`:160-167`），实测批次 `REQ-999` → 报「批次中的需求编号在 docs/requirements/ 下不存在：REQ-999」并提前返回，不再出现「无 AC 可查即通过」。**其余判据逐项复核**：真实 PR 正文（提到 5 个需求）批次 = `['REQ-003','REQ-004','REQ-006']`、`evaluate(正文, [REQ-003-004-005 报告, REQ-006 报告])` = `[]`（CI 那次误判已消除）；报告必须新增/修改且非模板（`:81-87`）；front matter 四字段（`:25`、`:119-122`）；`independence` 白名单（`:123-128`）；覆盖本批全部 REQ（`:131-134`）；必须记录形如「1444 passed」（`:24`、`:136-137`）；每条 AC 打勾（`:52-58`、`:139-144`）；多报告并集（实测 → `[]`）；docs-only 放行（→ `[]`）、docs+代码拦下（→ 报缺报告）；按需求分节隔离（批次仅 REQ-003 时，报告里 REQ-005 的 `- [ ]` 不连累 REQ-003）。新增 2 项单测（`tests/test_release_gates.py:284-306`）单独跑 `2 passed`。残留项见存疑项 1、4、5。
- [x] **AC-4**：成立。阈值 `THRESHOLD = 3`（`regression_gate.py:30`），只计 `^feat(\(|:|!)` 或 `^Merge pull request`（`:28-29`、`:66-72`），自上次记录 `covered-until` 起算（`:75-84`、`:190-200`），达阈值且无更新记录即阻断（`:87-104`）；`validate_record`（`:107-122`）校验 front matter 四字段有效（`TBD` 无效）、正文含「## 全量测试」+ passed 数字、含「## 逐条验收」小节。实测：3 条 feat → 阻断，2 条 → `[]`；`--check --main-ref main`/`origin/main` → `exit=0`；`--new` 出含 `covered-until` 与「## 逐条验收」的草稿（示例数字已改占位符）；`validate_record` 对空壳/缺 passed/`reviewer: TBD` 分别报错，对完整记录 → `[]`；`make regression-check` 已挂进 `verify` 并 `exit=0`。残留软点见存疑项 4。
- [x] **AC-5**：成立（机制层面；但登记表出现 1 行过期归属，见存疑项 1）。`docs/TEST_SCOPE.md` 有登记表与预算（上限 40 文件 / 1600 用例，当前 32 / 1447）；预算常量在 `scripts/test_scope.py:31-32`；`--check`（`:169-195`）校验登记表与真实 `tests/test_*.py` 集合一致（未登记 → missing、已删除仍登记 → stale）并检查预算。实测：`--check` → `exit=0`「32 支文件、1447 个用例，均在预算内」；`--report` → 「测试文件 32/40，用例 1447/1600」＋分层＋逐文件归属；用 `registered_files()` 复现 missing/stale 均命中；当前文件集合零漂移。`tests/test_test_scope.py` 覆盖采集/归属/解析/渲染/预算常量。**但**我用 `collect_scope()` 与登记表逐行比对归属列，发现 1 行不一致（`tests/test_release_gates.py` 登记为 `REQ-003, REQ-005, REQ-006, REQ-999`，实际为 `REQ-003, REQ-005, REQ-006`），且 `--check` 不校验归属列、因而放行——见存疑项 1。AC-5 明文要求的「新增/删除测试文件必须同步登记表、超预算必须先清理」两项强制均已复现成立，故本条判成立。
- [x] **AC-6**：成立。新条款（`docs/requirements/REQ-006-…md:49-52`）：本地 `make verify` 覆盖 CI 中对仓库内容的全部检查、PR 元数据三项由 CI 执行并由 `make gates` 预演、文档描述与数字一致且不得出现废弃分支说法。核验：`make -n verify` = `lint cov trace scope-check regression-check`，覆盖 `test`→`cov`（同一命令）、`lint`→`lint`（compileall + 工作区 diff + `merge-base..HEAD` 整段 diff，与 `ci.yml:64-66` 同语义，实测执行）、`test_scope`→`scope-check`、`regression-gate`→`regression-check`（`Makefile:36-40`，输出含回归门禁结果行）；`pr-title`/`pr-body`/`acceptance-gate` 属 PR 元数据，`make gates`（`Makefile:30-34`，验收示例含 `--base origin/main --head HEAD`）给预演；`grep -rn 等价 README.md docs/TESTING.md docs/DEVELOPMENT.md CONTRIBUTING.md Makefile` 为空；`docs/DEVELOPMENT.md:104` 的 `make gates` 重复行已合并；数字与模板占位符见「全量测试」；`develop` 仅剩否定式/禁止性表述与测试夹具。第三轮批准的 AC-6 变更记录仍在（`:54-62`），本轮只改度量值与模板示例，未动条款。

## 未通过 / 存疑项

1. **【建议推送前修】登记表有 1 行过期归属，且 `--check` 不校验归属列。** 现象：`docs/TEST_SCOPE.md:49` 把 `tests/test_release_gates.py` 的归属写成 `REQ-003, REQ-005, REQ-006, REQ-999`，而该文件实际只声明 `REQ-003, REQ-005, REQ-006`（`grep -n 999 tests/test_release_gates.py` 只有第 297 行运行期拼接 `"REQ-" + "999"`，源码中无 `REQ-999` 字面量）；`REQ-999` 并未在 `docs/requirements/` 登记，属悬空编号。成因可复原：登记表是在 `a0d5ef6` 编辑过程中、夹具还是字面量 `REQ-999` 时执行过一次 `--write`（因此用例数 26 / 1447 是最新的），随后夹具改为拼接写法但未重新生成登记表；`test_scope --check` 只比对「文件集合 + 用例数/文件数预算」，不比对归属列，所以一路放行（`make scope-check` → exit 0）。复现命令：`.venv/bin/python scripts/test_scope.py --report | grep release_gates`（得 `REQ-003, REQ-005, REQ-006`）与 `sed -n '49p' docs/TEST_SCOPE.md`（得含 `REQ-999`）对比。修复：`make scope-write`（我验证过 `render()` 会写成 `| \`tests/test_release_gates.py\` | REQ-003, REQ-005, REQ-006 | \`unit\` | — | 26 |`）。**这行错误会随本次合并进入 main**，请先跑这一条再推送。此缺陷不改变任何 AC 判定（AC-5 明文强制的两项仍成立），故 AC-5 维持 `[x]`。**建议后续加固**：让 `--check` 同时比对登记表的归属列与 `collect_scope()` 的结果（并在归属编号不存在于 `docs/requirements/` 时报错），这样归属列漂移与悬空编号都能在 CI 拦住。
2. **对第 4 点（`UNKNOWN_REQ = "REQ-" + "999"` 运行期拼接）的判断：不构成规避门禁，处理得当。** 理由：(a) 被绕开的不是生产门禁，而是治理测试 `tests/test_requirement_traceability.py` 对「测试文件里出现 `REQ-\d{3}` 字面量必须真实存在」的检查；该夹具**故意**需要一个不存在的编号来验证 `acceptance_gate` 会拒绝它，属真实的负路径构造，注释也写明了原因（`tests/test_release_gates.py:295-296`）。(b) 这条豁免是**自校验**的：若将来真的登记了 `REQ-999`，`test_acceptance_gate_rejects_unknown_requirement_ids` 会因为拿不到「不存在」报错而失败，夹具不会悄悄变成合法编号。(c) 从规避方向看也无利可图：把真实编号拼接写只会让该测试文件少一条归属引用，而 `test_delivered_requirements_are_covered_by_tests` 要求已交付需求必须有测试引用——隐藏引用只会让门禁更难过。(d) 生产侧我实测 `acceptance_gate.evaluate` 确实拒绝 `REQ-999`（见 AC-3），治理规则没有被放宽（diff 里 `tests/test_requirement_traceability.py` 本轮零改动）。若想把「例外」做得更可审计，可选做法是给治理测试加一个显式允许清单（如 `# traceability: synthetic-unknown-id` 标注或 `ALLOWED_SYNTHETIC_IDS` 常量），但这不是必需项。
3. **对「是否收紧 `pr_body_guard`」的建议：值得做，但请作为独立变更，不要塞进本次推送。** 现状（`pr_body_guard.py:60-65`）：判据是「『## 需求编号』小节有实质内容 **或** 正文任意位置出现 `REQ-NNN`」，与 `base` 无关；我实测无该小节、正文提到 REQ-005 且自测栏填实的正文在 `--base main` 下也通过。**安全性上不是必须收紧**——省略小节会让 `acceptance_gate` 走「回退全篇扫描」这条更严的路径（可能把附带提及的需求也卷进批次，只会多要求 AC、不会少要求），而且「未知编号拒绝」上线后回退路径也会校验登记；省略小节且注释外无任何 `REQ-NNN` 时，`pr_body_guard` 与 `acceptance_gate` 会双双失败（已复现）。**但建议收紧**，理由是让「本批」有一个确定、显式、无歧义的声明来源，并让门①就给出反馈：把判据改为「任何 PR 的『## 需求编号』小节必须非空」（即去掉 `or REQ_RE.search(body)` 兜底），保留 `acceptance_gate` 的全篇回退作为纵深防御。代价与注意点：这会失败掉正文缺该小节的 PR（模板始终带该标题，影响面小），属**加强**而非收窄、无需需求变更流程，但需同步 `tests/test_release_gates.py` 的相关断言、`docs/DEVELOPMENT.md`/`CONTRIBUTING.md`/PR 模板说明与 `CHANGELOG.md`。
4. **存疑（非阻断）：回归记录内容校验只到「小节存在」这一层**。`validate_record`（`regression_gate.py:107-122`）要求出现「## 逐条验收」，但不要求其中真有 `- [x] **AC-` 留痕；实测仅写「## 逐条验收」+「见 docs/verification/…」即可通过。AC-4 的可判定核心成立，故判成立。
5. **存疑（非阻断）：`test_scope.py --check` 的失败路径仍无直接单测**（`tests/test_test_scope.py:4-7` 明确不测 CLI `main`，避免 pytest 递归）；本轮仍以 `registered_files()`/`collect_scope()` 内存比对验证。这与存疑项 1 的盲区相关：建议把「文件集合 + 预算 + 归属列」的判定抽成纯函数再补单测。
6. **观察（非阻断）：`tests/test_release_gates.py:57/63/68` 仍以 `"develop"` 作为「非 main 目标分支」的测试夹具字符串**；功能正确，属可选清理项。
7. **观察（非阻断）：REQ-006 自己的提交不会被门③计数**。`git log --pretty=%s e453255..HEAD` 的标题均非 `feat` 前缀；若以 squash 合入 `main`，按 `regression_gate.py:28-29` 的明文口径不计入「特性合入」。属 AC-4 计数口径副作用，非缺陷。
8. **观察（非阻断）：PR 正文 `/tmp/pr_body_req006.md` 的「验证方式」段仍写 1440 passed / 76.35% / 1443 用例**（早前几轮的值）。PR 正文不是仓库文件、不受 AC-6 约束，但推送前建议同步为 1444 / 76.37% / 1447，以免评审者对着旧数字核对。

## 结论

**通过（AC-1…AC-6 全部成立）；附 1 项推送前应修的具体缺陷（存疑项 1，一条命令）。**

- 全量测试：**1444 passed, 3 skipped, 覆盖率 76.37%**（`make verify`，exit 0，pytest 59.26s / 总 1m01s，全 mock；追溯门禁 8 passed；scope 32 支文件 / 1447 用例；回归门禁通过）。
- AC-1…AC-6 全部成立：分支模型与 main 保护；每个 PR 的 CI 全量 + 自测栏守卫；独立验收报告门禁（批次取自「## 需求编号」+ 缺节剥注释回退 + 未知编号拒绝 + 分节隔离 + 并集 + docs-only 放行）；每 3 个特性合入的批量回归门禁（含记录内容校验，已挂进 verify）；测试 scope 登记与预算；本地 `make verify` 覆盖 CI 对内容的全部检查、PR 元数据边界写明、文档措辞与数字一致、模板示例改占位符、无废弃分支说法。
- 本轮新修项的独立核验：两处修复都按我上一轮给的方式落地并实测有效（注释剥离后批次 `['REQ-006']`；`REQ-999` 被拒并提前返回）；真实 PR 正文下 `evaluate` 仍为 `[]`；新增 2 项单测单独跑 `2 passed`；9 处文档数字对齐 1444/76.37%/1447。
- **推送前请先执行 `make scope-write`**：`docs/TEST_SCOPE.md:49` 的 `REQ-999` 归属行已过期（`--check` 因不校验归属列而放行），不修则会随本次合并进入 `main`。该缺陷不改变 AC 判定。
- 对第 4 点的判断：运行期拼接构造合成编号**不构成规避门禁**，处理得当且自校验；可选把豁免显式化。对收紧 `pr_body_guard` 的建议：**值得做，但作为独立变更**（安全性上非必需，收益是让「本批」声明显式且门①即可反馈）。
