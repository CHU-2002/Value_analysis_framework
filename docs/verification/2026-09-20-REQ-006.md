---
batch: 2026-09-20-REQ-006
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-006
base: 4f8fde0
full-suite: "1471 passed, 3 skipped, 覆盖率 76.80%"
---

# 独立验收报告：REQ-006 工程化开发流程（建立与持续维护）

## 独立验收声明

评审者为无上下文的独立 agent：未参与 REQ-006 的实现，未查看开发过程，也未复述实现者的说法。
本报告的所有结论只来自我自己读到的文件、我自己运行的命令与其输出，并以「试图证伪」为前提构造输入。

本轮背景与基线：REQ-006 原为「需求-测试-开发流程与三道门」，经五轮复验后 **AC-1…AC-6 成立**
（历史版本见本文件的 Git 历史，基线 `a0d5ef6`）。2026-09-20 使用者把「维护开发流程」明确为**一条常设需求**：
REQ-006 升格为「工程化开发流程（建立与持续维护）」，保留 AC-1…AC-6 并**新增 AC-7**，新增
「## 任务清单」T1–T4；原 REQ-007（门禁加固 8 项）与原 REQ-008（覆盖率补强）置 `superseded`，
分别并入 T2 与 T4；测试里的 `REQ-007` 覆盖标注改为 `REQ-006`。

- 被验收对象（本轮）：分支 `maint/req-007-gate-refinements`，`base` = `git rev-parse --short HEAD` = `4f8fde0`
  （`git status --porcelain` 为空，工作树干净）。
- 复核范围：`docs/requirements/REQ-006-…md`（AC-1…AC-7 与任务清单）、`REQ-007`/`REQ-008`（superseded）、
  `docs/requirements/ledger.md`、`README.md` §6、`tests/test_release_gates.py`、`tests/test_test_scope.py`、
  `tests/test_two_layout_e2e.py`、`tests/test_update_docs_contract.py`、`scripts/acceptance_gate.py`、
  `scripts/test_scope.py`、`docs/TEST_SCOPE.md`、`docs/verification/TEMPLATE.md`、`Makefile`、`.github/workflows/ci.yml`。
- 破坏性验证（AC-3/T3 的检查禁用、AC-5 的临时扫描）均已完全恢复：`scripts/acceptance_gate.py` 的 sha256
  `cc7f3d3f398aca73367bdd44f2e45cfed095a06fde46584e1a2f49db390771ec`、`scripts/test_scope.py` 的
  `280d89a2e3da2555cb816bd2c5c313e159cc765d0236c4b4c2c7b51042054d2c` 均与验证前一致，`git status` 为空。
  除本报告外未新建、未修改、未提交任何文件。
- 本地无 `python` 命令，脚本一律用 `.venv/bin/python`（Python 3.12）运行。

## 全量测试

```bash
make verify
```

结果摘要（`4f8fde0` 实测）：

```
1471 passed, 3 skipped in 55.97s
Required test coverage of 74% reached. Total coverage: 76.80%
.venv/bin/python -m pytest -q tests/test_requirement_traceability.py   ->  8 passed in 0.03s
.venv/bin/python scripts/test_scope.py --check  ->  测试 scope 检查通过：33 支文件、1474 个用例，均在预算内。
main 批量回归门禁通过（自 e453255 起累积的特性合入未达阈值 3）。
本地可复现的门禁全部通过；PR 上下文门禁（pr-title / pr-body / acceptance-gate）由 CI 执行。
（exit code 0）
```

- passed / skipped / 覆盖率：**1471 passed / 3 skipped / 76.80%**（门禁 ≥ 74%）。
- 3 个 skipped 全部是 `tests/test_integration.py:30/76/110` 的 live API 用例（`TUSHARE_TOKEN not set`），符合「CI 不依赖网络与 Token」的约束。
- `make -n verify` 展开 = `lint`（compileall + 工作区 `git diff --check` + `merge-base..HEAD` 整段 diff）→ `cov` → `trace` → `scope-check` → `regression-check`，与 AC-6 的描述一致。
- 文档基线数字一致性（AC-6）：`README.md:248`、`docs/TESTING.md:82`、`docs/DEVELOPMENT.md:153`、
  `CONTRIBUTING.md:191`、`CHANGELOG.md:13` 均为覆盖率基线 76.8%，与实测 76.80% 吻合；三份 prose 文档
  已不再写死测试数量与耗时（见 AC-7 的历史加固 T2）。

## 逐条验收

- [x] **AC-1**：成立。分支模型为「一个特性一条特性分支，子 PR 合入特性分支，特性分支直接合入 `main`，
  不维护长期集成分支」（`docs/DEVELOPMENT.md:31` 等）。实测：`git branch -a` 本地与远端均**无 `develop`**；
  全仓 grep「develop」只剩否定式/历史性表述（`docs/DEVELOPMENT.md:31` 的「没有 `develop` 之类」、
  `REQ-006:44/51/68` 的「夹具去 `develop`」/「不得出现已废弃分支」、`REQ-007` 的 superseded 历史文本），
  无任何有效分支或有效判据使用它。`main` 保护由 `.github/workflows/setup-branch-protection.yml` 应用
  （`required_status_checks` 上下文 `ci-success` 见 `:81-83`、CODEOWNERS、线性历史、禁 force push/删除）。
- [x] **AC-2**：成立。`.github/workflows/ci.yml:8` 的 `pull_request:` 无 `branches` 过滤，`test` 作业（`:18` 起）
  无 `if`，即任何目标分支的 PR 都跑全量 pytest + `--cov-fail-under=74`；`pr-body` 作业（`:118`）
  也以 `github.event_name == 'pull_request'` 为条件、不限目标分支。手工自测栏由
  `scripts/pr_body_guard.py` 强制：实测「研发自测（手工）」只有 `<!-- 待填 -->` → 退出码 1；
  填实 → 退出码 0。
- [x] **AC-3**：成立。`acceptance-gate` 仅在 `github.base_ref == 'main'` 时运行（`ci.yml:139`）。
  实测缺少验收报告时 `acceptance_gate.evaluate('## 需求编号\n\nREQ-006\n', [])` 报
  「本 PR 没有新增或修改 docs/verification/ 下的验收报告…」；给出一份覆盖 REQ-006 全部 AC 的独立报告后返回空问题列表。
  本轮更新后的本报告本身即通过该门禁的自检（以本报告调用 `acceptance_gate.evaluate` → 空问题列表）。
- [x] **AC-4**：成立。`regression_gate.THRESHOLD == 3`；实测 3 条 `feat`/merge 提交 → 阻断，2 条 → 放行；
  记录内容校验会拒绝空的「## 逐条验收」小节（T2 加固，`validate_record`）；`make verify` 中的
  `regression-check` 实测通过（自 `e453255` 起未达阈值）。
- [x] **AC-5**：成立。`docs/TEST_SCOPE.md` 有登记表与预算（上限 40 文件 / 1600 用例，当前 33 / 1474），
  `scripts/test_scope.py --check` 强制文件集合一致、预算与归属编号已登记，并在 T3 后**额外比对归属列内容**。
  实测 `--check` → 退出码 0「33 支文件、1474 个用例，均在预算内」。
- [x] **AC-6**：成立。`make -n verify` 覆盖 CI 中「对仓库内容」的全部检查（lint/全量测试+覆盖率/追溯/scope/回归），
  PR 元数据三项由 CI 执行、`make gates` 预演；文档描述与数字一致（覆盖率基线各处 76.8% = 实测 76.80%，
  见「全量测试」）；不得出现废弃分支的说法（见 AC-1）。第三轮批准的 AC-6 条款变更记录仍在条目内。
- [x] **AC-7**：成立（新条款；其第一批适用对象即 T3 的两处修正，见下一节）。
  **记录形式**：`docs/requirements/REQ-006-…md:36-46` 有「## 任务清单」T1–T4，每项都有**状态**与**证据**：
  T1（建立三道门）完成 / PR #25 + `2026-09-20-REQ-006.md`；T2（门禁加固 8 项）完成 / PR #29 +
  `2026-09-20-REQ-007.md`；T3（本次两处修正）完成 / 指向本需求「维护与合并记录」（缺陷见存疑项 1）；
  T4（覆盖率补强）未开始 / `REQ-008` 文件作规格。四个证据链接目标文件均存在。
  **只准更准或更严**：T3 的两处修改我逐条判断并证伪（详见下节）——`acceptance_gate` 的改动是**更准**
  （剔除举例文本、消除误判，真实的未打勾行仍被抓出），`test_scope --check` 的改动是**更严**
  （新增一条失败路径），二者都没有放宽任何既有判定。
  **独立验收确认**：本报告即为 T3 两处修正的独立验收（第三节给出证伪证据与结论）。
  存疑项 1 指出 T3 证据指针不完整，属记录缺陷、不改变上述实质判断。

## 本次维护验证（T3）

T3 = 「验收报告容忍举例（扫描前剔除引用块/代码块/行内代码）；`test_scope --check` 比对归属列**内容**」。
两处修改都落在 `c2d689e`，本轮在 `4f8fde0` 上复核。

### 修正一：`acceptance_gate` 扫描前剔除引用块/代码块/行内代码

- 代码：`scripts/acceptance_gate.py` 新增 `scannable()`，在 `checked()` / `unchecked()` 匹配前
  依次剔除围栏代码块、引用块、行内代码。
- **证伪（关掉剔除逻辑）**：临时把 `scannable()` 改成原样返回 `text`，运行
  `tests/test_release_gates.py::test_acceptance_gate_ignores_ac_examples_in_quotes_and_code`
  → **FAILED**；`cp` 还原后 `scripts/acceptance_gate.py` 的 sha256 回到原值。
- **反向保证（不能把真实结论一起吞掉）**：`test_acceptance_gate_still_detects_a_real_unchecked_ac`
  实测 passed；我另做端到端：同一份报告把「未打勾」只写在引用块 / 围栏代码 / 行内代码里 → `evaluate` 返回 `NONE`；
  把其中一行改成真正的正文未打勾行 → 立即报「AC-1 在验收报告中未打勾」。
- **结论：更准，未放宽。** 它只消除「举例被误判成结论」的假阳性，真实结论仍必须写在「## 逐条验收」的正文行里。
  本报告刻意在 T3 描述里用行内代码写出 `- [ ] **AC-2**：不成立` 作为举例，更新后仍能通过
  `acceptance_gate`（见「独立验收声明」的自检），即此修正的确在真实报告上生效。

### 修正二：`test_scope --check` 比对登记表归属列内容

- 代码：`scripts/test_scope.py` 新增 `ownership_drift_problems(entries, text)`，`--check` 中对
  **已登记**文件逐行比对「归属需求」列与 `collect_scope()` 的计算值，不一致即失败并提示 `make scope-write`。
- **证伪（关掉比对）**：临时把 `problems.extend(ownership_drift_problems(...))` 改成 `pass`，运行
  `tests/test_test_scope.py::test_check_fails_on_ownership_column_drift` → **FAILED**（`--check` 打印「检查通过」而过）；
  `cp` 还原后 sha256 回到原值。同文件的正向用例 `test_ownership_drift_problems_passes_when_in_sync` 仍 passed。
- **独立复现真实场景**（即 T3 存在的理由）：重构把测试标注从 `REQ-007` 改成 `REQ-006` 后，用
  `git show c2d689e:docs/TEST_SCOPE.md`（改标注前、尚未 `scope-write` 的登记表）与当前
  `collect_scope()` 比对，`ownership_drift_problems` **恰好报出 4 行「归属列已过期」**：
  `test_release_gates.py`（表写 `…, REQ-007`、实为 `…, REQ-006`）、`test_test_scope.py`、
  `test_two_layout_e2e.py`、`test_update_docs_contract.py`。把 `SCOPE_PATH` 指向该旧表跑
  `main(['--check'])` → 退出码 1、输出 4 条「归属列已过期」；而当前 `docs/TEST_SCOPE.md` 与
  `collect_scope()` 已同步，`ownership_drift_problems` 返回空。也就是说：**上一轮只能靠人工只读 diff
  才发现的漂移，现在门禁直接拦住**。
- **结论：更严，未放宽。** 它是在原有「文件集合 + 预算 + 归属编号已登记」之上**新增**的失败条件，
  不会让任何原本失败的检查通过。

## 未通过 / 存疑项

1. **【非阻断，建议修】T3 的证据指针不完整。** 任务清单 T3 的「证据」列写「见本需求的『维护与合并记录』
   （交付 PR 号在合入后回填）」，但 `REQ-006:115-120` 的「## 维护与合并记录」只有两条：REQ-007/008
   并入 T2/T4、REQ-009 撤回并入 T3；**并未记录 T3 的两处修正本身**（那两行现存于已被 `superseded`
   的 `REQ-007:104-111` 的「维护记录」）。按 AC-7「每项有状态与证据」的字面，T3 有状态与证据入口，
   但跟随该指针读不到 T3 的交付内容。建议：把两处修正（含本报告链接与 PR 号）写进 REQ-006 的
   「维护与合并记录」，或把 T3 的证据直接指向 `2026-09-20-REQ-007.md` 的维护记录与 PR。这不改变
   AC-7 判成立（实质的「不放松」与独立验收均已具备），但会让这条常设需求的记录自洽。
2. **【非阻断，观察】台账 REQ-006 的关联测试列含一个未标注 `REQ-006` 的文件。** 台账行声明 5 支
   （`test_release_gates.py`、`test_requirement_traceability.py`、`test_test_scope.py`、
   `test_update_docs_contract.py`、`test_two_layout_e2e.py`）；全 tests/ 扫描实际含 `REQ-006` 字面量的是 4 支，
   `tests/test_requirement_traceability.py` 不含 `REQ-006`（它作为追溯门禁本身，被扫描器按设计排除）。
   追溯门禁只要求「已交付需求至少被一支测试引用」，故不报错、AC-7 亦不受影响。若要延续 T3 刚建立的
   「声明 == 实际」标准，可给该文件补一行 `# 覆盖需求：REQ-006` 的注释，或从台账该行列中移除它。
3. **【非阻断，观察】AC 在条目中未按编号顺序排列**：`REQ-006-…md` 里 AC-7（`:63-64`）印在 AC-6（`:65-68`）之前，
   `requirement_ac_ids('REQ-006')` 也返回 `['1','2','3','4','5','7','6']`。不影响任何判据与门禁，纯阅读顺序问题。
4. **历史存疑项已闭合（记录用）**：上一版报告（基线 `a0d5ef6`）列出的多项观察，已被 T2/T3 处理：
   ① `docs/TEST_SCOPE.md` 的 `REQ-999` 过期归属行与「`--check` 不校验归属列」→ T3 的归属列比对 + `scope-write` 修复
   （当前登记表与 `collect_scope()` 完全同步）；② `test_scope --check` 失败路径无直接单测 → T2 补齐隔离单测；
   ③ `pr_body_guard` 收紧为「需求编号小节必填」（实测无小节、正文提及 `REQ-003` → 退出码 1）；
   ④ 回归记录只校验「小节存在」→ T2 深入小节内校验；⑤ `develop` 夹具 → 已清除（`grep -rn develop tests/*.py` 无命中）。

## 结论

**通过（AC-1…AC-7 全部成立）。** 附 1 项非阻断的记录缺陷（存疑项 1，建议随本轮一起补），不改变任何 AC 判定。

- 全量测试：**1471 passed, 3 skipped, 覆盖率 76.80%**（`make verify`，exit 0；追溯门禁 8 passed；
  scope 33 支文件 / 1474 用例；回归门禁通过）。
- AC-1…AC-6（既有流程能力判据）在 `4f8fde0` 上逐条复核**无回归**；AC-7（常设需求的任务清单 +
  修正只准更准/更严 + 独立验收确认）**首次判定成立**。
- T3 两处维护修正独立复核成立，并均以证伪法确认：`acceptance_gate` 的剔除逻辑关掉 → 容忍举例的测试 FAILED；
  `test_scope` 的归属列比对关掉 → 漂移测试 FAILED；两次都还原并核对 sha256 一致。真实场景也复现：
  改标注后旧登记表被报出 4 行「归属列已过期」，`scope-write` 后通过。
- 对「常设需求 + 任务清单」结构的意见：**方向合理，优于按零散修正各开一个编号**。理由：这条需求描述的是
  「流程能力」，其价值在于持续维护，用 AC 钉住能力、用任务清单承载工作包与证据，能让需求单保持稳定、
  生命周期不再被碎成一地；本次把 REQ-007/008/009 收敛回 T2/T3/T4 是正确执行。两点建议：
  （a）任务清单的「证据」列应指向**能直接读到交付内容**的位置，并在 PR 合入后回填编号（存疑项 1 即此）；
  （b）可考虑给任务清单加一条「完成 = 有独立验收链接」的硬约定，与 AC-7 的「每次修正都要有独立验收确认」
  对齐，避免任务被标「完成」却没有可追溯的验收证据。
