---
batch: 2026-09-20-REQ-006
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-006
base: bbbf330
full-suite: "1440 passed, 3 skipped, 覆盖率 76.35%"
---

# 独立验收报告：REQ-006 需求-测试-开发流程与三道门

## 独立验收声明

评审者为无上下文的独立 agent：未参与 REQ-006 的实现，未查看开发过程，也未复述实现者的说法。
本报告的所有结论只来自我自己读到的文件、我自己运行的命令与其输出。

- 被验收对象（第三轮复验）：分支 `chore/req-test-dev-flow`，`git rev-parse --short HEAD` = `bbbf330`（`git status --porcelain` 为空，工作树干净）。
- 前两轮基线：`397687f`（首版，AC-6 不成立）、`e080655`（第二轮，AC-6 仍不成立）；本轮在 `e080655..bbbf330` 的 8 个改动文件上逐项复核，`main` = `e453255`。
- 复核范围：需求条目（含新增的 AC-6 需求变更记录）、`.github/workflows/*.yml`、`scripts/pr_body_guard.py`、`scripts/acceptance_gate.py`、`scripts/regression_gate.py`、`scripts/test_scope.py`、`Makefile`、PR 模板、`docs/DEVELOPMENT.md`、`docs/TESTING.md`、`CONTRIBUTING.md`、`README.md`、`CHANGELOG.md`、`docs/TEST_SCOPE.md`、`docs/regression/`、`docs/requirements/README.md`、`tests/test_release_gates.py`、`tests/test_test_scope.py`、`tests/test_requirement_traceability.py`。
- 本地无 `python` 命令，脚本一律用 `.venv/bin/python`（Python 3.12）运行；CI 侧用 `python`。

## 全量测试

命令（仓库根目录，全 mock，未设置 `TUSHARE_TOKEN`）：

```bash
make verify
```

结果摘要（`bbbf330` 实测）：

```
1440 passed, 3 skipped in 58.14s
Required test coverage of 74% reached. Total coverage: 76.35%
6 files skipped due to complete coverage.   （TOTAL 11107 语句 / 2627 未覆盖）
.venv/bin/python -m pytest -q tests/test_requirement_traceability.py   ->  8 passed in 0.02s
.venv/bin/python scripts/test_scope.py --check  ->  测试 scope 检查通过：32 支文件、1443 个用例，均在预算内。
main 批量回归门禁通过（自 e453255 起累积的特性合入未达阈值 3）。        <- 本轮新增，已出现在 verify 输出里
本地可复现的门禁全部通过；PR 上下文门禁（pr-title / pr-body / acceptance-gate）由 CI 执行。
real 1m0.793s   （exit code 0）
```

- passed / skipped / 覆盖率：**1440 passed / 3 skipped / 76.35%**（门禁 ≥ 74%）。
- 3 个 skipped 全部是 `tests/test_integration.py:30/76/110` 的 live API 用例（`TUSHARE_TOKEN not set`），符合「CI 不依赖网络与 Token」的约束。
- `make -n verify` 实测展开顺序（`Makefile:5`、`:41`）：compileall → `git diff --check`（工作区）→ `git diff --check $(git merge-base origin/main HEAD)..HEAD`（整段）→ `cov` → `trace` → `scope-check` → `regression-check` → 汇总提示；`make regression-check` 单独跑也 `exit=0`。回归门禁结果行确实出现在 `make verify` 输出中，不是只挂在目标上。
- 数字一致性：`docs/DEVELOPMENT.md:152/153/159`、`docs/TESTING.md:46/77`、`CONTRIBUTING.md:61/191`、`README.md:245/248`、`CHANGELOG.md:13/26`、`docs/TEST_SCOPE.md:18`、`docs/regression/2026-09-20-baseline.md`、`docs/requirements/ledger.md:52` 均为 1440 passed / 3 skipped / 76.35% / 32 支文件 / 1443 用例，与 `make verify`、`--collect-only` 实测吻合。

## 逐条验收

- [x] **AC-1**：成立。分支模型为「一个特性一条特性分支、子 PR 合入特性分支、特性分支直接合入 main、不维护长期集成分支」（`CONTRIBUTING.md:33-37`、`docs/DEVELOPMENT.md:23-37`、`docs/DEVELOPMENT.md:31` 明文「没有 `develop` 之类」）；`main` 保护由 `setup-branch-protection.yml:80-96` 应用（必需状态检查 `ci-success`、至少 1 个 review、CODEOWNERS 审阅、线性历史、禁 force push/删除），其 `branch` 输入说明为「如 main；特性分支要保护也可填分支名」（`:7`）；`git branch -a` 实测只有 `main` / `feat/buy-sell-plan` / `fix/qualitative-context-evidence-budget` / 本 `chore/…`，不存在 `develop`；全仓 grep「develop」只剩否定式表述（`docs/DEVELOPMENT.md:31`、`docs/requirements/REQ-006-…md:37`）、新 AC-6 的禁止性条款（`:52`）与 `tests/test_release_gates.py:57/63/68` 把「develop」当任意非 main 字符串的测试夹具，已无把 `develop` 当现有分支的说法；CI 只在整支合 `main` 时加跑验收与回归门禁（`ci.yml:139`、`ci.yml:163`）。限制（不变）：GitHub 分支保护无法按来源分支约束 PR，「main 只接受特性分支的 PR」由评审与门禁流程保证，非机器强制。
- [x] **AC-2**：成立。`ci.yml:8` 的 `pull_request:` 无任何 `branches` 过滤，`test` 作业（`ci.yml:18-48`）无 `if`，因此任何目标分支的 PR（含合入 `feat/*` 的子 PR）都跑全量 pytest + 覆盖率门禁 `--cov-fail-under=74`（矩阵 3.10/3.12）；`pr-body` 作业（`ci.yml:116-135`）同样无 `if`，对每个 PR 调用 `scripts/pr_body_guard.py`（`:57-81`）。实测：自测栏填实 + `--base feat/x` → `exit=0`；自测栏为空/只有 `<!-- 待填 -->` → `exit=1`「为空或只有占位」；`--base main` 且验收报告栏「不适用」→ `exit=1`；链接 `docs/verification/…md` → `exit=0`。判定确定性（无网络/时间因素），PR 模板三栏齐备（`.github/PULL_REQUEST_TEMPLATE.md:12-39`）。
- [x] **AC-3**：成立。`acceptance-gate` 仅 `base_ref == 'main'` 时运行（`ci.yml:139`），脚本要求本 PR 新增/修改 `docs/verification/` 报告（`acceptance_gate.py:79-85`）、front matter 四字段齐全（`:25`、`:117-120`）、`independence` ∈ {independent,yes,true}（`:121-126`）、覆盖 PR 正文全部 `REQ-NNN`（`:98-101`、`:129-132`）、必须有形如「1440 passed」的记录（`:24`、`:134-135`）、每条 AC 必须 `- [x] **AC-n**`（`:52-58`、`:137-142`）；多份报告取并集；docs-only 放行（`:27`、`:74-76`、`:94-95`）；AC 校验收敛到按需求分节的范围（`req_sections`/`texts_for_requirement`，`:54-88`）。我独立复现：报告声明 `REQ-003, REQ-005`，`### REQ-003` 节 AC-1 打勾、`### REQ-005` 节 AC-1 写 `- [ ]` → 只报 `REQ-005 的 AC-1 未打勾`，不连累 REQ-003；只给 `REQ-005` 分节 → 报「没有 REQ-006 的逐条验收内容」；未分节报告按整篇判定。新增单测（`tests/test_release_gates.py:204-245`）单独跑 `4 passed`。「缺失/不合格即 CI 失败」的闭环由 `ci-success`（`ci.yml:181-205`）＋分支保护必需检查 `ci-success` 完成。
- [x] **AC-4**：成立。阈值 `THRESHOLD = 3`（`regression_gate.py:30`），只计 `^feat(\(|:|!)` 的 squash 提交或 `^Merge pull request`（`:28-29`、`:66-72`），自上次记录的 `covered-until` 起算（`:75-84`、`:190-200`），达到阈值且无更新记录即阻断（`:87-104`）；`validate_record`（`:107-122`）校验 front matter `date`/`covered-until`/`reviewer`/`full-suite` 有效（`TBD` 无效）、正文含「## 全量测试」+ passed 数字、含「## 逐条验收」小节。实测：3 条 feat 提交 → 阻断，2 条 → `[]`；`--check --main-ref main` 与 `origin/main` → `exit=0`；`--new` 出含 `covered-until` 与「## 逐条验收」的草稿；`validate_record` 对空壳记录报「缺少「## 逐条验收」小节」、对缺 passed 报「没有记录全量测试结果」、对 `reviewer: TBD` 报「缺少有效的 `reviewer`」、对完整记录 → `[]`；`make regression-check`（本轮新增，已挂进 `verify`）`exit=0`。残留软点见存疑项 4。
- [x] **AC-5**：成立。`docs/TEST_SCOPE.md` 有登记表与预算（上限 40 文件 / 1600 用例，当前 32 / 1443）；预算常量在 `scripts/test_scope.py:31-32`；`--check`（`:169-195`）校验登记表与真实 `tests/test_*.py` 集合一致（未登记 → missing、已删除仍登记 → stale）并检查预算。实测：`--check` → `exit=0`「32 支文件、1443 个用例，均在预算内」；`--report` → 「测试文件 32/40，用例 1443/1600」＋分层＋逐文件归属；用 `registered_files()` 复现：当前 `actual ^ registered == []`，并入虚构新文件 → missing 命中，并入虚构已删文件 → stale 命中。`tests/test_test_scope.py` 覆盖采集/归属/解析/渲染/预算常量。残留软点见存疑项 5。
- [x] **AC-6**：**成立**（按 `bbbf330` 生效的新条款）。新条款（`docs/requirements/REQ-006-…md:49-52`）：本地 `make verify` 覆盖 CI 中**对仓库内容**的全部检查——编译/空白（含相对基线的整段 diff）、全量测试、覆盖率、追溯、scope、批量回归门禁；依赖 PR 元数据的 `pr-title`/`pr-body`/`acceptance-gate` 由 CI 执行、`make gates` 给本地预演；文档描述与数字一致且不得出现已废弃分支说法。逐项核验：(1) **verify 真的覆盖了 CI 的内容类检查**——`make -n verify` 显示 `lint cov trace scope-check regression-check`，其中 CI 的 `test`→`cov`（同一命令）、`lint`→`lint`（compileall + `git diff --check` 工作区 + `git diff --check $(git merge-base origin/main HEAD)..HEAD`，与 `ci.yml:64-66` 的 `base...sha` 同工具同语义；实测 `BASE=e4532550…` 非空 → 该分支确实执行，`range-check exit=0`）、`test_scope`→`scope-check`、`regression-gate`→`regression-check`（本轮新增，`Makefile:36-40`），且 `make verify` 输出里出现了回归门禁结果行；`pr-title`/`pr-body`/`acceptance-gate` 属 PR 元数据，由 CI 执行，`make gates` 给出预演方式（`Makefile:30-34`，验收示例已补 `--base origin/main --head HEAD`）。(2) **措辞**——全仓 `grep -rn 等价 README.md docs/TESTING.md docs/DEVELOPMENT.md CONTRIBUTING.md Makefile` 返回空；`docs/TESTING.md:46`、`CONTRIBUTING.md:61`、`README.md:245` 均改为「本地全部门禁：… + 回归门禁」，并写明 PR 上下文边界。(3) **数字与一致性**——1440 passed / 3 skipped / 76.35% / 32 支文件 / 1443 用例在 DEVELOPMENT、TESTING、CONTRIBUTING、README、CHANGELOG、TEST_SCOPE、regression 记录、ledger 中一致，并与 `make verify` 实测吻合。(4) **废弃分支说法**——见 AC-1，仓库内只剩否定式/禁止性表述与测试夹具。条款改写已按需求变更流程留痕，合规性判断见存疑项 2。

## 未通过 / 存疑项

1. **无阻断项。** AC-1…AC-6 全部成立，`make verify` 全绿（1440 passed / 3 skipped / 76.35%，exit 0）。
2. **对「AC-6 条款改写 + 批准记录」的合规性判断：满足我上一轮提出的标准，判定为受控的需求变更，不认定为「改小标准」。** 逐项对照我要求与 `docs/requirements/README.md:77-81`、`docs/DEVELOPMENT.md:147-148` 的新规矩：(a) **原条款原文**留在条目内的变更记录里（`docs/requirements/REQ-006-…md:55-56`，与 `397687f` 的原文逐字一致，我核对过）；(b) **变更原因**写明并可验证——`pr-title`/`pr-body` 读的是 GitHub PR 标题与描述、`acceptance-gate` 依赖 PR 的 base/head，属 PR 元数据（`:57-58`；对应 `ci.yml:97`、`:131-132`、`:158-159`）；(c) **新条款**就是当前 AC-6 正文（`:49-52`）；(d) **owner 批准**记录在案（`：61`，CHU-2002，2026-09-20）；(e) **流程文档同步**——`docs/requirements/README.md:77-81` 与 `docs/DEVELOPMENT.md:147-148` 都新增「收窄判据必须留原条款/原因/新条款/批准人并告知独立评审者，静默收窄视为违规」；(f) **独立评审**指向本报告（`:62`）。此外这次变更是**有补偿的再划定**而非单纯放松：`make verify` 增加了原条款未要求的批量回归门禁，lint 从「只看未暂存改动」升级为「工作区 + 相对基线整段 diff」，即被删掉的只是本地无法等价复现的 PR 元数据三项，同时把本地内容类门禁从 4 项扩到 6 项。因此我的结论是：这属于「显式、留痕、经 owner 批准、由独立评审复核」的需求变更，不是偷偷改小标准。唯一无法由我独立证实的是批准动作本身（我只能看到记录中的批准人与日期，看不到那次会话）；这是留痕型流程的固有边界，不是本次交付的缺陷，但我会把它记在这里以免读者高估其证据力。
3. **观察（非阻断）：`docs/DEVELOPMENT.md:104-105` 把 `make gates` 写了两遍**（一行说明 PR 上下文预演、一行仍是「查看三道门在本地怎么自检」）。两行描述都正确，属编辑残留的重复行，建议合并为一行。
4. **存疑（非阻断）：回归记录内容校验只到「小节存在」这一层**。`validate_record`（`regression_gate.py:107-122`）要求出现「## 逐条验收」，但不要求其中真有 `- [x] **AC-` 留痕；实测仅写「## 逐条验收」+「见 docs/verification/…」即可通过。AC-4 的可判定核心成立，故判成立；若要更严，可要求该小节出现 AC 打勾或 `docs/verification/` 链接。
5. **存疑（非阻断）：`test_scope.py --check` 的失败路径仍无直接单测**（`tests/test_test_scope.py:4-7` 明确不测 CLI `main`，避免 pytest 递归）；本轮仍以 `registered_files()` 内存模拟验证 missing/stale。受本次「只改一个报告文件」的约束，未在仓内实造未登记文件。
6. **观察（非阻断）：AC-6 的需求变更记录是 HTML 注释**（`docs/requirements/REQ-006-…md:54-62`），内容齐全但在 GitHub 渲染视图里不可见；若希望评审者直接看到，可提升为可见的「## 需求变更记录」小节（需同步确认追溯门禁仍通过）。
7. **观察（非阻断）：`tests/test_release_gates.py:57/63/68` 仍以 `"develop"` 作为「非 main 目标分支」的测试夹具字符串**。功能正确，也未被 AC-6 的「文档」口径覆盖，属可选清理项。
8. **观察（非阻断）：REQ-006 自己的提交不会被门③计数**。`git log --pretty=%s e453255..HEAD` 的标题均非 `feat` 前缀（`fix(process)`/`refactor(process)`/`chore(process)`/`chore(governance)`）；若以 squash 合入 `main`，按 `regression_gate.py:28-29` 的明文口径不计入「特性合入」。属 AC-4 计数口径的副作用，非缺陷，仅提示后续特性分支留意标题或合并方式。

## 结论

**通过。**

- 全量测试：**1440 passed, 3 skipped, 覆盖率 76.35%**（`make verify`，exit 0，pytest 58.14s / 总 1m01s，全 mock；追溯门禁 8 passed；scope 32 支文件 / 1443 用例；回归门禁通过）。
- AC-1…AC-6 全部成立：分支模型与 main 保护；每个 PR 的 CI 全量 + 自测栏守卫；独立验收报告门禁（并集 + docs-only 放行 + 按需求分节）；每 3 个特性合入的批量回归门禁（含记录内容校验，并已挂进 `make verify` 与 `make regression-check`）；测试 scope 登记与预算；本地 `make verify` 覆盖 CI 的对内容检查、PR 元数据门禁边界写明、文档措辞与数字一致、无废弃分支说法。
- 第三轮新修项均已独立核验：三处「与 CI 等价」措辞清除（`grep -rn 等价 …` 为空）；`make lint` 改为工作区 + 相对基线整段 diff（实测 merge-base 分支执行、exit 0，工具与 `ci.yml:64-66` 一致）；`make verify` 新增 `regression-check` 且输出里可见回归门禁结果行；AC-6 条款改写走需求变更流程并留全五项记录。
- 对条款改写的判断：**满足「原条款原文 + 原因 + 新条款 + owner 批准 + 同步流程文档 + 独立评审复核」的全部要求，且伴随实质加强（verify 多覆盖回归门禁、lint 覆盖整段 diff），判定为合规的受控需求变更，不是改小标准。** 唯一不可独立证实的是 owner 批准动作本身（仅有留痕），已在存疑项 2 注明。
- 存疑项 3–8 均为非阻断的观感/可选强化项，不影响本次验收结论；后续如需收紧，优先做存疑项 4（回归记录 AC 留痕）与 3（DEVELOPMENT 重复行）。
