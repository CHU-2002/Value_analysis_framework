---
batch: 2026-09-20-REQ-006
date: 2026-09-20
reviewer: independent-agent
independence: independent
requirements: REQ-006
base: e080655
full-suite: "1440 passed, 3 skipped, 覆盖率 76.35%"
---

# 独立验收报告：REQ-006 需求-测试-开发流程与三道门

## 独立验收声明

评审者为无上下文的独立 agent：未参与 REQ-006 的实现，未查看开发过程，也未复述实现者的说法。
本报告的所有结论只来自我自己读到的文件、我自己运行的命令与其输出。

- 被验收对象（复验）：分支 `chore/req-test-dev-flow`，`git rev-parse --short HEAD` = `e080655`（`git status --porcelain` 为空，工作树干净）。
- 上一轮验收基线：`397687f`（本报告首版即基于它，当时 AC-6 判不成立）；本轮在 `397687f..e080655` 的 21 个改动文件上逐项复核。
- 对照 `main` = `e453255`。
- 复核范围：需求条目、`.github/workflows/*.yml`、`scripts/pr_body_guard.py`、`scripts/acceptance_gate.py`、`scripts/regression_gate.py`、`scripts/test_scope.py`、`Makefile`、PR 模板、`docs/DEVELOPMENT.md`、`docs/TESTING.md`、`CONTRIBUTING.md`、`README.md`、`CHANGELOG.md`、`docs/TEST_SCOPE.md`、`docs/regression/`、`tests/test_release_gates.py`、`tests/test_test_scope.py`、`tests/test_requirement_traceability.py`。
- 本地无 `python` 命令，脚本一律用 `.venv/bin/python`（Python 3.12）运行；CI 侧用 `python`。

## 全量测试

命令（仓库根目录，全 mock，未设置 `TUSHARE_TOKEN`）：

```bash
make verify
```

结果摘要（`e080655` 实测）：

```
1440 passed, 3 skipped in 55.36s
Required test coverage of 74% reached. Total coverage: 76.35%
6 files skipped due to complete coverage.
.venv/bin/python -m pytest -q tests/test_requirement_traceability.py   ->  8 passed in 0.02s
.venv/bin/python scripts/test_scope.py --check  ->  测试 scope 检查通过：32 支文件、1443 个用例，均在预算内。
本地校验全部通过。
real 0m57.474s   （exit code 0）
```

- passed / skipped / 覆盖率：**1440 passed / 3 skipped / 76.35%**（门禁 ≥ 74%；`TOTAL 11107 语句 / 2627 未覆盖`）。
- 3 个 skipped 全部是 `tests/test_integration.py:30/76/110` 的 live API 用例（`TUSHARE_TOKEN not set`），符合「CI 不依赖网络与 Token」的约束。
- `make verify` = `lint`（compileall + `git diff --check`）→ `cov` → `trace` → `scope-check`（`Makefile:41`），总墙钟约 57s。
- 文档数字已与本轮实测对齐：`docs/DEVELOPMENT.md:152/153/159`、`docs/TESTING.md:77`、`CONTRIBUTING.md:191`、`README.md:248`、`CHANGELOG.md:13/26`、`docs/TEST_SCOPE.md:18`、`docs/regression/2026-09-20-baseline.md:5/6/26`、`docs/requirements/ledger.md:52` 现在一致为 1440 passed / 3 skipped / 76.35% / 32 支文件 / 1443 用例；`make verify` 的 scope 检查与 `pytest --collect-only` 均为 1443，逐项吻合。

## 逐条验收

- [x] **AC-1**：成立。分支模型为「一个特性一条特性分支、子 PR 合入特性分支、特性分支直接合入 main、不维护长期集成分支」（`CONTRIBUTING.md:33-37`、`docs/DEVELOPMENT.md:23-37`、`docs/DEVELOPMENT.md:31` 明文「没有 `develop` 之类」）；`main` 保护由 `setup-branch-protection.yml:80-96` 应用（必需状态检查 `ci-success`、至少 1 个 review、CODEOWNERS 审阅、线性历史、禁 force push/删除），其 `branch` 输入说明已修正为「如 main；特性分支要保护也可填分支名」（`:7`）；`git branch -a` 实测只有 `main` / `feat/buy-sell-plan` / `fix/qualitative-context-evidence-budget` / 本 `chore/…`，不存在 `develop`，仓库内已无把 `develop` 当现有分支引用之处（剩余仅 `docs/DEVELOPMENT.md:31` 的「没有 develop 之类」与 `docs/requirements/REQ-006-…md:37` 的否定式表述，以及 `tests/test_release_gates.py:57/63/68` 把它当任意非 main 字符串的测试夹具）；CI 只在整支合 `main` 时加跑验收与回归门禁（`ci.yml:139`、`ci.yml:163` 的 `github.base_ref == 'main'`），子 PR 不加跑。限制（不变）：GitHub 分支保护无法按来源分支约束 PR，「main 只接受特性分支的 PR」由评审与门禁流程保证，非机器强制。
- [x] **AC-2**：成立。`ci.yml:8` 的 `pull_request:` 无任何 `branches` 过滤，`test` 作业（`ci.yml:18-48`）无 `if`，因此任何目标分支的 PR（含合入 `feat/*` 的子 PR）都跑全量 pytest + 覆盖率门禁 `--cov-fail-under=74`（矩阵 3.10/3.12）；`pr-body` 作业（`ci.yml:116-135`）同样无 `if`，对每个 PR 调用 `scripts/pr_body_guard.py`（`scripts/pr_body_guard.py:57-81`）。实测（`.venv/bin/python scripts/pr_body_guard.py --body-file … --base …`）：自测栏填实 + `--base feat/x` → `exit=0`「PR 描述校验通过（目标分支 feat/x）」；自测栏为空或只有 `<!-- 待填 -->` → `exit=1`「「## 研发自测（手工）」为空或只有占位」；`--base main` 且验收报告栏「不适用」→ `exit=1`；`--base main` 且链接 `docs/verification/…md` → `exit=0`。判定为确定性（无网络/时间因素），PR 模板三栏齐备（`.github/PULL_REQUEST_TEMPLATE.md:12-39`）。
- [x] **AC-3**：成立，且本轮已修好我上一轮提到的「AC 打勾判定跨需求串台」。`acceptance-gate` 仅 `base_ref == 'main'` 时运行（`ci.yml:139`），脚本要求：本 PR 必须新增/修改 `docs/verification/` 报告（`acceptance_gate.py:79-85`）、front matter 四字段齐全（`:25`、`:117-120`）、`independence` ∈ {independent,yes,true}（`:121-126`）、覆盖 PR 正文全部 `REQ-NNN`（`:98-101`、`:129-132`）、必须有形如「1440 passed」的记录（`:24`、`:134-135`）、每条 AC 必须 `- [x] **AC-n**`（`:52-58`、`:137-142`）；多份报告取并集；docs-only PR 放行（`:27`、`:74-76`、`:94-95`）。新增的按需求分节逻辑 `req_sections` / `texts_for_requirement`（`:54-88`）我已独立复现：报告声明 `REQ-003, REQ-005`，`### REQ-003` 节 AC-1 打勾、`### REQ-005` 节 AC-1 写 `- [ ]` → 只报 `REQ-005 的 AC-1 在验收报告中未打勾`，`REQ-003` 不再被连累；报告声明两个需求却只给 `REQ-005` 分节 → 报「没有 REQ-006 的逐条验收内容」；未分节的报告仍按整篇判定（我的 `REQ-006` 报告 → 仍只报 AC-6）。新增单测 3 项（`tests/test_release_gates.py:204-245`）我单独跑过 → `4 passed`（含既有的完整报告用例）。「缺失/不合格即 CI 失败」的闭环由 `ci-success`（`ci.yml:181-205`）＋分支保护必需检查 `ci-success` 完成。
- [x] **AC-4**：成立，且本轮补上了我上一轮存疑的「记录内容校验」。阈值 `THRESHOLD = 3`（`regression_gate.py:30`），只计 `^feat(\(|:|!)` 的 squash 提交或 `^Merge pull request`（`:28-29`、`:66-72`），自上次记录的 `covered-until` 起算（`:75-84`、`:190-200`），达到阈值且无更新记录即阻断（`:87-104`）；新增 `validate_record`（`:107-122`）校验 front matter `date`/`covered-until`/`reviewer`/`full-suite` 有效（`TBD` 视为无效）、正文含「## 全量测试」+ passed 数字、含「## 逐条验收」小节。实测：`evaluate([], ["feat(a)…","feat(b)…","feat(c)…"], 3)` → 1 条「已累积 3 个特性合入」，两条 feat → `[]`，两条 feat + 一条 merge → 1 条；`.venv/bin/python scripts/regression_gate.py --check --main-ref main`（及 `origin/main`）→ `exit=0`「未达阈值 3」；`--new` 出含 `covered-until` 与「## 逐条验收」的草稿；`validate_record` 对空壳记录 → 报「缺少「## 逐条验收」小节」，对缺 passed 数字 → 报「没有记录全量测试结果」，对 `reviewer: TBD` → 报「缺少有效的 `reviewer`」，对完整记录 → `[]`；`docs/regression/TEMPLATE.md` 与基线记录都已补「## 逐条验收」小节。残留软点见存疑项 3。
- [x] **AC-5**：成立。`docs/TEST_SCOPE.md` 有登记表与预算（上限 40 文件 / 1600 用例，当前 32 / 1443）；预算常量在 `scripts/test_scope.py:31-32`；`--check`（`:169-195`）校验登记表与真实 `tests/test_*.py` 集合一致（未登记 → missing、已删除仍登记 → stale）并检查两项预算。实测：`.venv/bin/python scripts/test_scope.py --check` → `exit=0`「32 支文件、1443 个用例，均在预算内」；`--report` → 「测试文件 32/40，用例 1443/1600」＋分层＋逐文件归属（`tests/test_release_gates.py` 已登记为 22 个函数，与实际一致）；用 `registered_files()` 复现漂移判定：当前 `actual ^ registered == []`，并入虚构新文件 → missing 命中，并入虚构已删文件 → stale 命中。`tests/test_test_scope.py` 覆盖采集/归属/解析/渲染/预算常量。残留软点（失败路径无直接单测）见存疑项 4。
- [ ] **AC-6**：**仍不成立**（较上一轮大幅收窄）。**已确认修好的部分**：`Makefile:37` 的 `make gates` 示例不再用 `develop`（改为 `--base feat/xxx`）；`Makefile:41` 的 `verify` 说明改为「覆盖 CI 中可在本地复现的检查项」；`CONTRIBUTING.md:110-113` 明确写出 `make verify` 覆盖哪些、`pr-title`/`pr-body`/`acceptance-gate`/`regression-gate` 依赖 PR 上下文只能由 CI 跑并指向 `make gates`；`regression.yml:3/5` 的「3 条功能合入」→「3 个特性合入」、「develop → main」→「特性分支 → main」；`setup-branch-protection.yml:7` 的 `develop` 文案清除；上一轮的用例数/耗时/覆盖率基线漂移（1436/1433/76.77%）已在 DEVELOPMENT / TESTING / CONTRIBUTING / README / CHANGELOG / TEST_SCOPE / regression 基线 / ledger 全部对齐到实测 1440 / 76.35% / 1443。**仍未修好的部分**（复现命令见下）：(a) 原条款「本地 `make verify` 覆盖 CI 的检查项」按字面仍不成立——`Makefile:41` 的 `verify` = `lint cov trace scope-check`，CI 的 `pr-title`（`ci.yml:90-114`）、`pr-body`（`ci.yml:116-135`）、`acceptance-gate`（`ci.yml:137-159`）、`regression-gate`（`ci.yml:161-179`）仍不在其中（现已由文档说明，属措辞与实现不符，而非隐瞒）；(b) `docs/TESTING.md:46` 仍写 `make verify # 与 CI 等价：…`（TESTING.md 是 AC-6 点名的文档），与 `CONTRIBUTING.md:110-113`、`Makefile:41` 的新表述直接矛盾；(c) `CONTRIBUTING.md:61` 仍写 `make verify  # 提交前自检（与 CI 等价）`，与同一文件 `:110-113` 自相矛盾；(d) `README.md:245` 仍写 `make verify   # 与 CI 等价：编译检查 + 全量测试 + 覆盖率门禁`（README 未在 AC-6 点名，但在 REQ-006「追溯」表中，且漏掉 trace 与 scope）；(e) 即便按「本地可复现」口径，`Makefile:19-21` 的 lint 仍只跑无参 `git diff --check`（仅未暂存改动），而 CI 是 `git diff --check base...head`（`ci.yml:64-66`），工作树/暂存区干净时本地这项等于空跑，并未复现 CI 的空白与冲突标记检查。复现：`grep -n "等价" docs/TESTING.md CONTRIBUTING.md README.md`；`sed -n '19,22p' Makefile` 与 `sed -n '64,69p' .github/workflows/ci.yml`；`make -n verify`；`make gates`。

## 未通过 / 存疑项

1. **AC-6 不成立（阻断项，仅剩文档措辞与本地 lint 口径）**：具体为上一条 (a)–(e)。其中 (b)(c)(d) 是三处「与 CI 等价」的过期措辞，(e) 是 lint 覆盖强度差异，(a) 是原条款字面与实现的差距（现已由文档如实说明）。建议处理：把 `docs/TESTING.md:46`、`CONTRIBUTING.md:61`、`README.md:245` 三处的「与 CI 等价」改为「覆盖 CI 中可在本地复现的检查项」，并在 `docs/TESTING.md` 补一句「`pr-title`/`pr-body`/`acceptance-gate`/`regression-gate` 依赖 PR 上下文，本地用 `make gates` 预演」；`Makefile:21` 改为 `git diff --check` 覆盖本分支相对 `main` 的范围（如 `git diff --check main...HEAD`）或明确注释其局限。这些改动不触碰任何门禁逻辑。
2. **对「AC-6 条款替换」的判断：该替换并未实际落地；且若落地，实质属于改小验收标准，不建议据此关闭 AC-6。** 事实层面：我逐字核对了 `docs/requirements/REQ-006-requirement-test-dev-flow.md`，`git diff 397687f..e080655 -- docs/requirements/REQ-006-requirement-test-dev-flow.md` 为**空**，该文件在本轮提交里根本没被改动；AC-6 仍是原文「本地 `make verify` 覆盖 CI 的检查项；`docs/DEVELOPMENT.md`、`docs/TESTING.md`、`CONTRIBUTING.md` 的描述与实际门禁一致。」（`:49-50`），其下没有你所说的替换注释（`:51` 空行，紧接 `## 范围`）。因此「显式替换 + 注释说明」目前不可核验，等于没有替换。实质层面：把「覆盖 CI 的检查项」改成「覆盖 CI 中可在本地复现的检查项」是在实现完成之后收窄判据去适配现状，属 `docs/DEVELOPMENT.md:146`「若实现方式与设计文档不符，改设计文档，**不要改小验收标准**」所禁止的形态；而且原条款并非不可满足——`make gates`（`Makefile:35-39`）已经证明三个门禁脚本都支持本地带参调用，只要把带输入的预演目标接进 `verify`（或让 `verify` 在提供 PR 描述文件时调用它们）就能真正覆盖，属于「能修而未修」。若确实认为该条款措辞本身不可判定，正确路径是由需求 owner 走显式变更：在需求条目里记录新旧表述、变更理由与批准（并把同步说明写进 `DEVELOPMENT.md`），而不是静默改文档或只改 CI；本轮两条路径都没有发生，故我判定为「未落地的替换，若落地即为改小标准」。
3. **存疑（非阻断）：回归记录内容校验目前只到「小节存在」这一层**。`validate_record`（`regression_gate.py:107-122`）要求「## 逐条验收」小节出现，但不要求其中真有 `- [x] **AC-` 留痕；实测一份 `## 逐条验收` 下只写「无（见 docs/verification/…）」的记录即可通过。相比上一轮已是实质改进、且 AC-4 的可判定核心成立，故 AC-4 判成立；若要更严，可在 `validate_record` 里要求该小节出现 AC 打勾或 `docs/verification/` 链接。
4. **存疑（非阻断）：`test_scope.py --check` 的失败路径仍无直接单测**（`tests/test_test_scope.py:4-7` 明确不测 CLI `main`，避免 pytest 递归）；本轮仍以 `registered_files()` 内存模拟验证 missing/stale 判定，未在仓内真造未登记文件（受本次验收「只改一个报告文件」约束）。
5. **观察（非阻断）：REQ-006 自己的提交不会被门③计数**。`git log --pretty=%s e453255..HEAD` 的标题是 `fix(process)…`/`refactor(process)…`/`chore(process)…`/`chore(governance)…`/`fix(process): close the gaps…`，均非 `feat` 前缀；若以 squash 合入 `main`，按 `regression_gate.py:28-29` 的口径不计入「特性合入」。这与 AC-4 明文写死的计数口径一致，属口径副作用而非缺陷，仅提示后续特性分支留意标题或合并方式。
6. **观察（非阻断）：`tests/test_release_gates.py:57/63/68` 仍以 `"develop"` 作为「非 main 目标分支」的测试夹具字符串**。功能正确（该分支名不存在也可以），但与 AC-1 废弃 `develop` 的叙述风格不一致，属可选清理项。

## 结论

**不通过（唯一未通过项为 AC-6，且只剩文档措辞与本地 lint 口径）。**

- 全量测试：**1440 passed, 3 skipped, 覆盖率 76.35%**（`make verify`，exit 0，pytest 55.36s / 总 57.5s，全 mock，scope 32 支文件 / 1443 用例）。
- AC-1、AC-2、AC-3、AC-4、AC-5 全部成立：分支模型与 main 保护、每个 PR 的 CI 全量 + 自测栏守卫、独立验收报告门禁（并集 + docs-only 放行 + 本轮新增的按需求分节）、每 3 个特性合入的批量回归门禁（本轮新增记录内容校验）、测试 scope 登记与预算，均以命令复现。
- AC-6 仍不成立：数字与 `develop` 文案类问题已全部修好，但 `docs/TESTING.md:46`、`CONTRIBUTING.md:61`、`README.md:245` 三处「与 CI 等价」仍在，`Makefile:21` 的 lint 仍未复现 CI 的范围检查，且原条款「make verify 覆盖 CI 的检查项」按字面仍不成立。
- 对条款替换的判断：**没有落地**（需求文件本轮零改动），因此无从认可；且若按你描述的方式替换，属于**改小验收标准**，应由需求 owner 走显式变更并说明理由，而不是静默收窄。
- 复核建议：修掉结论中列出的三处措辞 + lint 口径（或按显式变更流程重定 AC-6 并留批准记录）后，把本报告 AC-6 改为 `- [x]` 即可放行；其余 AC 无需再验。
