---
requirement: REQ-006.1
ac: AC-1.9
date: 2026-09-25
recorded-by: AI agent（会话内受需求 owner 授权代跑）
owner-authorization: 「你授权我跑，我出命令与观察，你过目后签实跑记录」
runs:
  - 20260925T042255414048Z   # 第一轮：REQ-006.1 AC-1.9 首次复跑（显式 --max-chars，已披露）
  - 20260925T053458757536Z   # 第二轮：main b23270b 上重跑，全程默认参数，零覆盖
---

# REQ-006.1 AC-1.9 实跑记录（两轮）

> 本文件是**原始观察**，不是独立验收报告。按 `docs/verification/TEMPLATE.md`，验收结论由**未参与实现**的
> 评审者在收口报告里另写，本文件作为其「## 实跑记录」小节的素材与可复现依据。
> 相关判据：AC-1.9「在只读 HOME + 真实 token 下**复跑**一次迭代，不使用任何运行时补丁，
> 产出完整 run（台账、manifest、结构化结果、两篇报告），命令与观察写进实跑记录。」

## 0. 两轮的关系与为什么要跑两轮

| | 第一轮 | 第二轮 |
|---|---|---|
| run_id | `20260925T042255414048Z` | `20260925T053458757536Z` |
| 框架 | `fff05d74`（main，含 PR #45） | `b23270b`（main，含 PR #46 = 任务 T8） |
| `synthesis` 预算 | **显式** `--max-chars 120000`（因默认值装不下，见发现 F6） | **默认参数，未传任何 `--max-chars`** |
| 目的 | 完成 AC-1.9、暴露真实载荷下的问题 | 在修掉 AC-1.5 缺口后重跑，证明**不再需要任何覆盖** |
| supersedes | `20260920T160940077275Z` | `20260925T042255414048Z` |
| 结论 | `D7` 结论维持（同期重跑链的起点） | 结论维持 |

第一轮暴露出**阻断级**发现 F6（`synthesis` 默认预算装不下真实载荷 → **AC-1.5 不成立**），
按 owner 决定「先修默认值 + 回归测试，然后重跑整轮」，修复走 PR #46 合入 `main`（`b23270b`），
第二轮即在该 commit 上进行。**第二轮的 `synthesis` 全程使用默认参数、零丢弃**，故
**AC-1.9 的「不使用任何运行时补丁」在第二轮是无保留成立的**。

## 1. 环境（两轮相同）

| 项 | 值 |
|----|-----|
| 日期 | 2026-09-25（CST） |
| Python | 3.12.13（`.venv/bin/python`） |
| HOME | `/tmp/ro-home`，`chmod 555`，**只读**（写探针 `touch /tmp/ro-home/probe` → `Permission denied`） |
| token 来源 | 仓库 `.env` 的 `TUSHARE_TOKEN`（`scripts/config.py:get_token()` 读取，不写 HOME）；**本记录不写 token 本身** |
| 数据源 | Tushare Pro（行情/财务）、CNINFO（定期报告发现）、本地 PDF（`600887_2026_中报.pdf`） |
| 工作区 | 两轮均 `git status` 干净（仅有一个与本任务无关的既有未跟踪文件 `dsh-lan.patch.yml`）；`dirty=false` |

## 2. 第二轮（主证据）：逐步命令与观察

命令均在仓库根目录执行，均带 `HOME=/tmp/ro-home`。`{R2}` = `output/600887_伊利/runs/20260925T053458757536Z`。

### Step 0 前置状态
```bash
.venv/bin/python scripts/analysis_status.py --company-dir output/600887_伊利 --ticker 600887 --json
```
- 退出码 **3**；`reasons`: `downstream_stale`、`framework_changed`
  （`code_fingerprint: fff05d74… → b23270b8…`）。即新框架（含 T8 修复）已生效。

### Step 1 发现与下载
```bash
.venv/bin/python scripts/download_report.py --stock-code 600887 --report-type auto --since 2026Q1 \
  --save-dir output/600887_伊利/sources/pdf
```
- `status: SUCCESS`、`periods_skipped: 2026H1,2026Q1`、`message: All periods already present: 2026H1,2026Q1`
  → **0 次下载**。此前 `discover_report.py --latest` 确认 CNINFO 最新定期报告仍是 **2026H1**（2026-08-27）。

### Step 1B 真实采集（本期唯一消耗 token 的一步）
```bash
.venv/bin/python scripts/tushare_collector.py --code 600887.SH \
  --output output/600887_伊利/data_pack_market.md
```
- 退出码 **0**。
- **AC-1.2 实证**：只读 HOME 下 exit 0，且执行后 `/tmp/ro-home` **仍为空目录**（未生成 `~/tk.csv`）。
- 采集真实联网：`yc_cb` 因无权限失败（见发现 F2），但行情/财务块正常刷新。

### Step 2 章节解析
```bash
.venv/bin/python scripts/pdf_preprocessor.py --pdf output/600887_伊利/sources/pdf/600887_2026_中报.pdf \
  --period 2026H1 --output output/600887_伊利/sources/pdf/pdf_sections_2026H1.json
```
- `Done: 9/9 sections found`。

### Step 3 签发 run
```bash
.venv/bin/python scripts/runs.py new --company-dir output/600887_伊利 \
  --ticker 600887.SH --company 伊利股份 --market CN --kind report-update \
  --primary-period 2026H1 --supersedes 20260925T042255414048Z \
  --input output/600887_伊利/data_pack_market.md \
  --input output/600887_伊利/sources/pdf/600887_2026_中报.pdf \
  --input output/600887_伊利/sources/pdf/pdf_sections_2026H1.json
```
- 输出 `{R2}` = `…/runs/20260925T053458757536Z`；`run.json` 的 `framework.git_commit = b23270b8…`、`dirty=false`。

### Step 4 prepare
```bash
.venv/bin/python -m scripts.results.prepare --output-dir {R2} --ticker 600887.SH --company 伊利股份 \
  --market CN --primary-period 2026H1 \
  --prior-analysis output/600887_伊利/runs/20260925T042255414048Z/synthesis/result.json
```
- `d6_triggered: false`；`warnings: []`；生成 6 个 context bundle + `evidence/index.json`。
- 两轮可比性核验：六个模块的 `selection` 对象**逐模块完全一致**（脚本比对，全等）
  → 两轮差异只有 `run_id`、`input_digest` 与刷新后的行情数值，因此可直接对照。

### Step 5 六个模块（并行）
```bash
.venv/bin/python -m scripts.results.validate_result "{R2}/modules/{module}/result.json" \
  --evidence-index "{R2}/evidence/index.json" --context "{R2}/contexts/{module}.json"
```

| 模块 | `run.status` | claims | evidence | 校验器 |
|---|---|---|---|---|
| business_moat | `partial` | 14 | 8 | `Valid result` exit 0 |
| environment | `partial` | 6 | 7 | `Valid result` exit 0 |
| governance | `partial` | 13 | 9 | `Valid result` exit 0 |
| mda_quality | `partial` | 10 | 8 | `Valid result` exit 0 |
| period_delta | `partial` | 11 | 11 | `Valid result` exit 0 |
| holding_structure | `not_applicable` | 4 | 5 | `Valid result` exit 0 |

- **六条全部通过，无一处使用 `--allow-legacy`、无一处放宽判定。**

### Step 6 对账与汇总（**AC-1.5 的判定点**）
```bash
.venv/bin/python -m scripts.results.reconcile_results \
  --input {R2}/modules/business_moat/result.json … --input {R2}/modules/period_delta/result.json \
  --optional-input {R2}/modules/holding_structure/result.json \
  --output {R2}/synthesis/reconciliation.json
.venv/bin/python -m scripts.results.synthesis --input … --optional-input … \
  --reconciliation {R2}/synthesis/reconciliation.json --evidence-index {R2}/evidence/index.json \
  --output {R2}/synthesis/context.json
```
- `reconcile_results`：exit 0，**1 conflict** —— `DATE-001`（temporal / **high**）：六个模块 `as_of` 不一致
  （`business_moat 2026-07`、`governance 2026-09-18`、其余 `2026-06-30`）→ `overall_confidence: low`（发现 F16）。
- `synthesis`：**未传 `--max-chars`**，退出码 0，**且没有打印「预算不足，已丢弃 …」**：

  ```
  Synthesis context written: …/synthesis/context.json (97359 chars)
  ```

  载荷自查（读 `budget`）：

  | 项 | 值 |
  |---|---|
  | `max_chars` | **120000**（= `DEFAULT_MAX_CHARS`，未显式指定） |
  | `actual_chars` | **97359** |
  | `dropped_count` | **0** |
  | `dropped` | `{}` |
  | 逐卡片 `omitted` 合计 | **0**（metrics / claims / risks / watchlist / evidence / quality 全为 0） |
  | `module_count` / `evidence_count` | 6 / 31 |

  → **AC-1.5 成立**：默认预算放得下真实载荷，**全卡片零丢弃、零省略**（连 evidence 都没有省略）。
- 对照第一轮（同一套真实载荷、旧默认）：默认 40,000 下 `dropped_count=148`、逐卡片 `omitted` 合计 299，
  且被丢的包括每个模块的 `quality.missing_inputs`（发现 F6）。

### Step 7 更新既有结论
```bash
.venv/bin/python -m scripts.results.resolve_qualitative --output-dir {R2} --ticker 600887.SH \
  --output {R2}/qualitative_input.json
```
- 产出 `{R2}/qualitative_report.md`（50,895 字节 / 26,871 字符）与 `{R2}/synthesis/result.json`
  （`result_type=qualitative.synthesis`、`scope=[D1..D6]`、`run.status=partial`、
  28 claims / 13 risks / 16 watchlist / 31 evidence / 55 parameters，校验器 exit 0）。
- `resolve_qualitative`：exit **0**，**`source=structured`** → **AC-1.4 成立**
  （`period_delta` 作为可选模块被接受、digest 一致；未回退目录 Markdown 兜底）。
- 综合对 `DATE-001` 的处理：按证据层分层（财报期末 2026-06-30 为唯一财务口径基准 / 期末后事项 /
  政策日期 / 股东快照 2026-09-08 / 质押统计日 2026-09-18 / 历史证据），明确 `2026-07` 与 `2026-09-18`
  **不作**财务口径基准，质押 6.29% 不得与期末净资产同时点解读；治理类诉讼/仲裁、承诺、内控一律写
  「本 run 未覆盖、未核实」，**未**写「无重大诉讼/无承诺失信」。

### Step 8 变化报告
```bash
.venv/bin/python -m scripts.results.change_report \
  --delta {R2}/modules/period_delta/result.json --synthesis {R2}/synthesis/result.json \
  --prior-synthesis output/600887_伊利/runs/20260925T042255414048Z/synthesis/result.json \
  --evidence-index {R2}/evidence/index.json --reconciliation {R2}/synthesis/reconciliation.json \
  --output {R2}/synthesis/change_report_context.json
```
- 上下文 67,534 / 160,000 字符 → 默认预算充裕、零丢弃。
- 产出 `{R2}/change_report_2026H1.md`（32,295 字节 / 16,572 字符，八节）与
  `{R2}/change_report_2026H1.json`（13,024 字节，`schema=investment.change_report/1.0`、
  `business_trend=稳定`、`requires_full_rerun=false`、差异清单 11 条、监控项 10 条）。

### Step 9 写入台账
```bash
.venv/bin/python scripts/runs.py finish --company-dir output/600887_伊利 --run-dir {R2} \
  --status complete --primary-period 2026H1 \
  --report-periods "2023FY,2024FY,2025FY,2026Q1,2026H1" \
  --artifact report={R2}/qualitative_report.md \
  --artifact change_report={R2}/change_report_2026H1.md \
  --artifact qualitative_input={R2}/qualitative_input.json \
  --artifact change_report_sidecar={R2}/change_report_2026H1.json \
  --conclusion-changed "…"
```
- 退出码 **0**；`latest.json` 指向 `20260925T053458757536Z`；
  `history.jsonl` 里本条的 `framework.git_commit = b23270b880e25…`，与 `run.json`（创建时）**一致**
  —— 本次 run 全程未改代码，指纹自洽。

### Step 10 下游新鲜度
```bash
.venv/bin/python scripts/analysis_status.py --company-dir output/600887_伊利 --ticker 600887 --json
.venv/bin/python scripts/runs.py export --company-dir output/600887_伊利
```
- `analysis_status`：退出码 **1**，状态 `stale`，**唯一** reason 是 `downstream_stale`
  （`value_computed` / `buy_sell_basis` 基于旧期次），`recommended_action: report-update`。
- `runs.py export`：四条 run 的时间线正确（baseline → 20260920 → 第一轮 → 第二轮）。
- **未自动重生成买卖计划**（规范要求由 owner 显式触发）。若要清标记：
  `runs.py downstream --company-dir output/600887_伊利 --fresh value_computed`（或 `--fresh all`）。

## 3. 第一轮（`20260925T042255414048Z`）的差异与披露

第一轮唯一与第二轮不同的地方是 **Step 6 显式传了 `--max-chars 120000`**，原因是当时
`synthesis` 的默认值（40,000）装不下真实载荷（发现 F6）。当时的实测梯度：

| `--max-chars` | 实际载荷 | `dropped_count` |
|---|---|---|
| 40000（当时默认） | 39,874 | 148 |
| 60000（2026-09-20 那轮人工用的值） | 59,996 | 128 |
| 80000 | 79,986 | 85 |
| **120000** | **96,496** | **0** |
| 200000 | 96,496（饱和） | 0 |

该披露的目的：**当时这一处不是 monkeypatch（是 `synthesis` 的文档化参数）**，
且按 AC-1.8「不允许只用运行时补丁绕过」的要求**当次登记**为待修项（任务 T8），
随后由 PR #46 修好默认值——**第二轮证明不再需要它**。

## 4. AC-1.9 判据逐条对照（以第二轮为主证据）

- [x] **只读 HOME + 真实 token 下复跑一次迭代**：HOME=`/tmp/ro-home`(555)，token 来自 `.env`；
  Step 1B 真实联网采集 exit 0，两轮执行后 HOME **均为空目录**。
- [x] **不使用任何运行时补丁**：第二轮无 monkeypatch、无临时改环境变量、无手工改文件、未放宽判定、
  **未传任何预算覆盖参数**（第一轮的那处覆盖已披露并已修复，本轮不需要）。
- [x] **产出完整 run**：`history.jsonl` / `latest.json` / `record.json` / `runs/<id>/run.json`，
  且台账与 run 的框架指纹一致（`b23270b`）。
- [x] **manifest**：`run_manifest.json`，`validate_manifest_inputs` 预检无错。
- [x] **结构化结果**：6 个 `modules/*/result.json` + `synthesis/result.json` + `synthesis/reconciliation.json`
  + `qualitative_input.json`（`source=structured`）。
- [x] **两篇报告**：`qualitative_report.md` + `change_report_2026H1.md`。
- [x] **命令与观察写进实跑记录**：本文件。

## 5. 顺带在真实环境被行使的其它判据

| 判据 | 实证 |
|---|---|
| AC-1.2（token 不写 HOME） | 两轮 Step 1B 在只读 HOME 下 exit 0，`~/tk.csv` 全程未生成 |
| AC-1.4（`period_delta` 参与 digest、不再 `result_digest does not match`） | 两轮 `resolve_qualitative` 均 exit 0 且 `source=structured` |
| AC-1.5（默认预算不丢卡） | 第二轮默认参数：载荷 97,359、`dropped_count=0`、逐卡片 `omitted` 全 0 |
| AC-1.6（`finish --artifact name=path` 的路径与存在性校验） | 两轮 `finish` 均用相对路径传入、exit 0，台账里存的是绝对路径 |
| AC-1.3（run id 不自相分叉） | 两轮 `prepare` 均沿用 `runs.py new` 签发的 id，未出现分叉告警 |

## 6. 本次实跑发现的问题与去处（26 条）

完整证据、复现命令与「我是怎么验的」见
[`AC-1.9-实跑发现清单`](./2026-09-25-REQ-006.1-AC-1.9-实跑发现清单.md)（同目录）。

| 处置 | 条目 |
|---|---|
| **已修**（REQ-006 任务 T8，PR #46） | F6（`synthesis` 默认预算装不下真实载荷，AC-1.5 的直接判据）、F15（丢弃计数少算：同预算下 148 → 220） |
| **已登记为证据**（不改判据） | F2 → `REQ-009.4` 的 AC-4.5 现状问题 |
| **已登记**（新受理子需求 `REQ-006.2`，归组 `AC-2.1`~`AC-2.7` + 实跑判据 `AC-2.8`） | F1、F3、F4、F5、F7、F8、F9、F10、F11、F12、F13、F14、F16、F17、F18、F19、F20、F21、F22、F23、F24、F25、F26、F27 |
| **不登记**（经核实为设计如此，5 条） | `dropped` 逐项表只留 3 个样本；bundle `budget.actual_chars` 是整包 JSON 长度；附注「万元」单位有标注；`market_data:1` 是被索引的（只是未被 D3 选中）；`prior_analysis` 按设计只进 D7 的 bundle |

其中几条**只有真跑才能看见**、且已做成判定性证据（直接打真实接口或反算）：

- **F9**：`fina_indicator` 不认 `revenue_yoy`（静默丢弃该字段），正确字段是 `or_yoy`/`tr_yoy`，实测回 **4.1345 / 4.1285**。
- **F11**：`pledge_stat` 返回的 `total_share=632536.07` 与用 §1「总市值 ÷ 现价」反推的 **632,536.06 万股**吻合
  → 接口单位就是万股，而 `other_data.py:436-438` 又除了一次 `1e4`（**差 10,001 倍**）。
- **F8**：§9 分部表重复行 + 字面 `nan`，按行朴素汇总 74,829.08（比真实合计高 **16%**）。
- **F17**：§13.1 的风险警示只报 2007→2008 / 2008→2009，本期减值暴增反而没报。
- **F22**：bundle 每节只给 1 条代表摘录（索引里 MDA 有 8 块、§17 有 4 块），D2 的市场份额与 Capex 证据引不到。
- **F12**：`MDA` 章节的前置 buffer 页（非经常性损益表）**恰好**被选为 D5 的代表证据，真 MD&A 一个字没进材料。

## 7. 未做与边界

- **未**重生成下游（`value_computed` / `buy_sell_plan`）：按规范必须由 owner 显式触发，
  因此 `analysis_status` 合理地停在 `stale / downstream_stale`。
- **未**在本轮修复除 T8 之外的任何代码：其余 24 条全部登记（写入 `REQ-006.2`），以保持本 run 指纹自洽。
- 两轮的模块结论均为 `partial`（数据层缺口所致，逐条登记）；最终综合的 `run.status` 如实记为 `partial`，未粉饰。
- 同一期间、同一 `selection` 下，两轮 agent 对 `period_delta` 的 `run.status`
  （`complete` → `partial`）与定性参数（`恶化/重大` → `稳定/一般`）判定不同 → 已登记为 F26。

## 8. owner 签署

- [ ] 我已过目以上命令与观察，确认与我的授权一致
      （含第一轮的 `--max-chars 120000` 覆盖及其披露、以及「仍然跑了 Step 1B 真实采集」）
- [ ] 我确认本实跑记录可用于 REQ-006.1 的收口报告
- 签署人 / 日期：______________________
