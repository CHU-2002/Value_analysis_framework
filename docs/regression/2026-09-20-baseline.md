---
date: 2026-09-20
covered-until: e453255
reviewer: CHU-2002（REQ-006 引入批量回归门禁时的基线留档）
full-suite: "1440 passed, 3 skipped, 覆盖率 76.35%"
coverage: "76.35%"
---

# main 全量回归记录（门禁启用基线）

## 覆盖范围

`covered-until: e453255` —— 建立本门禁时 `main` 的 HEAD。
自此前推，`main` 上累积的功能合入才开始计数；每达 3 条必须再留一份记录。

本次全量测试实际在 `chore/req-test-dev-flow` 分支上执行
（`main` = `e453255` + REQ-006 的流程改动），作为计数的起点留档。
分支模型在评审中调整过一次：不维护长期集成分支，改为「一个特性一条特性分支、特性分支直接合 `main`」，本记录按调整后的模型计数特性合入。

## 全量测试

```bash
make verify
```

结果：1440 passed, 3 skipped，覆盖率 76.35%，约 54s；测试 scope 32 支文件 / 1443 用例，均在预算内。
（数字在本门禁启用 PR 的最终提交上复测。）

## 逐条验收

本记录是门禁启用时的基线留档，早于「逐条 AC 验收」机制；其后每个特性合入 main 时
都会带独立验收报告（见 `docs/verification/`），批量回归记录只需汇总引用。

## 结论

通过。基线建立，后续按阈值触发。
