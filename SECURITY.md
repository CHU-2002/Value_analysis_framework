# 安全政策 (Security Policy)

## 支持的版本

本项目的安全修复应用于 `main` 分支的最新代码。

## 报告漏洞

请**不要**通过公开 Issue 报告安全漏洞。

请通过 GitHub 的私密漏洞报告功能提交：

1. 打开仓库的 **Security** 标签页
2. 点击 **Report a vulnerability**
3. 填写复现步骤、影响范围与可能的修复建议

如果无法使用该功能，请通过维护者的 GitHub 主页联系方式私下联系。

我们会在收到报告后尽快确认，并在修复发布前与你保持沟通。请在修复发布前避免公开披露细节。

## 范围

以下问题属于本项目的安全范围：

- 命令注入、路径穿越、任意文件读写
- 凭证或 token 泄露（如将 `.env`、token 写入日志或提交）
- 依赖引入的已知高危漏洞
- 结构化结果管线中可绕过血缘 / 证据校验的缺陷

## 不属于范围

- 需要本地已具备可信执行环境的纯计算错误
- 第三方数据源（Tushare、CNINFO 等）自身的可用性或数据准确性
- 仅影响本地开发、且不涉及敏感数据的配置问题

## 最佳实践

- 永远不要提交 `.env`、`*.token` 或任何真实凭证
- 使用最小权限的 API Token
- 在 Issue、PR 与日志中遮蔽敏感信息
