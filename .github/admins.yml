# 管理员名单（GitHub 用户名，每行一个）。
#
# 管理员拥有仓库全部权限，可以直接合并 Pull Request，并绕过必需检查与 review。
# 添加管理员：在 admins 下追加一行 “- username”，然后到
# Actions → Setup Branch Protection → Run workflow 应用一次即可生效。
#
# 同时请把该用户名加到 .github/CODEOWNERS，用于指定审阅人。

admins:
  - CHU-2002
